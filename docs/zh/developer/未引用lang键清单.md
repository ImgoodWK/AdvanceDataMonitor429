# 未引用 lang 键清单

本文档列出 `en_US.lang` / `zh_CN.lang` 中存在、但在 Java 源码与 `assets/advancedatamonitor/` 资源（手册 JSON、assistant 配置等）中未找到引用的键。

- lang 键总数 / total keys: **552**
- 未引用键数 / unreferenced: **0**

> `item.*.name` / `tile.*.name` 若通过 `setUnlocalizedName` / `setBlockName` 映射则视为已引用。 部分键可能通过动态拼接使用，需人工复核。
