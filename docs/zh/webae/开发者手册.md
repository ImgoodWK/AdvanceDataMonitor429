# TeXTech WebAE 控制台开发者文档

> 受众：开发者 · 最后同步：2026-07  
> 玩家说明见 [用户手册](用户手册.md) · 技术总览见 [技术文档](../developer/技术文档.md)

---

## 目录

- [1. 概述](#1-概述)
- [2. 架构总览](#2-架构总览)
- [3. 包结构](#3-包结构)
- [4. 配置](#4-配置)
- [5. REST API 端点列表](#5-rest-api-端点列表)
- [6. 前端架构](#6-前端架构)
- [7. 线程模型](#7-线程模型)
- [8. 安全模型](#8-安全模型)
- [9. 网络包](#9-网络包)
- [10. 命令](#10-命令)
- [11. 各子系统设计摘要](#11-各子系统设计摘要)
  - [11.1 存储监控](#111-存储监控)
  - [11.2 电力监控](#112-电力监控)
  - [11.3 GT 机器监控](#113-gt-机器监控)
  - [11.4 配方搜索](#114-配方搜索)
  - [11.5 样板管理](#115-样板管理)
  - [11.6 AE 合成下单](#116-ae-合成下单)
  - [11.7 主题系统与仪表盘自定义](#117-主题系统与仪表盘自定义阶段-21--22)
  - [11.8 物品图标缓存与材质包](#118-物品图标缓存与材质包阶段-31)
  - [11.9 聊天系统](#119-聊天系统阶段-e)
  - [11.10 玩家信息与皮肤 URL](#1110-玩家信息与皮肤-url阶段-e)
  - [11.11 命令触发上传](#1111-命令触发上传阶段-e)
  - [11.12 仪表盘设置面板与单组件颜色](#1112-仪表盘设置面板与单组件颜色阶段-e)
  - [11.13 导航栏三态与顶栏刷新状态](#1113-导航栏三态与顶栏刷新状态阶段-e)
  - [11.14 存储总览可配置与 CPU 独立页面（Phase 3）](#1114-存储总览可配置与-cpu-独立页面phase-3)
  - [11.15 电力页可配置与防闪烁（Phase 4）](#1115-电力页可配置与防闪烁phase-4)
  - [11.16 科技风主题 + 图表动效 + 图标渲染优化（Phase 8）](#1116-科技风主题--图表动效--图标渲染优化phase-8)
  - [11.17 网络拓扑图](#1117-网络拓扑图频道车道-ae_budget_v2--磁盘持久化)
  - [11.18 页面可见性轮询优化（Phase 4b）](#1118-页面可见性轮询优化phase-4b)
  - [11.26 世界地图视图](#1126-世界地图视图phase-ab--ae-透视层)
- [12. 前端资源](#12-前端资源)
- [13. 调试与故障排查](#13-调试与故障排查)

---

## 1. 概述

WebAE 控制台是 TeXTech 模组内嵌的 **浏览器可访问** HTTP 管理面板。玩家可以通过任意现代化浏览器，实时查看服务器端的 AE2 存储状态、电力/蒸汽消耗速率、GT 机器运行情况、搜索 NEI 配方、编辑 AE2 样板，以及批量提交 AE2 合成订单。

技术栈：
- **服务端**: NanoHTTPD 轻量级 HTTP 服务器，嵌入 Minecraft 服务端进程
- **前端**: React 18 + TypeScript + Ant Design 5 单页应用（SPA），Vite 5 构建，内联 SVG 图表（`ChartTrendSvg` 等），通过 Fetch API 调用 REST 接口；完全离线打包（无 CDN 依赖）
- **渲染**: MVC 模式，后端只返回 JSON，所有 UI 由浏览器端 React 渲染

## 2. 架构总览

```
浏览器 (React + Ant Design + 内联 SVG 图表)
    ↓ HTTP REST API (JSON, Bearer Token)
[ NanoHTTPD 服务 ]
    ↓ CountDownLatch 主线程采集
[ HandlerTick 任务队列 ]
    ↓
[ AE2 API / GT API ]
```

核心数据流：

1. **HTTP 请求进入**：NanoHTTPD 工作线程解析 URL/method，经 `WebApiRouter` 分发到对应的 handler
2. **主线程数据采集**：所有需要访问 Minecraft 世界状态的操作（AE2 网络查询、GT 机器状态读取、样板注入等）通过 `CountDownLatch` 阻塞 HTTP 线程，将任务提交到 `HandlerTick` 主线程任务队列，完成后唤醒 HTTP 线程返回结果
3. **快照缓存**：高频轮询的存储/电力数据通过 `SnapshotScheduler` 定时在主线程采集后缓存，HTTP 请求直接读取缓存，避免每次阻塞主线程

## 3. 包结构

全部源码位于 `com.imgood.textech.webae/` 包下（231 文件），详细逐文件清单见 `project-structure-details.mdc`。

| 子包 | 文件数 | 作用 |
|------|--------|------|
| `webae/` 根 | 3 | `WebConsoleServer` + `WebConsoleUrlHelper` + `WebAeLocalDataDir` |
| `webae/auth/` | 5 | Token/登录码/认证中间件/OP 校验 |
| `webae/api/` | 1 | `WebApiRouter` REST 路由分发 |
| `webae/api/handler/` | 37+ | 各 REST 端点实现（含 `ServerDiagnosticsHandler`） |
| `webae/cache/` | 2 | TTL 快照缓存 + `SnapshotScheduler`（storage/GT/pattern/p2p/cells/networks/monitor/scanner） |
| `webae/perf/` | 1 | `WebAePerfProfiler` tick/HTTP/快照耗时诊断 |
| `webae/dto/` | 15+ | 各子系统 JSON DTO |
| `webae/power/` | 1 | 电力滑动窗口采样 |
| `webae/snapshot/` | 2 | AE2 + GT 快照采集 |
| `webae/gt/` | 2 | GT 机器状态读取 |
| `webae/metric/` | 2 | 网络指标采样 + 降采样 |
| `webae/network/` | 13 | 配方/图标/世界地图 HD 网络包 + 上传节流 |
| `webae/recipe/` | 9+ | NEI/vanilla/GT 配方采集、磁盘 meta/分块与懒加载缓存 |
| `webae/pattern/` | 8+ | 样板编码/注入/browse 缓存 |
| `webae/topology/` | 20 | 网络拓扑 + P2P 地图 |
| `webae/worldmap/` | 29 | 世界地图 meta/markers/瓦片/AE overlay（类别 ID 图）/清晰度档位/预取进度 |
| `webae/craft/` | 2 | 材料树计算器 |
| `webae/favorites/` | 1 | 收藏持久化 |
| `webae/planner/` | 1 | Factory Flow 导出 |
| `webae/events/` | 1 | SSE hub |
| `webae/icon/` | 31 | 图标材质包 + 多级回退渲染 |
| `webae/player/` | 4 | 玩家信息与皮肤 |
| `webae/chat/` | 2 | 聊天存储 |
| `webae/debug/` | 1 | 分功能 debug 日志（含 `webae-perf.log`） |

## 4. 配置

所有 Web 控制台配置通过 `[webConsole]` 节管理，由 `ConfigWebaeLoader` 加载。

| 配置项 | 类型 | 默认值 | 范围 | 说明 |
|--------|------|--------|------|------|
| `enabled` | boolean | `false` | — | 是否启用 WebAE 控制台。启动后监听配置的端口 |
| `port` | int | `8090` | 1024-65535 | HTTP 服务监听端口 |
| `bindAddress` | string | `127.0.0.1` | — | 绑定地址。默认仅本机，设为 `0.0.0.0` 可局域网访问 |
| `snapshotIntervalSeconds` | int | `30` | 0-3600 | 遗留回退快照间隔（秒）。设为 0 禁用。基本被 `refreshIntervalMs` 取代 |
| `recipeUploadEnabled` | boolean | `true` | — | 是否允许 OP 通过 `/textech web recipes upload` 命令上传 NEI 配方（Phase 2 起移除了快捷键） |
| `recipeCacheMode` | string | `full` | `lru`/`full` | 配方缓存淘汰模式。GTNH 推荐 `full`（不 LRU 淘汰） |
| `maxRecipeCacheMB` | int | `256` | 1-2048 | 配方缓存的最大近似内存（MB）；`lru` 模式超过时淘汰，`full` 模式仅告警 |
| `recipeUploadBatchesPerTick` | int | `3` | 1-32 | 客户端配方上传每 tick 发送批次数 |
| `recipeSearchMinIntervalMs` | int | `1000` | 0-5000 | 模糊配方搜索 `/api/recipes/search?q=` 每 owner 最小间隔（毫秒） |
| `recipeKeepMemoryAfterUpload` | boolean | `false` | — | 上传并写盘后是否保留服务端内存全量配方；默认 `false`（清堆，浏览器分块同步；材料树/兜底 API 经 `ensureLoaded` 懒加载） |
| `recipeSyncChunkSize` | int | `400` | 50-2000 | 浏览器同步每个分块文件 / `GET /api/recipes/sync/chunk` 的配方条数 |
| `nesqlRepositoryPath` | string | `` | — | NESQL 导出仓库根路径（`/textech web icons import-nesql`）；留空 → `<实例根>/TeXTech/WebAE/`（与客户端配方导出目录相同） |
| `neiDeepScanItemsPerTick` | int | `0` | 0-512 | NEI 物品驱动深度扫描（默认 0；`/textech web recipes upload deep` 启用全量 item 扫描） |
| `iconMissingDispatchPerTick` | int | `8` | 1-64 | IconMissingQueue 懒加载派发数/tick |
| `iconDirectRenderEnabled` | boolean | `false` | — | HTTP 404 同步直渲（阻塞）；默认关，日常用异步队列 + SSE |
| `iconDirectRenderTimeoutMs` | int | `3000` | — | 同步直渲最大等待 ms |
| `iconDirectRenderPerTick` | int | `4` | — | 客户端每 tick 同步直渲数 |
| `powerSampleWindowSeconds` | int | `60` | 10-600 | 电力/蒸汽速率计算的滑动窗口时长（秒） |
| `gtDefaultScanRadius` | int | `16` | 1-256 | 数据映录器批量扫描 GT 机器的默认半径 |
| `refreshIntervalMs` | int | `10000` | 1000-60000 | 服务端采集与前端轮询的统一刷新间隔（毫秒） |
| `gtRefreshIntervalMs` | int | `30000` | 1000-60000 | GT 机器快照采集间隔（毫秒） |
| `maxNetworksDisplayed` | int | `5` | 1-20 | 网页同时显示的网络数上限 |
| `tokenLifetimeHours` | int | `0` | 0-8760 | Web 认证 Token TTL（小时）。0=永不过期；>0 时按签发时间 + TTL 校验 |
| `iconCacheEnabled` | boolean | `true` | — | 是否启用物品图标缓存与 `/api/icon` 服务 |
| `iconUploadEnabled` | boolean | `true` | — | 是否允许显式 C→S 上传（`/textech web icons upload` / import）；**不**开启 HTTP 404 懒加载 |
| `iconLazyCaptureEnabled` | boolean | `false` | — | 为 true 时 GET `/api/icon` miss 才入队 `IconMissingQueue`（须聊天同意后派发） |
| `iconLazyPreferOpOnly` | boolean | `true` | — | 懒加载同意仅询问 OP |
| `iconPackEnabled` | boolean | `true` | — | 是否允许管理员 zip 上传材质包（`POST /api/icon/pack`） |
| `metricSampleIntervalMs` | int | `30000` | 1000-60000 | AE 网络指标历史采样间隔（毫秒） |
| `metricSampleWindowSeconds` | int | `300` | 60-3600 | 网络指标历史滑动窗口时长（秒） |
| `patternBrowsePageSize` | int | `80` | 20-200 | 样板 browse API 默认分页大小 |
| `patternBrowseMaxTotal` | int | `20000` | 1000-100000 | 样板 browse 单网络最大条目数 |
| `patternCacheTtlMs` | int | `120000` | 5000-300000 | 样板 browse TTL 缓存时长（毫秒） |
| `topologyEnabled` | boolean | `true` | — | 是否启用 `GET /api/network/topology` 网络拓扑 API |
| `topologyCacheTtlMs` | int | `10000` | 1000-3600000 | 手动拓扑快照采集冷却（毫秒；默认 10 秒） |
| `worldMapSnapshotCooldownMs` | int | `10000` | 1000-3600000 | 手动世界地图快照请求冷却（毫秒；客户端采集；默认 10 秒） |
| `topologySnapshotPersist` | boolean | `true` | — | 是否将拓扑快照持久化到 `TeXTech/WebAE/topology/` |
| `topologySimulatedEnabled` | boolean | `false` | — | **已弃用**：拓扑「线缆模拟」视图与 `GET /api/ae2/cable-texture`；默认关闭 |
| `worldMapEnabled` | boolean | `true` | — | 是否启用 `GET /api/worldmap/*` 世界地图 API（需 `topologyEnabled`） |
| `worldMapTilePx` | int | `128` | — | **已废弃**：旧版单一瓦片像素；非 128 时迁移为 medium 档 |
| `worldMapMaxQualityTier` | string | `medium` | — | 服务端允许的最高清晰度（low/medium/high/ultra） |
| `worldMapDefaultQualityTier` | string | `medium` | — | WebAE 未设置偏好时的默认清晰度 |
| `worldMapBoundsPaddingChunks` | int | `1` | 0-16 | AE 网络 bbox 外扩 chunk 数 |
| `worldMapTileBudgetPerTick` | int | `1` | 1-32 | 每 tick 渲染 chunk 瓦片数 |
| `worldMapMaxChunks` | int | `512` | 16-4096 | 单维度最大 chunk 瓦片数（超出 clamp 并设 `boundsTooLarge`） |
| `worldMapRequireNetworkScope` | boolean | `true` | — | 瓦片仅渲染 AE 网络 scope 内 chunk |
| `worldMapViewsEnabled` | string | `flat` | — | 启用的视角（逗号分隔） |
| `worldMapObliqueEnabled` | boolean | `false` | — | 斜视角总开关；`false` 时仅俯视（与 `worldMapViewsEnabled` AND） |
| `worldMapZoomLevels` | int | `1` | 1-6 | zoom 金字塔层数；默认 1（仅 z0，前端 viewport 缩放） |
| `worldMapClientHdEnabled` | boolean | `true` | — | 是否允许客户端 GL 截图上传 |
| `worldMapClientHdBudgetPerTick` | int | `3` | 1-8 | 客户端 GL 瓦片渲染预算/tick |
| `worldMapClientCaptureMode` | string | `when_online` | — | 客户端截图策略：`off` / `ultra_only` / `when_online` |
| `worldMapClientCaptureRadius` | int | `2` | 0-8 | 探索预热 chunk 半径（0=关闭） |
| `worldMapClientCaptureBudgetPerTick` | int | `1` | 0-4 | 每 tick 主动捕获瓦片数 |
| `worldMapProgressiveFallback` | boolean | `true` | — | miss 时返回低清/Dynmap 裁剪占位（`upgrading`） |
| `worldMapClientHdTimeoutMs` | int | `5000` | 1000-30000 | 等待客户端 GL 超时后回退服务端渲染 |
| `worldMapAeOverlayEnabled` | boolean | `true` | — | 是否渲染 AE 透视 overlay 层 |
| `worldMapAeOverlayIncludeCables` | boolean | `true` | — | AE overlay 是否包含线缆 |
| `worldMapAeOverlayQualityTier` | string | `medium` | — | AE 叠加层瓦片清晰度（与地形 quality 解耦） |
| `worldMapAeQualityBoost` | boolean | `false` | — | 含 AE 设备的 chunk 地形 +1 档（默认关闭） |
| `worldMapRenderEngine` | string | `uv` | — | 俯视引擎：`legacy`（平均色）/ `uv`（纹理 UV + 生物群系/光照） |
| `worldMapObliqueEngine` | string | `ray` | — | 斜视角引擎：`legacy`（柱体 painter）/ `ray`（逐像素光线追踪） |
| `worldMapChunkPadding` | int | `1` | 0-4 | 瓦片快照跨 chunk 查询 padding（1 = 3×3） |
| `worldMapTextureCacheMax` | int | `2048` | 256-8192 | UV/ray 方块纹理 LRU 上限 |
| `worldMapRayBudgetPerTick` | int | `1` | 1-32 | 斜视角 ray 每 tick chunk 渲染预算 |
| `worldMapRenderThreads` | int | `0` | 0-32 | 世界地图瓦片后台渲染线程数；`0`=自动（CPU 核心数 / 2，至少 1） |
| `worldMapMaxRayDepth` | int | `3` | 1-8 | ray 像素透明方块叠层上限 |
| `worldMapLowTierObliqueEngine` | string | `legacy` | — | low/medium 档斜视角降级：`legacy` / `ray` |
| `worldMapZoomBudgetPerTick` | int | `4` | 1-64 | 每 tick 最多合成的父级 zoom 瓦片数 |
| `worldMapBlockPatchesEnabled` | boolean | `true` | — | 斜视角 ray 启用 JSON/内置方块 patch（楼梯、半砖、GT 等） |
| `worldMapServerAtlasEnabled` | boolean | `true` | — | 服务端方块面纹理烘焙进单张 atlas，减少 Image 对象数 |
| `worldMapServerAtlasPx` | int | `2048` | 256-4096 | 服务端纹理 atlas 边长（像素，须为 16 的倍数） |
| `questEnabled` | boolean | `true` | — | 启用 BetterQuesting 任务书页与只读 API（BQ 未安装时无效） |
| `questSubmitEnabled` | boolean | `true` | — | 允许从 AE 网络 Web 提交物品/流体 |
| `questChainSubmitEnabled` | boolean | `true` | — | 允许链式提交（按拓扑顺序处理前置，可合成后提交） |
| `questSubmitMaxStacks` | int | `64` | 1-512 | 单次提交最大物品种类数 |
| `questCraftWaitTimeoutMs` | int | `120000` | 5000-600000 | 合成后提交/链式合成等待超时（毫秒） |
| `questEscrowEnabled` | boolean | `true` | — | AE 虚拟 escrow：提交/持有检测前锁定物料，失败或 Retrieval 成功后退回；合成路径会预锁已有并在产物到账后 appendLock |
| `questEscrowTimeoutMs` | int | `120000` | 5000-600000 | escrow 会话超时（毫秒）；超时自动退回 AE |
| `questFluidAllContainersOption` | boolean | `false` | — | 为 true 时 Web 提交面板可勾选「计入全部流体容器」；关闭时流体/单元等价仅认 GT/IC2 单元 |
| `questCacheTtlSec` | int | `300` | 30-3600 | 任务线定义缓存 TTL（秒）；进度缓存约为其 1/10 |

**GTNH / 大型整合包 TPS 友好默认值**（新玩家首次生成 cfg 即为此档；旧 cfg 中低于阈值的项会在启动时被内存覆盖并打 WARN，不会写回磁盘）：

| 键 | 默认 | 说明 |
|----|------|------|
| `refreshIntervalMs` | `10000` | 快照/轮询间隔 |
| `gtRefreshIntervalMs` | `30000` | GT 机器快照 |
| `patternCacheTtlMs` | `120000` | 样板预采集 |
| `metricSampleIntervalMs` | `30000` | 仪表盘指标采样 |
| `worldMapTileBudgetPerTick` | `1` | 世界地图每 tick chunk 预算 |
| `worldMapMaxQualityTier` / `worldMapAeOverlayQualityTier` | `medium` | 地图与 AE 叠加层档位 |
| `worldMapAeQualityBoost` | `false` | 关闭 AE chunk 提质量 |
| `dashboardMaxTracksGlobal` / `dashboardMaxItemTracks` | `16` / `8` | 减少仪表盘跟踪项 |
| `recipeSearchMinIntervalMs` | `1000` | 配方模糊搜索节流 |
| `recipeKeepMemoryAfterUpload` | `false` | 上传写盘后清堆；浏览器 Fetch 分块到 IndexedDB |
| `recipeSyncChunkSize` | `400` | 同步分块大小 |

**安全提醒**：默认绑定 `127.0.0.1`。所有 `/api/` 端点强制 Bearer 认证（无关闭配置）。修改 `bindAddress` 为 `0.0.0.0` 将暴露给局域网，务必配合防火墙或 SSH 隧道。管理员专用端点（刷新）额外要求 OP 等级 >= 2。

**调试开关**：`[debug] debugWebae`（默认 false，由 `ConfigDebugLoader` 加载）控制 WebAE 诊断日志，目前用于 `NeiRecipeCollector` 配方采集阶段输出 handler 数量、numRecipes 总和与过滤前后配方数，便于排查「配方搜索无结果」类问题。

## 5. REST API 端点列表

| 方法 | 路径 | 认证 | 管理员? | 说明 |
|------|------|------|---------|------|
| POST | `/api/auth/exchange` | **否** | 否 | 用 6 位登录码兑换 owner Token（5 分钟 TTL、单次使用；body `{"code":"123456"}`） |
| GET | `/api/auth/login` | 是 | 否 | 认证后返回玩家信息（playerUuid） |
| GET | `/api/config` | 是 | 否 | 公开的客户端可读配置（refreshIntervalMs、gtRefreshIntervalMs、maxNetworksDisplayed、tokenLifetimeHours、themePresets（兼容，等同 themeColors）、themeColors、themeLayouts、pageStyles） |
| GET | `/api/ui-defaults` | **否** | 否 | 整合包/模组默认 WebAE UI 设置 JSON（实例 `TeXTech/WebAE/ui-defaults.json` 优先，否则 jar `assets/textech/webae/ui-defaults.json`；无文件时 `defaults:null`） |
| GET | `/api/networks` | 是 | 否 | 列出玩家的可用 AE2 网络；**HTTP 只读缓存**（`cached`/`timestamp`）；`SnapshotScheduler` 按 owner 预采集；`?refresh=1` 异步强制重建（不阻塞 HTTP） |
| GET | `/api/storage?network=<id>` | 是 | 否 | 缓存的存储快照（未命中返回上次快照 + `cached:false`） |
| GET | `/api/storage/batch?networks=0,1,2` | 是 | 否 | 批量缓存读取 |
| GET | `/api/storage/items?network=<id>&cursor=&limit=200&sort=amount_desc&search=` | 是 | 否 | 物品 cursor 分页（内存切片；`nextCursor`/`totalEstimate`/`snapshotVersion`；cursor 过期 409） |
| GET | `/api/storage/fluids?network=<id>&cursor=&limit=&sort=&search=` | 是 | 否 | 流体 cursor 分页（同上） |
| GET | `/api/storage/essentia?network=<id>&cursor=&limit=&sort=&search=` | 是 | 否 | 源质 cursor 分页（同上） |
| POST | `/api/refresh?network=<id>` | 是 | **是（OP）** | 强制存储重采集（非 OP 返回 403） |
| POST | `/api/refresh/batch?networks=0,1,2` | 是 | **是（OP）** | 批量强制存储重采集 |
| GET | `/api/power?network=<id>` | 是 | 否 | 缓存的电力/蒸汽快照 |
| GET | `/api/power/batch?networks=0,1,2` | 是 | 否 | 批量缓存读取 |
| POST | `/api/power/refresh?network=<id>` | 是 | **是（OP）** | 清电力缓存（下一采样 tick 重填） |
| POST | `/api/power/refresh/batch?networks=0,1,2` | 是 | **是（OP）** | 批量清电力缓存 |
| GET | `/api/gt/machines?network=<id>` | 是 | 否 | 缓存的 GT 机器列表快照 |
| GET | `/api/gt/machines/batch?networks=0,1,2` | 是 | 否 | 批量缓存读取 |
| POST | `/api/gt/machines/refresh?network=<id>` | 是 | **是（OP）** | 强制 GT 重采集 |
| POST | `/api/gt/machines/refresh/batch?networks=0,1,2` | 是 | **是（OP）** | 批量 GT 重采集 |
| GET | `/api/recipes/sync/manifest` | 是 | 否 | 配方磁盘目录元数据（revision/chunkCount/handlers；不加载全量入内存） |
| GET | `/api/recipes/sync/chunk?index=N` | 是 | 否 | 按块读取 `recipe-chunks/chunk-NNNN.json`（浏览器 IndexedDB 同步） |
| GET | `/api/recipes/handlers` | 是 | 否 | 列出配方处理器类型及数量（需内存或元数据） |
| GET | `/api/recipes/status` | 是 | 否 | 配方缓存状态（磁盘/内存/懒加载） |
| GET | `/api/recipes/browse?handler=<id\|all>&offset=&limit=` | 是 | 否 | 服务端分页浏览兜底（`ensureLoaded`；响应含 `total`） |
| GET | `/api/recipes/search?output=<name>&handler=<id>` | 是 | 否 | 按产物 registry 精确搜索（服务端兜底） |
| GET | `/api/recipes/search?input=<name>&handler=<id>` | 是 | 否 | 按原料 registry 精确搜索（服务端兜底） |
| GET | `/api/recipes/search?q=<text>&handler=&offset=&limit=` | 是 | 否 | 模糊搜索（服务端兜底；分页，响应含 `total`） |
| GET | `/api/recipes/suggest?q=<text>&limit=` | 是 | 否 | 物品自动补全（服务端兜底；registry + 显示名） |
| GET | `/api/recipes/{handlerId}/{recipeIndex}` | 是 | 否 | 获取单个配方（含 gridSlots / GT 参数；服务端兜底） |
| POST | `/api/order` | 是 | 否 | 提交单笔合成订单；body 可选 `cpuName`、`patternId`（优先于 itemName） |
| POST | `/api/order/batch` | 是 | 否 | 批量提交合成订单（items 项可带 `patternId` 按样板下单；body 可选 `cpuName`） |
| GET | `/api/order/list` | 是 | 否 | 列出活跃订单 + 历史订单（`{success,orders,history}`；进度：`ICraftingLink` + CPU `startItemCount/remainingItemCount`，按 networkId；history 按 owner 隔离） |
| GET | `/api/order/status?jobId=<id>` | 是 | 否 | 查询单个订单状态；未知 jobId 返回 404（不再伪造 completed） |
| POST | `/api/order/cancel` | 是 | 否 | 取消当前玩家全部待处理/活跃订单 |
| GET | `/api/order/templates` | 是 | 否 | 列出当前 owner 的批量下单模板（`TeXTech/WebAE/web-order-templates.json`，按 ownerUuid 隔离） |
| PUT | `/api/order/templates` | 是 | 否 | 覆盖保存 owner 模板列表；body `{ "templates": [{ id, name, cpuName?, networkId, items:[{ itemName, amount, patternId? }], updatedAt }] }`；校验名称非空、items≥1、amount≥1、每模板 items≤50 |
| GET | `/api/interfaces?network=<id>` | 是 | 否 | 枚举 ME 接口（含坐标、槽位状态、`machineRecipeType`、`existingPatterns` 已有样板摘要） |
| POST | `/api/pattern/encode` | 是 | 否 | 编码样板；body 可选 `networkId` + `consumeBlank`（默认 networkId≥0 时 true），主线程从 AE 网络扣 1 空白样板；不足返回 `code: NO_BLANK_PATTERN` |
| POST | `/api/pattern/inject` | 是 | 否 | 注入样板到接口；body 含 `consumeBlank`（默认 true，编码已扣时可传 false 避免重复扣除）；注入前扣空白样板 |
| GET | `/api/patterns?network=<id>` | 是 | 否 | 列出当前网络所有 ME 接口样板（富信息）；**HTTP 只读缓存** `patterns_rich`；样板变更时与 browse 一并失效 |
| GET | `/api/patterns/browse?network=<id>&q=&offset=&limit=&source=both\|grid\|interface` | 是 | 否 | Grid（`ICraftingGrid`）+ Interface 槽位双源合并分页 browse；**HTTP 只读缓存**（`cached`/`timestamp`），后台 `SnapshotScheduler` 按 `webPatternCacheTtlMs` 预采集；owner 离线时复用 `WebAeOwnerContext.getOwnerPlayerOrFake`；`PatternBrowseInvalidationGridCache` 监听 `MENetworkCraftingPatternChange` + Web PUT/DELETE/inject 主动 `invalidateAll` |
| POST | `/api/patterns/browse/refresh?network=<id>` | 是 | **是（OP）** | 管理员强制重建 browse 缓存（主线程采集，不阻塞 GET） |
| GET | `/api/patterns/grid/<gridKey>?network=<id>` | 是 | 否 | Grid 单条目详情（`gridKey`=`grid:<index>`，与 browse 索引一致）；返回完整 inputs/outputs |
| GET | `/api/patterns/<id>` | 是 | 否 | 单个样板详情（id = `<x>:<y>:<z>:<dim>#<slot>`） |
| DELETE | `/api/patterns/<id>` | 是 | **是（OP）** | 从接口槽位删除样板并触发 `MENetworkCraftingPatternChange` |
| PUT | `/api/patterns/<id>` | 是 | **是（OP）** | 编辑回写样板（解码新 NBT → 写回槽位 → 触发网络样板变更事件） |
| GET | `/api/icon?item=<itemId>&pack=<pack>&meta=<int>&size=<16\|32\|64>` | 是 | 否 | 取物品/流体图标 PNG（带 ETag + Cache-Control）；磁盘 miss 立刻 404 并异步入队（默认同步直渲关闭） |
| GET | `/api/ae2/cable-texture?type=smart\|covered\|dense` | 是 | 否 | **已弃用**（需 `topologySimulatedEnabled=true`）：拓扑线缆模拟 Fluix 材质；默认关闭时 503 |
| GET | `/api/icon/packs` | 是 | 否 | 列出已安装材质包，响应含 `defaultPack` 字段（最近上传的 pack，无则 null） |
| GET | `/api/icon/sync/manifest?pack=&mode=` | 是 | 否 | 材质包 revision 元数据（整包同步用） |
| GET | `/api/icon/sync/bulk?pack=&mode=` | 是 | 否 | 整包 zip（内存构建；仅 Settings 手动/用户开启 auto-sync，禁止登录默认） |
| POST | `/api/icon/pack?pack=<packName>` | 是 | **是（OP）** | 上传材质包 zip（Zip Slip 防护） |
| GET | `/api/chat/history?limit=<n>&since=<ts>` | 是 | 否 | 拉取聊天历史（默认 limit=200，可选 since 增量） |
| GET | `/api/chat/since=<ts>` | 是 | 否 | 增量拉取 since 之后的新消息 |
| POST | `/api/chat/send` | 是 | 否 | 网页发送聊天（body `{content}`，从 Token 取 playerUuid，校验后追加 `source=web` 消息并广播到游戏内 `[Web] <name>: <content>`；被封禁/空/超长内容拒绝） |
| GET | `/api/players` | 是 | 否 | 返回 `{online:[...],offline:[...]}`，每项含 uuid/name/online/onlineMs/lastLogin/lastLogout/skinUrl |
| GET | `/api/players/since=<ts>` | 是 | 否 | 增量拉取玩家在线状态变化 |
| GET | `/api/players/online/history` | 是 | 否 | 返回在线人数历史趋势 `[{"ts":,"count":}]`（最近 ~60 分钟，每 30s 一个采样点，供仪表盘趋势组件） |
| GET | `/api/players/locations` | 是 | 否 | **Phase6** 在线玩家坐标 `{locations:[{uuid,name,x,y,z,dim,online}]}` |
| POST | `/api/auth/guest-invite` | 是 | 否（主人） | 生成可分享访客 Token 链接 `{token,url}`；可选 body `networkKeys:string[]`（缺省=全部网）；主人禁用时 `401 webae_disabled` |
| GET | `/api/admin/players` | 是 | **是（admin grant/OP）** | 玩家摘要列表（disabled、networkCount、缓存计数等） |
| GET | `/api/admin/players/:uuid/access` | 是 | **是** | 自有网（含 `networkKey`/suspended）+ 作为访客的 token/网 ACL 详情 |
| POST | `/api/admin/players/:uuid/disable` | 是 | **是** | 账号停用：撤销全部 token/登录码、清缓存、停采集；对方后续 API `401 webae_disabled` |
| POST | `/api/admin/players/:uuid/enable` | 是 | **是** | 重新启用账号 |
| POST | `/api/admin/players/:uuid/clear-cache` | 是 | **是** | 清除该玩家快照/拓扑/地图缓存 |
| POST | `/api/admin/players/:ownerUuid/networks/:networkKey/suspend` | 是 | **是** | 停用单网（含网主；稳定键 `dim:x:y:z`）；业务 API `403 network_suspended` |
| POST | `/api/admin/players/:ownerUuid/networks/:networkKey/resume` | 是 | **是** | 恢复单网 |
| POST | `/api/admin/players/:actorUuid/acl` | 是 | **是** | body `{ownerUuid,networkKey,effect:deny\|allow}` actor 级 deny 覆盖 |
| POST | `/api/admin/players/:actorUuid/guest-tokens/revoke` | 是 | **是** | body `{token}` 撤销访客令牌 |
| GET | `/api/network/metrics?network=<id>` | 是 | 否 | AE 网络指标历史（物品/流体/源质/字节/CPU 忙碌率/GT 活跃数等滚动窗口采样，`NetworkMetricSampler`） |
| GET | `/api/network/metrics/fluids?network=<id>&fluids=water,lava` | 是 | 否 | 钉选流体 amount 趋势（上限见 `dashboardMaxFluidTracks` / 单次请求截断为 `dashboardMaxTracksPerWidget`） |
| GET | `/api/network/metrics/items?network=<id>&items=mod:item,...` | 是 | 否 | 钉选物品存量趋势（网络中不存在记 0；上限见 cfg） |
| GET | `/api/network/metrics/entities?network=<id>&entities=cpu:Name,gt:0:1:2:3&fields=...` | 是 | 否 | 钉选 CPU/GT 数值趋势（默认 `craftingProgress` / `progressPercent`） |
| GET | `/api/network/topology?network=<id>&mode=logical\|spatial` | 是 | 否 | AE 网络拓扑图（logical=`ae_budget_v2` 频道预算：dense 32→4×smart 8 + 角色舱；spatial=空间 bin；非真实布线；TTL 见 `topologyCacheTtlMs`；`topologyEnabled=false` 时 503） |
| GET | `/api/network/cells?network=<id>` | 是 | 否 | 网络存储元件字节摘要 + 无限元件检测（I5）；**HTTP 只读缓存** |
| GET | `/api/network/balance?networks=0,1&minSurplus=&minShortage=&limit=` | 是 | 否 | 多网存储平衡建议（只读；对比缓存 snapshot，返回短缺网/余量网/可调配数量；Phase 8） |
| GET | `/api/network/p2p?network=<id>` | 是 | 否 | P2P 隧道按 frequency 分组地图（Phase 10；需 `topologyEnabled`）；**HTTP 只读缓存** |
| GET | `/api/worldmap/progress?network=<id>&view=&dim=&quality=` | 是 | 否 | 瓦片批量预渲染进度（per-chunk terrain/ae 状态；queued/rendering/done/empty/failed） |
| GET | `/api/worldmap/meta?network=<id>` | 是 | 否 | 世界地图 meta（维度 bbox、tilePx、qualityTiers[]、max/defaultQualityTier、markerCount、`boundsTooLarge`；**Phase A**；需 logical 快照；`worldMapEnabled=false` 或 `topologyEnabled=false` 时 503） |
| GET | `/api/worldmap/markers?network=<id>` | 是 | 否 | 扁平化 AE 设备标记（来自 logical 快照；无快照返回 `code:no_logical_snapshot`） |
| GET | `/api/worldmap/tiles/{view}/{dim}/{cx}/{cz}.png?network=&quality=` | 是 | 否 | 地形瓦片 PNG（scope 外透明；miss 时入队渲染） |
| GET | `/api/worldmap/tiles/{view}/ae/{dim}/{cx}/{cz}.png?network=&quality=` | 是 | 否 | AE 透视 overlay 瓦片 PNG |
| POST | `/api/worldmap/invalidate?network=&views=&layer=&quality=` | 是 | 否 | 失效缓存并按 allowedChunks 批量 prefetch terrain+ae |
| GET | `/api/craft/tree?item=&amount=&network=&maxDepth=` | 是 | 否 | 材料树（`required`/`inStock`/`toCraft`/`patternId`；Phase 4.1） |
| GET/PUT | `/api/favorites` | 是 | PUT 否（guest 只读） | 收藏列表（`web-favorites.json`；Phase 4.2） |
| GET/POST | `/api/planner/plans` | 是 | POST 否（guest） | 计划列表 / 新建（`plans.json`；Phase 4.4） |
| PATCH/DELETE | `/api/planner/plans/<id>` | 是 | 否（guest） | 编辑标题 / 完成 / 删除计划 |
| POST | `/api/planner/export-flow` | 是 | 否（guest） | 材料树导出（`factory-flow-v1` / `gtnh-flow-v1`；Phase 4.3） |
| GET | `/api/events/stream?token=` | 是 | 否 | SSE 告警推送 + 15s 心跳（Phase 9；Bearer 或 query token） |
| GET | `/api/monitor/preview?dim=&x=&y=&z=&slot=` | 是 | 否 | 监视器单槽折线预览（Phase 11） |
| GET | `/api/scanner/blocks?type=&q=` | 是 | 否 | Link Scanner 只读镜像；全量缓存后 HTTP 线程过滤 `type`/`q` |
| GET | `/api/monitor/bindings` | 是 | 否 | 数据监视器 Link/GT 绑定只读视图；**HTTP 只读缓存** |
| POST | `/api/assistant/query` body `{text,locale}` | 是 | 否 | Web 助手规则意图解析（2s 限流；无 AI 密钥；I4） |
| GET | `/api/pocket/overview` | 是 | **是（OP）** | 次元口袋最小只读概览（无物品内容；I6） |
| GET | `/api/quests/meta` | 是 | 否 | BQ 是否可用、开关、版本、任务线数量 |
| GET | `/api/quests/lines` | 是 | 否 | 任务线（章节）列表 |
| GET | `/api/quests/lines/{lineId}` | 是 | 否 | 单线节点+边（含图标、`mainQuest`、跨线幽灵节点） |
| GET | `/api/quests/progress` | 是 | 否 | 全任务进度快照（含 `updatedAt`） |
| GET | `/api/quests/search?q=` | 是 | 否 | 按名称搜索任务（最多 50） |
| GET | `/api/quests/{id}` | 是 | 否 | 任务详情（`tasks`/`rewards` 来自 BQ `TaskStorage`/`RewardStorage` 的 int 键 `DBEntry`；`requirementQuestIds`；`prerequisites[]`/`dependents[]` 含 name、lineId、state、requirementType） |
| GET | `/api/quests/{id}/analysis?network=` | 是 | 否 | AE 库存 vs 任务步骤缺料分析；可选 `includeAllFluidContainers=true` |
| GET | `/api/quests/{id}/chain-plan?network=` | 是 | 否 | 链式提交拓扑计划（前置序 + 缺料摘要）；可选 `includeAllFluidContainers` |
| POST | `/api/quests/{id}/detect` | 是 | 否 | Retrieval 持有检测；body 可选 `{networkId,includeAllFluidContainers}`，有 AE 时走 escrow+离线 completeRetrieval（访客 403） |
| POST | `/api/quests/{id}/submit` | 是 | 否 | 提交物品/流体；body `{networkId,dryRun,steps?,includeAllFluidContainers?}`；启用 escrow 时先锁定再提交（访客 403） |
| POST | `/api/quests/{id}/submit-craft` | 是 | 否 | 缺料先 AE 合成再提交；预锁已有物料，产物到账后 appendLock；job.phase 含 `crafting`/`locking`/`escrow_failed`/`done`（访客 403） |
| POST | `/api/quests/{id}/submit-chain` | 是 | 否 | 链式提交；body `{networkId,dryRun,skipMissing,craftMissing,includeAllFluidContainers?}`；需 `questChainSubmitEnabled` |
| GET | `/api/quests/submit-jobs/{jobId}` | 是 | 否 | 轮询合成后提交 job |
| GET | `/api/quests/chain-jobs/{jobId}` | 是 | 否 | 轮询链式提交 job |

任务书提交相关实现：`QuestFluidEquivalence`（流体↔单元等价）、`QuestInventoryEscrow`（AE 虚拟锁定 lock/lockPartial/appendLock/release/commit/超时退回）、`QuestSubmitService`（DETECT 走 `BqApiFacade.completeRetrievalTask`，单元 DETECT 可用游离流体虚拟栈）、`QuestCraftOrchestrator`/`QuestChainOrchestrator`（合成前预锁 → 产物到账 appendLock → `submitFromEscrow`）。进度只写入 Token owner；组队不同步。

`QuestTaskDeserializer`：`bq_standard:retrieval` + `consume=true` → Web 动作 **SUBMIT**（与流体 `bq_standard:fluid` + `consume=true` 对称）；`consume=false` 的 retrieval/fluid 为 **DETECT**。`BqApiFacade.completeRetrievalTask` 对 `consume=true` 任务拒绝 `forceComplete` 回退，须走 `submitItem`/`submitFluid` + escrow commit。

流体/单元等价（`QuestFluidEquivalence`，默认仅 GT/IC2 单元；`questFluidAllContainersOption` + 请求标志可扩到全部 FCR 容器）：
- **DETECT（物品单元）**：装满单元数 + `floor(游离流体 mB / 容量)` 加总；执行时 escrow 真实单元 + 等量流体，向 BQ 交虚拟装满单元栈。
- **SUBMIT（物品单元）**：优先真实装满单元；不足则空单元+流体装填；禁止「仅有流体」。
- **真流体任务**：可用量 = 游离 + 单元内 mB；扣减优先从单元 drain 所需 mB，余量流体+空单元退回 AE。

流体单元展示（`iconItemId` + `displayName`）：装满单元保持 `mod:id:meta`。仅 FluidDisplay 与真 `requiredFluids` 写 `iconItemId=fluid:<name>`。

本地 WebAE 联调：dev fixtures 首 Tab 为 **WebAE Test Lab**（`dev-fixtures/betterquesting/`，进 Git、不进模组 jar；见该目录 `README-dev.md`）。

| GET | `/api/alerts` | 是 | 否 | 活跃自动化告警 + `web-alerts.json` 规则镜像（A1–A5）；响应含 `canEditRules`（OP 可编辑） |
| GET | `/api/alerts/rules` | 是 | 否 | 仅返回 `rules` + `canEditRules` |
| PUT | `/api/alerts/rules` body `WebAlertsConfig` | 是 | **OP** | 校验并写入 `TeXTech/WebAE/web-alerts.json`；`WebAlertEngine` 下一 tick 读最新配置；**Webhook URL 不回显**（`***` + 末 4 位） |
| GET | `/api/server/health` | 是 | 否 | 服务器 TPS / MSPT（与 `/forge tps` Overall 同口径）/ 在线人数 / 运行时长 + 300s 滚动历史 |
| GET | `/api/server/diagnostics` | 是 | 否 | WebAE 性能诊断：tick 分段耗时、`HandlerTick` 队列深度、快照采集耗时、`snapshotWorkerBusy` / `snapshotTimeouts` / `snapshotSkippedBusy` / `snapshotSkippedQueue`、慢 HTTP / 路由 Top、相关 Config 摘要；前端「性能诊断」页轮询 |
| GET | `/api/oc/summary` | 是 | 否 | OC 只读摘要（物品种类数、CPU 忙、活跃订单、TPS；1 req/s 限流；详见 [oc-integration.md](oc-integration.md)） |
| GET | `/api/search?q=&limit=&offset=&types=&network=` | 是 | 否 | 聚合只读搜索（storage/recipe/gt/pattern；500ms 限流；分页；Phase 4a） |

认证方式：请求头 `Authorization: Bearer <token>`。所有 `/api/` 端点都需要认证；管理员专用端点额外通过 `WebAuthOpCheck` 校验玩家 OP 等级（>= 2）。

401 响应包含 `code` 字段：`missing_token`、`invalid_format`、`empty_token`、`token_expired`（仅当 `tokenLifetimeHours > 0` 时）、`invalid_token`，或账号停用时的 `webae_disabled`（同时写 `error` 字段；前端清 token 回登录页）。

网络访问拒绝：`403` + `code:network_suspended`（网停用，含网主）或 `network_access_denied`（访客 allowlist/ACL）。稳定网络键为 Monitor 坐标 `dim:x:y:z`（非 runtime `networkId`）。持久化：`web-network-suspends.json`、`web-network-acl.json`；统一校验见 `webae/access/WebAeNetworkAccess`。`WebAeNetworkKeys.fromNetworkId` / `toNetworkId` 与 HTTP 侧 `findNetworkGroups` **不得**触碰 `World`（只用 `NetworkRegistry` 登记坐标键 / 过期 connector 缓存）；世界新鲜度由主线程 `getNetworks` / `refreshHealth` 维护。

## 6. 前端架构

前端源码位于项目根目录 `webae-frontend/`，使用 Vite + React 18 + TypeScript + Ant Design 5 构建。构建产物输出到 `src/main/resources/assets/textech/webae/`，由 NanoHTTPD 直接提供。

```
webae-frontend/               # 前端源码（永久项目部分）
├── package.json              # 依赖与构建脚本
├── vite.config.ts            # Vite 配置（outDir → resources/webae，base './'）
├── tsconfig.json             # TypeScript 配置
├── index.html                # SPA 入口 HTML
└── src/
    ├── main.tsx              # React 根渲染
    ├── App.tsx               # ConfigProvider + Login/AppLayout 切换
    ├── api/client.ts         # Fetch 封装（Bearer 认证 + 错误处理）
    ├── types/dto.ts          # TypeScript 接口（镜像 Java DTO）
    ├── i18n/                 # zh + en 双语字典 + I18nProvider hook
    ├── context/AppContext.tsx  # 全局状态（认证/主题/设置/预设/连接/刷新）
    ├── theme/                # 128 配色 + 30 布局 + 126 页面样式（7 chrome + 12 构图 + 20 大胆 + 20 batch2 + 50 batch3 + 12 Printstream batch4 + 5 Auraeco batch5）+ antd 主题构建器 + 预览变量收集
    ├── hooks/                # useServerHealth / useServerDiagnostics / useSnapshotData / usePlayers / useWebAlerts / …
    ├── utils/                # format / presets / dashboardResolve / recipe / avatar / …
    ├── components/           # Login / Icon / Layout / dashboard / recipes / patterns / ordering / …
    ├── pages/                # Dashboard / Storage / … / AlertsHistory / Diagnostics / Settings / QuestBook
    └── styles/global.css     # 基础样式 + GridStack 覆盖
```

构建命令：`cd webae-frontend && npm install && npm run build`

前端开发规范见 `.cursor/rules/webae-frontend.mdc`。

技术要点：
- **构建工具**：Vite 5 + `@vitejs/plugin-react`；`tsc --noEmit` 类型检查 + `vite build` 打包；产物用 contenthash 文件名，完全离线（antd/react 全部打进 bundle，无 CDN）
- **UI 组件**：全部使用 Ant Design 5 组件；图表统一内联 SVG / CSS（`ChartTrendSvg`、`WidgetContent` 分类图、GT 页 `GtSummaryCharts`）；Dashboard 拖拽使用 `gridstack`（布局引擎例外）
- **SPA 路由**：React state 驱动页面切换（`activePage`），无 react-router；业务页统一包裹 `PageShell`（含性能诊断页）
- **共享 UI**：`navConfig.ts` 导航定义；`common/SettingRow`、`SelectableListRow`、`SelectableCard`；`global.css` 列表/导航/样板编辑工具类（`.webae-pattern-slot`、`.webae-scroll-panel` 等）；`patternEntryIconId` 统一样板图标 ID；三处 Settings Drawer 均使用 `SettingRow`
- **全局状态**：`AppContext` 管理 token/网络/lang/theme/numberFormat/presets/iconPack/localIconPack/sidebarMode/displayMode/autoRefresh/`pauseRefreshWhenHidden`/`refreshPaused`/连接状态
- **Token 稳定性**：`WebAuthToken` 内存缓存 + HandlerTick 去抖写盘 + 原子 rename；前端 `tokenRef` + 401 静默重登 + 心跳用 raw fetch 避免误 logout
- **图标双轨**：浏览器 IndexedDB 本地优先 + 服务器磁盘；miss 异步客户端补图（SSE）；`iconAutoSync` 默认 false；Settings 提供整包同步与可见补缺；性能标准见 `.cursor/rules/webae-icon-performance.mdc`；`Icon` 解析顺序：本地 → 服务器 → 缩写
- **hooks**：`useIconPackAutoSync`（可选 bulk）、`iconPrefetch.ts`（本地预热 / `fillMissingIconsFromServer`）、`visibleIconRegistry.ts`
- **配方本地优先**：OP 上传 → 服务端磁盘（`web-recipes.json` + meta + chunks）→ 玩家在配方页点「获取配方」分块写入浏览器 IndexedDB → 本地浏览/搜索；`useRecipeSync` + `recipeLocalStore`；服务端 browse/search 为兜底（`ensureLoaded`）
- **Recipe sync API**: `GET /api/recipes/sync/manifest`、`GET /api/recipes/sync/chunk?index=`；兜底仍有 browse/search/suggest
- **预设系统**：快速切换方案（主题/布局/语言/数字格式/图标包/侧栏/主仪表盘）；localStorage `webae_presets`
- **全量备份**（`utils/uiSettingsBundle.ts` + 设置页「备份与恢复」）：`WebUiSettingsBundle` v1 格式（`format:textech-webae-ui-settings`）聚合全部 localStorage 页面偏好；可选导出/导入服务端收藏、下单模板、告警规则（OP）；不含 Token / IndexedDB 图标二进制
- **默认布局**：`WebUiDefaultsStore` + `GET /api/ui-defaults`（公开，无需 Token）；`/textech web defaults status|install|clear`；首次访问且无已有 `webae_*` localStorage 时 `AppContext` 自动应用
- **整合包 / Agent 工作流**：Settings 导出 JSON → 写入 `TeXTech/WebAE/ui-defaults.json` 或 `assets/textech/webae/ui-defaults.json`；可选同步 `presets.ts` 中 `DEFAULT_*` 作为无 JSON 时代码回退
- **仪表盘**：GridStack 12 列网格，编辑模式拖拽/缩放，布局持久化 `webae_dashboard_config`（x/y/w/h）
- **WCAG 无障碍**：Skip link、aria-live（连接/倒计时）、focus-visible、图标按钮 aria-label
- **静态服务**：NanoHTTPD 将所有 `/` 路径映射为静态文件服务，读取 classpath `assets/textech/webae/`

## 7. 线程模型

核心挑战：Minecraft 服务端逻辑 **必须在主线程执行**，但 NanoHTTPD 在独立工作线程中处理 HTTP 请求。

解决方案：**CountDownLatch 主线程调度模式**

```java
// 伪代码示例
public SnapshotResult getSnapshot() {
    CountDownLatch latch = new CountDownLatch(1);
    SnapshotResult[] result = new SnapshotResult[1];

    HandlerTick.enqueueServerTask(() -> {
        result[0] = collectSnapshot();  // 在主线程采集
        latch.countDown();
    });

    latch.await(timeout, TimeUnit.SECONDS);
    return result[0];
}
```

三类采集策略：

| 策略 | 适用场景 | 阻塞 HTTP 线程？ |
|------|---------|-----------------|
| **缓存直读** | 存储/电力/GT、networks、P2P、cells、monitor bindings、scanner、patterns 富列表与 browse（`SnapshotScheduler` 定时采集） | 否 |
| **主线程实时查询** | 订单提交、样板注入/写操作、拓扑 POST 捕获、助手等 | 是（≤5–15 秒超时） |
| **客户端上传** | 配方数据（`PacketWebRecipeUpload` C→S） | 直接写入服务端缓存 |

`CountDownLatch` 超时默认 5 秒，超时后返回 HTTP 503。

## 8. 安全模型

- **Token 认证**：每位玩家一个随机 UUID，持久化到 `TeXTech/WebAE/web-tokens.json`（重新签发替换旧 Token）。TTL 可选，由 `tokenLifetimeHours` 控制（0=永不过期）
- **强制认证**：所有 `/api/` 端点都需要 `Authorization: Bearer <token>`，无关闭配置。401 响应带 `code` 字段（`missing_token`/`invalid_format`/`empty_token`/`token_expired`/`invalid_token`）
- **管理员专用端点**：`POST /api/refresh`、`/api/refresh/batch`、`/api/power/refresh`、`/api/power/refresh/batch`、`/api/gt/machines/refresh`、`/api/gt/machines/refresh/batch` 要求 OP 等级 >= 2（由 `WebAuthOpCheck` 校验）；非 OP 返回 403 + `code:admin_required`
- **默认本地绑定**：`bindAddress=127.0.0.1` 仅允许本机浏览器访问
- **无 HTTPS**：NanoHTTPD 不提供 TLS。局域网使用时通过 SSH 隧道或反向代理提供加密
- **Token 管理**：`/textech web issue`（自己）、`/textech web revoke [玩家]`（自己/OP）、`/textech web list`（OP）、`/textech web refresh [网络]`（OP，阶段 1.2）
- **不存储密码**：用户只需 Token 即可访问，不需要 Minecraft 账号验证

**安全警告**：
- `bindAddress=0.0.0.0` 会将控制台暴露给局域网所有设备，任何持有 Token 的人可查看存储、提交合成
- 建议在服务器防火墙层面限制端口访问

## 9. 网络包

WebAE 使用 5 个 Forge 网络包，覆盖配方/图标上传与命令触发上传：

| ID | 包类 | 方向 | 用途 |
|----|------|------|------|
| 26 | `PacketWebRecipeUpload` | C→S | 客户端 NEI 配方分批上传（大 payload 自动分片） |
| 27 | `PacketWebRecipeUploadAck` | S→C | 服务端配方上传确认（含总进度） |
| 28 | `PacketWebIconUpload` | C→S | 客户端物品/流体图标分批上传（itemId → base64PNG，复用分片模式） |
| 29 | `PacketWebIconUploadAck` | S→C | 服务端图标上传确认（回传 receivedChunks/totalChunks/success/message） |
| 30 | `PacketWebUploadTrigger` | S→C | 服务端命令触发客户端开始 recipes/icons 上传（`/textech web recipes upload`、`/textech web icons upload`） |

配方上传流程：
1. 客户端 `KeyBindings.uploadNeiRecipes(scope, snapshotItemIds)` 混合采集：`NeiRecipeCollector`（主线程；`deep` 启用物品驱动扫描）或 `RecipeSnapshotCollector`（`snapshot` 范围）+ `GameRecipeCollector`（后台），去重合并（NEI 优先）为**单次会话**
2. 首批 `isStart` 时 `RecipeUploadSession.onStart()` 决定是否 `clearMemoryOnly()`（每位玩家仅首个活跃会话清缓存；重叠上传忽略）
3. `RecipeUploadThrottler` 按 `recipeUploadBatchesPerTick` 节流客户端上传；经 `RecipeUploadBatcher` 按 FML 32KB 上限安全分片（默认 JSON ≤ ~32KB−512B，超大配方自动裁剪 grid/rawJson）
4. 服务端 `RecipeCacheStore.ingest()` + 末批 `rebuildHandlerCounts()`；异步去抖写盘：`web-recipes.json` + `web-recipes.meta.json` + `recipe-chunks/chunk-NNNN.json`；默认 `recipeKeepMemoryAfterUpload=false` 写盘后清堆
5. 客户端 `RecipeLocalExporter` 同步写入 `<实例根>/TeXTech/WebAE/web-recipes.json`（纯 JSON，无 gzip）
6. 全部完成后发送 `PacketWebRecipeUploadAck` 确认；浏览器需在配方页点「获取配方」经 sync API 拉入 IndexedDB

图标上传流程：
1. 客户端 `IconRenderer`（@SideOnly CLIENT）每帧离屏渲染若干物品为 32×32 PNG
2. 打包为 IconBundle（itemId → base64PNG），通过 `PacketWebIconUpload` 分片上传
3. 服务端解码写盘到 `TeXTech/WebAE/icons/<packName>/`，调用 `IconStore.recordDefaultPack` 记录最近上传 pack
4. `PacketWebIconUploadAck` 回传进度

命令触发上传流程（ID 30）：
1. OP 玩家执行 `/textech web recipes upload` 或 `/textech web icons upload`
2. `CommandWebConsole` 校验 OP（`canUseOpCommands`）与玩家身份，发送 `PacketWebUploadTrigger`（S→C，字段 uploadType=recipes/icons、packName）
3. 客户端 handler 调用 `KeyBindings.uploadNeiRecipes()` 或 `KeyBindings.triggerIconUpload(packName)` 这两个 public static 入口
4. 后续走与命令相同的上传链路（ID 26/28）

详见 `network-packets.mdc`。

## 10. 命令

**总索引**：`/textech help`（别名 `/adm help`、`/txt help`）列出 TeXTech 全部命令；help 文本在 `assets/textech/lang/` 的 `adm.command.*` 键，跟随游戏语言。

| 命令 | 侧 | 说明 |
|------|-----|------|
| `/textech help` | 服务端 + 客户端 | 命令总索引 |
| `/admassistant help` | 服务端 + 客户端 | AI 助手词库路径与热重载 |
| `/admai help` | **仅客户端** | AI API Key、模型、联网搜索等本地配置 |

`/textech web` 命令管理 Web 控制台的访问令牌与管理员操作。基础命令 `getRequiredPermissionLevel` 保持 0（issue/list/status 等对任意玩家开放），但 `recipes upload`/`icons upload` 子命令内部用 `canUseOpCommands` 校验 OP。

| 子命令 | 权限 | 说明 |
|--------|------|------|
| `/textech web issue` | 任意玩家 | 为自己生成新 Token（完整值仅显示一次） |
| `/textech web login` | 任意玩家 | 生成 6 位浏览器登录码（5 分钟 TTL、单次使用） |
| `/textech web guest <玩家>` | 监视器主人 | 向在线访客私发访客 Token |
| `/textech web copy` | 任意玩家 | 复制当前 Token 到剪贴板 |
| `/textech web revoke [玩家\|访客名]` | 自己 / OP | 撤销 Token |
| `/textech web list` | OP | 列出所有活跃 Token（仅前缀 + 签发时间） |
| `/textech web reload` | OP | 真正重载 TeXTech 配置：调用 `Config.reloadConfiguration()` 复用 `activeConfigFile` 重新加载所有分节（debug/compat/ai/voice/assistant/plannerHud/dataLoom/superOrange/matterBallDecompressor/grapple/webConsole）；部分项（`enabled`/`port`/`bindAddress` 等 web 服务器自身参数）仍需重启服务端，命令响应会列出此提示；Token 与运行时数据文件（web-tokens.json/web-players.json/web-chat.json/web-icons/）不受影响 |
| `/textech web server status` | 任意 | 查看 WebAE HTTP 服务状态 |
| `/textech web server restart` | OP | 重启 HTTP 服务 |
| `/textech web recipes upload [snapshot\|deep]` | **OP** | 客户端合并 NEI+Game 单次上传；写服务端磁盘并导出客户端 `TeXTech/WebAE/web-recipes.json`；玩家浏览器需再点配方页「获取配方」；`snapshot` 仅采集 AE 存储快照相关物品配方；`deep` 启用 NEI 全物品扫描（耗时） |
| `/textech web recipes export` | **OP** | upload 的别名，强调「导出到缓存」语义 |
| `/textech web recipes status` | 任意 | 查看服务端配方缓存状态（含磁盘缓存大小与最后保存时间） |
| `/textech web recipes clear` | OP | 清空内存与磁盘配方缓存 |
| `/textech web icons upload [pack] [mode\|all\|snapshot]` | **OP** | 校验 OP + 玩家身份后发 `PacketWebUploadTrigger` 触发自己客户端渲染上传物品图标 |
| `/textech web icons render/verify/import/import-nesql/modes/status/clear` | 见 `/textech web icons help` | 单物品渲染、验证 GUI、文件夹/NESQL 导入、模式列表、状态、清空（`clear` 异步删盘，不阻塞主线程） |
| `/textech web worldmap upload/accept/status` | 玩家 | 世界地图客户端快照采集与同意流程 |
| `/textech web refresh [网络]` | OP | 强制重采集一个网络或全部活跃网络（阶段 1.2） |
| `/textech web help` | 任意 | 显示用法（含分组 help） |

**`/admai` 客户端命令**（`CommandAIConfig`，`@SideOnly(CLIENT)`）：`key`、`clearKey`、`model`、`base`、`provider`、`network`、`search`、`searchKey`、`searchBase`、`debug`、`stream`、`status`；详见 `/admai help`。

## 11. 各子系统设计摘要

按功能域索引（实现状态：**已完成** / **待办 Phase C**）。完整 Phase 历史编号保留在章节标题中便于检索。

| 功能域 | 章节 | 状态 | 包 / 前端 |
|--------|------|------|-----------|
| 存储监控 | §11.1 | 已完成 | `webae/snapshot/` · Storage 页 |
| 电力监控 | §11.2 | 已完成 | `webae/power/` · Power 页 |
| GT 机器 | §11.3 | 已完成 | `webae/gt/` · GT 页 |
| 配方搜索 | §11.4 | 已完成 | `webae/recipe/` · Recipes 页 |
| 样板管理 | §11.5 | 已完成 | `webae/pattern/` · Patterns 页 |
| AE 下单 | §11.6 | 已完成 | `webae/craft/` · AeOrdering 页 |
| 主题/仪表盘 | §11.7 | 已完成 | `webae-frontend/` GridStack |
| 图标缓存 | §11.8 | 已完成 | `webae/icon/` · `client/` 上传 |
| 聊天 | §11.9 | 已完成 | `webae/chat/` · Chat 页 |
| 玩家信息 | §11.10 | 已完成 | `webae/player/` |
| 命令上传 | §11.11 | 已完成 | `webae/network/` packet 26–36 |
| 仪表盘设置 | §11.12 | 已完成 | Settings drawer |
| 存储/CPU 页 | §11.14 | 已完成 | Storage/Cpu 页 |
| 电力页 | §11.15 | 已完成 | Power 页 |
| 科技风主题 | §11.16 | 已完成 | CSS + IconRenderer |
| **网络拓扑** | §11.17 | 已完成 | `webae/topology/` |
| **世界地图** | §11.26 | 已完成（设备定位等增强见 backlog，非「未实现」） | `webae/worldmap/` · `TopologyWorldMapView` |
| 可见性轮询 | §11.18 | 已完成 | `usePageVisibility` |
| GT 图表 | §11.19 | 已完成 | `GtSummaryCharts` |
| 告警编辑 | §11.20 | 已完成 | `webae/alerts/` |
| Webhook/健康 | §11.20a | 已完成 | `WebhookDispatcher` |
| 监控深化 | §11.20b | 已完成 | Fluids/P2P power |
| 材料树 | §11.21 | 已完成 | `CraftTreeCalculator` |
| SSE 告警 | §11.22 | 已完成 | `EventStreamHub` |
| P2P 地图 | §11.23 | 已完成 | `P2pTunnelEnumerator` |
| 监视器预览 | §11.24 | 已完成 | `MonitorPreviewCollector` |
| PWA/移动端 | §11.25 | 已完成 | manifest + CSS |

### 11.1 存储监控

- **Handler**: `StorageHandler.java`（全量快照）、`StoragePagedHandler.java`（cursor 分页）
- **采集器**: `AeSnapshotCollector.java` 在主线程读取 AE2 网络存储统计
- **缓存**: `SnapshotCache` TTL = `webRefreshIntervalMs * 3`（容许采集抖动）；`snapshotVersion()` 供 cursor 失效检测
- **调度器（阶段 1.2）**: `SnapshotScheduler` 每 `refreshIntervalMs` 采集一次，分摊到多个 tick（每 tick 处理 `ceil(activeKeys / N)` 个，`N = intervalMs / 50`），只采集最近 2 分钟活跃的网络
- **返回数据**: items / fluids / essentia / bytes used+max / 合成处理器（`CpuEntry` 含坐标、监视器位置、remainingItems/startItems 等，Phase 3 扩展）
- **端点**: `GET /api/storage`（只读缓存，未命中返回上次快照）、`GET /api/storage/batch`、`GET /api/storage/items|fluids|essentia`（cursor 分页，默认 limit=200，sort=`amount_desc|amount_asc|name_asc|name_desc`，search 过滤；409 `cursor_stale`）、`POST /api/refresh`（管理员）、`POST /api/refresh/batch`（管理员）
- **前端**: 存储页物品/流体/源质 Tab 使用 `useStoragePaged` + `VirtualStorageTable`（`@tanstack/react-virtual` 行虚拟化 + 无限滚动）；搜索 300ms 防抖；排序切换重置 cursor；合并模式 ≤3 网络客户端 merge，>3 网络提示切换单网；顶部 `OverviewWidgetGrid` 使用分页 API 的 `totalEstimate`/`totalAmountSum` 计数（`OverviewSnapshot.itemCount` 等覆盖字段）；`useSnapshotData` 切换网络时 stale-while-revalidate（保留 per-network 缓存 + `refreshing` 指示）。CPU 独立菜单见 [11.14](#1114-存储总览可配置与-cpu-独立页面phase-3)

### 11.2 电力监控

- **Handler**: `PowerHandler.java`
- **采样器**: `PowerSampler.java` 保持 5 秒采样窗口（滑动窗口计算速率，单位 EU/t），每个采样写入 `SnapshotCache`
- **数据来源**: `WirelessPowerQuery` / `WirelessSteamQuery`（复用 AI 助手电力查询接口）
- **端点（阶段 1.2）**: `GET /api/power`（只读缓存）、`GET /api/power/batch`、`POST /api/power/refresh`（管理员，仅清缓存，下一采样 tick 重填）、`POST /api/power/refresh/batch`（管理员）
- **返回数据**: 当前 EU/蒸汽量、输入/输出速率、EU/蒸汽历史数组
- **前端**: 电力页全页可配置 GridStack（`PowerWidgetGrid` + `PowerWidgetContent`，持久化 `webae_power_config`）；默认含 EU 仪表、入/出率 statCard、蒸汽 progressBar、双序列 SVG 趋势 lineChart、蒸汽入/出 statCard；多网络 split 用 Tabs 切换单网络快照（见 [11.15](#1115-电力页可配置与防闪烁phase-4)）

### 11.3 GT 机器监控

- **Handler**: `GtMachineHandler.java`
- **采集器**: `GtSnapshotCollector.java` 通过 `GtMachineStateReader` 读取 GT 机器状态
- **调度器（阶段 1.2）**: GT 快照改由 `SnapshotScheduler` 每 `gtRefreshIntervalMs`（默认 10000 毫秒）采集，沿用存储的定速分摊模型
- **绑定**: `GtMachineBinding.java` 监控目标数据结构
- **返回数据**: 机器名称、进度百分比、当前配方、电力消耗、输入/输出仓物品
- **端点**: `GET /api/gt/machines`（只读缓存）、`GET /api/gt/machines/batch`、`POST /api/gt/machines/refresh`（管理员）、`POST /api/gt/machines/refresh/batch`（管理员）
- **前端**: 机器表格 + antd `Progress` 进度条；顶部状态分布饼图 + 配方图条形图（`components/gt/GtSummaryCharts.tsx`，数据来自 `utils/gtChartData.ts` 复用 `getGtStatusBreakdown`）；异常行高亮（`gt-row-error`）；可按状态/配方/电压排序 + 文本搜索；多选网络时合并聚合图表与表格

### 11.4 配方搜索

- **数据流**: OP `/textech web recipes upload*` → 服务端磁盘权威（不启动全量入内存）→ 玩家浏览器点「获取配方」→ IndexedDB 本地浏览/搜索
- **Handler**: `RecipeHandler.java`（含 sync manifest/chunk + 服务端 browse/search 兜底）
- **混合采集（客户端）**: `KeyBindings.uploadNeiRecipes(scope, …)` 先 `NeiRecipeCollector.collectAll(deepScan)` 或 `RecipeSnapshotCollector.collectForItems()`（客户端主线程），再 `GameRecipeCollector.collectAll()`（后台线程）；按 `handlerId:recipeIndex` 去重，NEI 优先；经 `RecipeUploadThrottler` 合并为单次上传会话
- **上传会话**: `RecipeUploadSession` 确保每位玩家仅首个 `isStart` 批次清空内存；并发重叠上传被忽略
- **NEI 解析**: `NeiRecipeCollector.extractRecipe()` 反射 `PositionedStack`；`handlerName` 经 `RecipeDisplayNames` 格式化为 `本地化名 (langKey)`；`itemId` 含 meta（`registry:meta`）
- **Game 采集**: `GameRecipeCollector` 为 ShapedRecipes 写入 grid；GT RecipeMap 写入流体与 EU/耗时；handler 显示名同上
- **上传**: `PacketWebRecipeUpload` 分批 C→S；结束时 `rebuildHandlerCounts()` 修正 handler 计数
- **缓存 / 磁盘**: `RecipeCacheStore` — 无启动全量加载；`ensureLoaded()` 懒加载（HTTP 线程可阻塞；**Server tick 线程只启动** `WebAE-RecipeCache-Load` 后台加载并立即返回，禁止同步解析全量 JSON）；任务书 analysis/submit 等在 HTTP 侧预 `ensureLoaded`；材料树在主线程且内存未加载时跳过展开；写盘产出 `web-recipes.json` + `web-recipes.meta.json`（`RecipeDiskMeta`）+ `recipe-chunks/chunk-NNNN.json`（大小由 `recipeSyncChunkSize`）；默认 `recipeKeepMemoryAfterUpload=false` 写盘后清堆；`recipeCacheMode`（GTNH 默认 `full`）仅在内存已加载时生效；模糊搜索兜底由 `RecipeSearchRateLimiter` 限流
- **Sync API**（主路径，不强制加载全量入堆）:
  - `GET /api/recipes/sync/manifest` — revision / chunkCount / handlers
  - `GET /api/recipes/sync/chunk?index=N` — 单块配方数组
- **服务端兜底 API**（材料树等；`ensureLoaded`）:
  - `GET /api/recipes/browse|search|suggest|…`
- **DTO 扩展**: `RecipeDto` 含 `gridSlots`、`gridWidth/Height`、`euPerTick`、`durationTicks`、`voltageTier`、`recipeType`
- **前端** (`webae-frontend/src/pages/Recipes.tsx` + `hooks/useRecipeSync.ts` + `utils/recipeLocalStore.ts` + `components/recipes/`)：
  - 工具栏 **「获取配方」**：对照 manifest revision，按块拉取并写入 IndexedDB；进度条可取消；revision 未变不自动重下
  - 获取完成后 **本地** 浏览/模糊搜索/补全；类别筛选、完整/合并、缩略/详细布局同前
  - **无限滚动**懒加载本地库；缩略/合并卡片 + `RecipeDetailModal`；详细视图合成格/流体/GT Tag
  - 布局切换 CSS 显示/隐藏 + `React.memo`；持久化 `localStorage.webae-recipe-layout` / `webae-recipe-display-mode`
- **清空**: `/textech web recipes clear` 清空服务端内存与磁盘（含 meta/chunks）；浏览器 IndexedDB 需各端自行清除或重新 Fetch 覆盖
### 11.5 样板管理

- **Handler**: `PatternHandler.java`（编码/注入/接口枚举）、`PatternListHandler.java`（样板总览/详情/删除/编辑回写）、`PatternBrowseHandler.java`（Grid+Interface 双源 browse 分页）、`PatternGridDetailHandler.java`（Grid 单条目完整 I/O）
- **Browse 缓存**: `PatternBrowseService.java` 分离「主线程 buildAndStoreCache」与「HTTP 线程 paginate」；`SnapshotScheduler` 对活跃网络按 `Config.webPatternCacheTtlMs` 后台预采集；GET 返回 `cached:true/false` + `timestamp`（过期仍返回 stale 数据）；owner 离线 browse 经 `getOwnerPlayerOrFake`；POST `/api/patterns/browse/refresh`（OP）强制重建；Web PUT/DELETE/inject 与 `PatternBrowseInvalidationGridCache`（监听 `MENetworkCraftingPatternChange`，`AdvanceDataMonitor.postInit` 经 `PatternBrowseGridCacheRegistrar` 注册）均调用 `invalidateAll()`
- **接口枚举**: `InterfaceLocator.java` 在主线程枚举所有 ME 接口终端；响应 DTO 含 `machineRecipeType`（机器名 + GT RecipeMap）与 `existingPatterns`（槽位/patternId/outputs/crafting）；反射访问器（`getPatterns`/`getTermName`/`getLocation`/`getInstalledUpgrades`/`saveChanges`）为 `public static`，供 `PatternListHandler` 复用
- **编码器**: `PatternEncoder.java` 将物品/流体列表编码为 AE2 样板 NBT；`decode(String)` 反向解析 NBT
- **空白样板**: `BlankPatternHelper.java` 从 AE 网络 `IStorageGrid` 提取 1 个 `materials().blankPattern()`；编码（`consumeBlank`）与注入均会扣除，不足时返回 `NO_BLANK_PATTERN`
- **注入器**: `PatternInjector.java` 在主线程将样板写入指定接口；支持 `consumeBlank: false` 跳过重复扣除（编码已扣时）
- **样板总览**: `PatternListHandler` 遍历当前网络所有 ME 接口的样板槽位，解码每个非空样板 NBT 为 `PatternListEntryDto`（inputs[27]/outputs/flags/author/sourceInterface/slot/encodedNbt），供前端仿增广样板终端展示；DELETE/PUT 触发 `MENetworkCraftingPatternChange` 通知合成处理器 重载
- **前端**（`pages/PatternEditor.tsx`，Phase 6）:
  - 左侧样板总览列表（图标+输出物+来源接口，模糊搜索+多选+批量删除/导出）
  - 右侧编辑：9×3 输入网格（图标+数量角标）+ 输出（图标+数量 + ×2/×4/×8/×16/×32/×64 倍率与 ÷2，不低于配方原始数量）
  - 配方搜索：`groupByPrimaryOutput()` 合并缩略列表，点击添加默认配方或「详情」弹窗（`RecipeDetailModal` + `onApplyRecipe`）选配方类型
  - 编码预览（扣空白样板）+ 保存回写 + 注入接口（接口下拉显示坐标/已有样板数/机器配方类型，选中后列出已有样板详情）

### 11.6 AE 合成下单（Phase 7 增强）

- **Handler**: `OrderHandler.java`
- **下单接口**:
  - 单物品 `/api/order`：body `{ networkId, itemName?, amount, rawText?, locale?, cpuName?, patternId? }`；`patternId` 优先于 itemName
  - 批量 `/api/order/batch`：body `{ networkId, cpuName?, items:[{ itemName, amount, patternId? }] }`（API 保留供内部/集成；**Web UI 不再暴露批量面板**）
- **CPU 选择**: `cpuName` 可选；`AssistantServerServices.submitCraft(..., cpuName)` 通过 AE2 `submitJob(..., ICraftingCPU, ...)` 指定 CPU，未指定则 AE2 自动分配；下单时快照 `cpuInfo`（协同/存储/并行数）写入 `OrderStatus`
- **真实进度**（`WebAeOrderProgressService`，按 `networkId` 读网格，**不**按玩家坐标搜 Link）:
  1. 下单时登记 `jobId`，`submitJob` 成功后绑定 `ICraftingLink` / `craftingId`
  2. 计算阶段：`AssistantCraftJobManager`（按 `trackingKey=jobId`）→ pending 约 1–25%
  3. 执行阶段：`(startItemCount - remainingItemCount) / startItemCount`（与 AE2 CPU GUI 同口径的**合成树步骤进度**，非最终产物个数；完成前封顶 99）
  4. 完成/取消：`ICraftingLink.isDone()` / `isCanceled()`（含游戏内取消）；Web 取消会调用 `link.cancel()`
  5. 同网络短 TTL（约 50ms）CPU 快照缓存，多订单复用；**禁止** `populatePlan` / 物品明细遍历
- **列表/历史**: `GET /api/order/list` 返回 `{ success, orders, history }`；完成/取消/失败从 active 移入内存 `historyOrders`（最多 200 条，重启清空，**按 ownerUuid 隔离**）；`OrderStatus` 含 `cpuName`/`cpuInfo`/`finalProgress`/`itemName`/`amount`/`patternId`/`networkId`/`craftingId`/`startItems`/`remainingItems`/`progressKind=steps`
- **状态/取消**: `GET /api/order/status?jobId=` 单条查询（未知 jobId → 404）；`POST /api/order/cancel` 取消当前玩家全部订单（计算 Future + 已上 CPU 的 link）
- **批量模板 API**: `GET/PUT /api/order/templates` 仍保留（持久化 `web-order-templates.json`）；Web 下单页 UI 已移除模板面板
- **底层**: `WebAeCraftService` + `AssistantServerServices` + `AssistantCraftJobManager` + `WebAeOrderProgressService`
- **前端**（`pages/AeOrdering.tsx`）:
  - 顶部 CPU `Select`（名称 + 容量 + 并行数 + 忙碌/空闲）
  - Tab「按样板」：分页浏览 + 单笔下单（产物快加 / 样板卡片购物车 / 产物弹窗）
  - Tab「按物品」：存储搜索 + 单笔下单
  - Tab「材料树」：仅展示缺口，不批量下单
  - 活跃/历史订单表轮询 `/api/order/list`（3s）；进度条 tooltip 标明步骤进度；历史表「再次下单」→ 确认后 `POST /api/order`

### 11.7 主题系统与仪表盘自定义（阶段 2.1 / 2.2）

- **主题系统**:
  - 主题 = 配色方案 × 布局预设 × **页面样式**，三者独立选择
  - 配色方案（`[data-theme-color]`）：共 **128** 套 — 经典 19 + Phase 8 科技风 5 + 构图配套 4 + 大胆批次 13 + batch2 20 + batch3 50 + Printstream batch4 12 + **Auraeco batch5 5**（`aura` / `aura-front` / `aura-design` / `aura-sys` / `aura-interact`：体素青光 / 前端青 / 设计紫 / 系统绿 / 互动蓝）
  - **页面样式**（`[data-page-style]`，`theme/pageStyles.ts`）：共 **126** 套 — chrome 7 + 构图级 12 + 大胆构图 20 + batch2 20 + batch3 50 + Printstream batch4 12 + **Auraeco batch5 5**（`aura-voxel` / `aura-spore` / `aura-dome` / `aura-sparks` / `aura-bubble`）；视觉见 `styles/bold-styles.css` + `bold-styles-batch2.css` + `bold-styles-batch3.css` + `bold-styles-batch4.css` + `bold-styles-batch5.css` + `effects-motion.css`。Printstream 以黑白几何 + Pantone 珠光渐变 + ASCII 虚线码流致敬 CS 社区印花；Auraeco 以 CSS 近似体素波纹 / 藤蔓粒子（无 Three.js），**均不对部件内容做 `clip-path` 裁切**；`effectsLevel` + `prefers-reduced-motion` 门控动效
  - **布局预设**（`[data-theme-layout]`）：共 **30** 套 — 经典 8（standard / compact / wide / sidebar-right / topnav / bottomnav / floating / split-chrome）+ **batch3 结构 22**（`chromeKind`：dual-rail / rail-only / dock / island / theater / dense-ops / magazine / split-pane / top-tabs / zen / command / tri-chrome / card-stack / hud-frame / pipeline / hero-header / status-strip / drawer-peek / corner-hub / widescreen / right-drawer / frame；见 `layout-batch3.css` + `AppLayout`）
  - **设置页外观 / 预设**：`ThemePreviewMini`（in-DOM 缩略结构预览）+ `ThemeOptionGrid` 缩略预览块；选前可见配色 / 布局 / 样式风格；内置 AppPreset 约 **121**（含 batch3 + Printstream batch4 + Auraeco batch5 的 `builtin-style-*` 与旗舰 `builtin-printstream` / `builtin-aura`）
  - 设置页：配色 + 布局 + **页面样式** + 动效强度；仪表盘编辑部件可覆盖图表样式
  - 图表配色通过 CSS 变量（如 `var(--accent)`）与主题 token 联动；Dashboard/电力趋势 SVG 随主题色更新；`effectsLevel=full` 时折线/面积/饼图/雷达/柱状图启用持续 CSS 动效（`.chart-flow-line` 等），`prefers-reduced-motion` 时关闭
  - 后端 `/api/config` 返回 `themeColors` / `themeLayouts` / `pageStyles` 列表供前端发现（前端亦内置同样目录）
- **仪表盘自定义**:
  - GridStack 11.x 通过 npm 依赖引入（`gridstack` + `gridstack.min.css`），提供拖拽/缩放网格；`pages/Dashboard.tsx` 渲染 12 列网格；主仪表盘 `gs-min-w/h=1`（自由尺寸，无硬性 2×2）
  - widget 模型（`utils/presets.ts` `DashboardWidgetConfig`）：`type`（statCard / progressBar[horizontal|circular] / lineChart / barChart / pieChart / dataTable / gauge / radarChart / **group** / **textNote** / **spacer** / **alertsSummary** / **craftingQueue**）+ `dataSource`（含 `customPins` / `none` / `alertsActive` / `craftingBusy`）+ `scope` + `width/height/x/y` + 可选 `fontSize?`/`chartSize?`/`alignment?`/`radarAxes?`/`colors?` + **`pins?`** + **`columns?`** + **`contentScale?`** + `pinsOnly?` / `gaugeThreshold?` / **`children?`**（group 嵌套）/ **`noteText?`** / **`locked?`/`noMove?`/`noResize?`/`sizeToContent?`** / **`alertThreshold?`**
  - **嵌套分组**：`type:'group'` 外层为 GridStack item，内层由 `GroupWidget.tsx` 再 `GridStack.init({ column:'auto' })`；树形 CRUD 见 `utils/dashboardTree.ts`（`flattenWidgets` / `updateWidgetById` / `widgetLayoutSignature`）；`migrateDashboardWidgets` / 导入导出递归处理 `children`
  - **编辑 UX**：编辑模式顶部组件库一键添加；底部 `.dashboard-trash` + GridStack `removable` 拖拽删除；分组标题栏可「向组内添加」；钉选历史采集对 `flattenWidgets` 递归
  - **布局/资讯组件**：`SpecialWidgets.tsx`（便签/占位/告警摘要/`useDashboardAlerts`/合成队列）
  - **数据优先编辑**：`EditWidgetModal` / 添加组件弹窗先选数据源与钉选，再选兼容图表类型（`dataSourceChartMap.ts`，`customPins` → `pinned`；`none` → layout；`alertsActive`/`craftingBusy` → feed）
  - **钉选与历史 API**：`GET /api/network/metrics/items`、扩展 `.../fluids`、`GET /api/network/metrics/entities`（`cpu:name` / `gt:dim:x:y:z`）；采样见 `NetworkMetricSampler`；上限 cfg：`dashboardMaxTracksPerWidget`（默认 10）、`dashboardMaxTracksGlobal`（16）、`dashboardMaxItemTracks` / `Fluid` / `Entity`（各 16，item 默认 8），经 `/api/config` 下发；折线为内置 timeseries ∪ 钉选历史叠加；雷达图 ≥3 钉选时用钉选当前值作轴；网络平衡行可搜索钉选
  - **Phase 2 补齐数据源**（`utils/overviewDataSources.ts` + `WidgetContent.tsx` 共享渲染）：`gtMachineList`（dataTable）；`machineByStatus`（bar/pie）；`networkCompare`（barChart）
  - **URL 深链**：`?page=order&network=0` 或 `?networks=0,1` 写入地址栏（`utils/urlNavigation.ts` + `AppContext` replaceState）；**命令面板（Phase 4a）**：`Ctrl+K` 打开 `CommandPalette.tsx`
  - 布局持久化：`localStorage.webae_dashboard_config` 存完整 `DashboardSettings`；「重置默认布局」恢复默认
  - 配置面板：编辑模式下可改数据/钉选/类型/标题/尺寸/`contentScale`/颜色/锁定与 sizeToContent；Edit Modal 改宽高后经 `widgetLayoutSignature` 重建 GridStack
  - 数据刷新：`useSnapshotData` + `useDashboardPinMetrics`（合并拉取钉选历史）+ `usePlayers` / `useNetworkMetrics` / `useDashboardAlerts`

### 11.8 物品图标缓存与材质包（阶段 3.1）

- **服务端存储**: `webae/icon/IconStore.java` 单例，管理 `TeXTech/WebAE/icons/<packName>/nei/<itemId>.png`（legacy 根目录 PNG 首次访问自动迁移）；`manifest.json` 记录 `modes[]`、`counts{}`、`uploadedAt`、`clientVersion`；`isValidPackName`/`isValidItemId`/`sanitizeItemId` 做路径穿越防护；`listPacks`/`listIcons`/`getIconFile`/`resolveWriteTarget`/`refreshPack`/`refreshIndex`/`recordModeUpload`/`migrateLegacyPackIfNeeded`；`recordDefaultPack(packName)`/`getDefaultPack()` 持久化最近上传 pack
- **REST Handler**: `webae/api/handler/IconHandler.java`
  - `GET /api/icon?item=<itemId>&pack=<pack>&mode=<mode>&meta=<int>&size=<16|32|64>` 返回 PNG（`mode` 默认 nei；磁盘可读旧 mode；**miss 立刻 404**；仅 `iconLazyCaptureEnabled=true` 时入队 `IconMissingQueue`；仅 `iconDirectRenderEnabled=true` 才同步直渲）
  - 客户端渲染（NESQL 主路径）: `IconNesqlStyleRenderer`（64×64 FBO + `GuiContainerManager.drawItem`，**输出 64×64**，对齐 NESQL exporter，无双线性缩糊）
  - 命令: `/textech web icons upload|local|pull|…`；`local` 写 `icons-local/`；`pull` 经 `PacketWebIconPullZip`(49)
  - Lazy-load SSE: **opt-in**（`iconLazyCaptureEnabled`，默认 false）+ 聊天同意 + 材质包知情提示；非 HTTP miss 默认路径
  - `GET /api/icon/packs` 列出材质包 JSON（`{success:true, packs:[...], defaultPack:"<name>"|null}`），`defaultPack` 来自 `IconStore.getDefaultPack()`，供前端首次加载选择
  - `GET /api/icon/sync/manifest` / `GET /api/icon/sync/bulk` 整包同步（前端默认不自动调用；Settings 手动或开启 auto-sync）
  - `POST /api/icon/pack?pack=<packName>` 管理员（`WebAuthOpCheck.isOp` OP>=2）上传 zip；服务端 `ZipInputStream` 解包到 `web-icons/<packName>/`，**Zip Slip 防护**（canonical 路径校验不逃出 packDir），只接受 `.png` 条目，刷新 IconStore 索引并调用 `recordDefaultPack` 记录最近上传 pack
- **与世界地图对比**：前端都会请求 HTTP；地图缺瓦片默认读快照/占位；图标服务端不 GL 渲染。图标并发高，**禁止**把同步 `IconDirectCaptureBridge` 当默认（对齐「异步占位再刷新」，而非地图 SP 同步直捕）
- **静态托管**: `WebConsoleServer.serveStatic()` 扩展，`/icons/<pack>/<itemId>.png` → 外部 `TeXTech/WebAE/icons/` 目录，canonical 路径穿越防护 + `Cache-Control`
- **路由**: `WebApiRouter` 新增 `/api/icon` 与 `/api/icon/` 前缀分支到 `IconHandler`
- **客户端渲染（NESQL 主路径）**: `GuiIconExportScreen` 每帧按 `iconRenderPerTick` 逐个渲染；活跃路径为 `IconNesqlStyleRenderer`（64×64 FBO + `GuiContainerManager.drawItem`，**输出 64×64**）经 `IconExportResolver.resolve`；流体走模组特殊路径；**仅 mode `nei`**
- **正式环境稳定性（GTNH 全量导出）**:
  - `IconRenderGuard` — 每次离屏 GL/FBO 导出后：结束未完成的 `Tessellator`、清 stencil/depth/blend 等易污染状态、恢复主 Framebuffer（防止 `Already tesselating!` 与批量导出时方形/AE 终端形「缺块」PNG）
  - `IconNesqlStyleRenderer` — 每图标 `glPushAttrib` + `clearFboBuffers`（COLOR|DEPTH|STENCIL）隔离自定义 `IItemRenderer`
  - `IconLazyRenderQueue` — 客户端懒加载队列，每 tick 最多渲染 2 个；bulk upload 开始时清空
  - **建议**：优先 `/textech web icons upload snapshot`、`/textech web icons local` 或 `import-nesql`
- **命令**: `/textech web icons upload|local|pull|render|verify|import|import-nesql|modes|status|clear|y|n`
- **NESQL 导入**: `WebAeLocalDataDir` + `NesqlIconImporter`
- **Lazy-load SSE**: **opt-in**（`iconLazyCaptureEnabled` 默认 false）+ 聊天同意 → 派发 → SSE `icon-ready`
- **外部导入**: `tools/icon-import/convert_icon_exporter.py`；`/textech web icons import` 或 zip 上传
- **IconExportResolver 活跃链**（`webae/icon/IconExportResolver.java`，批量与懒加载共用）:
  1. 流体感知 → `renderFluidAwareSlotIcon`（模组特殊路径）
  2. `IconNesqlStyleRenderer.renderItem`（NESQL 同款 `drawItem` + 单图标 FBO）
  3. `IconRenderer.createPlaceholderPng`（始终写入，避免 Web 404 无限懒加载）
  - 归档长链见 `resolveLegacy`（默认不调用）
  - `IconItemId.lookupCandidates` 与前端 `iconLookupIds` 支持 `mod:id` ↔ `mod:id:0` 双向查找（NESQL 兼容）；精确 id 优先。`GET /api/icon` 统一 `Cache-Control: max-age=86400`，响应头 `X-Icon-Exact` 仅诊断。SSE `icon-ready` 只 bump 版本重拉，**不删** IndexedDB/本机目录缓存（对齐 NESQL 静态长缓存）。
- **前端**: Settings 固定 `nei`；`iconModeFallbackChain` 为 `['nei']`；`/api/config` 的 `iconRenderModes` 仅含 `nei`；`iconAutoSync` 默认 false；拓扑/地图不向服务端批量 prefetch；Cytoscape 优先 `resolveLocalIconUrls`；性能标准见 `.cursor/rules/webae-icon-performance.mdc`
- **图标认证修复（v3.0）**: `WebAuthMiddleware.authenticate()` 新增 query token 回退：当 `Authorization` 头缺失时，从 `?token=` 或 `?access_token=` query 参数读取 token；保持安全（仍校验 token 有效性）；解决 `<img src="/api/icon?...">` 无法携带 Authorization 头导致图标 401 → 回退文字缩写的问题
- **itemId 修复（阶段 E）**: `webae/snapshot/AeSnapshotCollector` 的 `itemId` 改用 registry name（`GameRegistry.findUniqueIdentifierFor(item)`），meta 仅在 `!= 0`（且非 wildcard）时拼接；之前用 `unlocalizedName:meta` 与图标按 registry 名存储不一致，导致图标 404
- **默认 pack 修复（阶段 E）**: 前端 `AppContext` 首次加载（无 localStorage 记忆）时用 `/api/icon/packs` 响应 `defaultPack` 而非硬编码 `'default'`；`PacketWebIconUpload` 与 `IconHandler` zip 上传完成后均调用 `IconStore.recordDefaultPack` 持久化最近 pack

### 11.9 聊天系统（阶段 E）

- **DTO**: `webae/chat/ChatMessage.java`（id/senderUuid/senderName/content/timestamp/source=web|game|system）
- **存储**: `webae/chat/ChatMessageStore.java` 单例，`ArrayDeque<ChatMessage>` 环形缓冲（默认 200 条），`append`/`getSince(ts)`/`getRecent(limit)`/`latestTimestamp`；防抖保存到 `TeXTech/WebAE/web-chat.json`，重启后保留历史
- **游戏内收集**: `handler/HandlerWebChatCollector.java` `@SubscribeEvent` 监听 `ServerChatEvent`（`LoaderHandler` 注册到 `MinecraftForge.EVENT_BUS`），**不取消事件**，镜像写入 `ChatMessageStore` 作为 `source=game`
- **网页广播**: `ChatHandler.handleSend` 从 Token 取 `playerUuid` 校验，`ChatMessageStore.append(source=web)` 后通过 `FMLCommonHandler.instance().getMinecraftServerInstance().getConfigurationManager().sendChatMsg(new ChatComponentText("[Web] <name>: content"))` 广播到游戏内；被封禁玩家、空内容、超长内容拒绝
- **REST Handler**: `webae/api/handler/ChatHandler.java`
  - `GET /api/chat/history?limit=200&since=<ts>` 返回近期消息
  - `GET /api/chat/since=<ts>` 增量拉取
  - `POST /api/chat/send` body `{content}` 发送
- **响应 DTO**: `webae/dto/ChatMessageDto.java`
- **路由**: `WebApiRouter` 注册 `/api/chat/*` → `ChatHandler`
- **前端**: 见第 6 节「聊天页」

### 11.10 玩家信息与皮肤 URL（阶段 E）

- **DTO**: `webae/player/PlayerInfo.java`（uuid/name/firstLogin/lastLogin/lastLogout/totalOnlineMs/online）
- **存储**: `webae/player/PlayerInfoStore.java` 单例，内存 `Map<UUID,PlayerInfo>`；`touchLogin`/`touchLogout`/`reconcileOnline`/`getOnlinePlayers`/`getAllPlayers`/`effectiveOnlineMs`；防抖保存到 `TeXTech/WebAE/web-players.json`
- **追踪**: `handler/HandlerWebPlayerTracker.java` `@SubscribeEvent` 监听 `PlayerEvent.PlayerLoggedInEvent`/`PlayerLoggedOutEvent`（`LoaderHandler` 注册到 FML bus），写入 `PlayerInfoStore`；`HandlerTick` 每 tick 调用 `reconcileOnline` + `tickSave`
- **皮肤 URL**: `webae/player/SkinUrlResolver.java` 从 `EntityPlayerMP.getGameProfile().getProperties().get("textures")` 取 base64，用 `javax.xml.bind.DatatypeConverter` 解码（避免 Java 8+ API），解析 JSON 取 `textures.SKIN.url`；离线玩家返回 null
- **REST Handler**: `webae/api/handler/PlayerHandler.java`
  - `GET /api/players` 返回 `{online:[...],offline:[...]}`，每项含 uuid/name/online/onlineMs/lastLogin/lastLogout/skinUrl
  - `GET /api/players/since=<ts>` 增量
  - `GET /api/players/online/history` 在线人数历史趋势 `[{"ts":,"count":}]`，供仪表盘趋势组件
- **在线人数采样**: `webae/player/PlayerOnlineSampler.java` 单例，由 `HandlerWebPlayerTracker.onServerTick` 每 30s 采样一次 `MinecraftServer.getConfigurationManager().playerEntityList.size()`，维护最近 120 个采样点的滑动窗口
- **响应 DTO**: `webae/dto/PlayerDto.java`
- **路由**: `WebApiRouter` 注册 `/api/players` 与 `/api/players/online/history` → `PlayerHandler`
- **前端**: 头像优先用 Crafatar CDN（`https://crafatar.com/avatars/<uuid>`，需浏览器外网）加载玩家皮肤 2D 头像，失败/无外网回退首字母圆形占位；玩家列表可折叠（Drawer）；聊天气泡模式按发送者区分左右

### 11.11 命令触发上传（阶段 E）

- **网络包**: `webae/network/PacketWebUploadTrigger.java`（S→C，ID 30，字段 `uploadType`=recipes/icons、`packName`），在 `LoaderNetwork` 注册
- **命令侧**: `CommandWebConsole` 的 `recipes upload`/`icons upload` 子命令改为校验 OP（`canUseOpCommands`）+ 校验发送者是玩家 + 发包触发客户端上传；`getRequiredPermissionLevel` 保持 0（其他子命令不受影响）
- **客户端入口**: `KeyBindings.uploadNeiRecipes()` 与 `KeyBindings.triggerIconUpload(packName)`（Phase 2 从已删除的 `HandlerWebIconUpload` 迁入）为 public static 入口，供包 handler 调用；`uploadNeiRecipes()` 改为客户端主线程同步采集（`Minecraft.addScheduledTask`）+ 异步上传
- **后续链路**: 走与命令相同的上传链路（recipes → ID 26/27，icons → ID 28/29）

### 11.12 仪表盘设置面板与组件属性继承体系（阶段 E + WebAE 升级路线图 Phase 1）

- **全局配置**: `DashboardSettings` 类型（`utils/presets.ts`）含 `margin`、`widgetGap`、`contentInset`（UI 显示 0–24，实际内边距 = 10 + 设定值，不改变 GridStack 吸附）、`borderWidth`、`chartStretchMode`（`fit`/`stretchX`/`fill`）、`fontSize`、`chartSize`、`chartShowValueAxis/TimeAxis`、`showLastUpdated`（组件 footer 显示「N 秒前更新」）、`defaultAlignment`、`defaultColors`、`colorPresets`、`widgets`；持久化 `localStorage.webae_dashboard_config`；从旧配置加载时与 `DEFAULT_DASHBOARD_SETTINGS` 合并并 `migrateDashboardWidgets()` 迁移 sparkline/分类色
- **设置面板**: `DashboardSettingsDrawer.tsx`（antd `Tabs`：布局 / 图表 / 颜色 / 高级）——含 `contentInset`、`chartStretchMode`、颜色预设；底部 **取消/应用** 草稿模式（未应用关闭则丢弃）；高级 Tab 可选「导出/导入 widgets JSON」（剪贴板）；`showLastUpdated` 开关
- **雷达图**: `RadarChartWidget.tsx` + `EditWidgetModal` 多轴编辑器；轴值来自真实 `dataSourceValue`，组内归一化为百分比
- **图标 Wiki**: `AppContext.iconWikiEnabled`（`localStorage.webae_icon_wiki_enabled`，Settings 开关）；关闭后 AE 下单页产物卡片 `+` 快速加单仍可用
- **存储可合成**: 存储表 `amount=0` 显示「可合成」链接，经 `orderNavigation` 跳转 AE 下单并按产物搜索预填
- **属性继承体系**: `utils/dashboardResolve.ts` 提供 `resolveProp`、`resolveAllColors`、`resolveContentInset`、`resolveBorderWidth`、`resolveChartStretchMode`、`applyWidgetShellStyle`；widget 覆盖优先；颜色受 `colors.inheritDefault` 控制；`colors.borderWidth` 可单组件覆盖边框厚度
- **外壳统一**: `WidgetShell.tsx` 将 resolved 背景/边框/inset 应用到 `.grid-stack-item-content.widget-shell`（CSS 变量 `--widget-bg`/`--widget-border`/`--widget-border-width`）；Overview / Power / Dashboard 三处 Grid 均使用 `WidgetShell`
- **单组件编辑**: `EditWidgetModal.tsx`（antd `Tabs`：基础 / 布局 / 颜色 / 高级）；`WidgetColorSection.tsx` 复用颜色编辑；高级段含 `showDelta`/`significantDigits`（statCard）、`contentInset`/`chartStretchMode` 继承、`borderWidth` 继承
- **大数与趋势**: `formatLargeWithDelta` + statCard `showDelta`；`ChartTrendSvg` 支持 `stretchMode`；加载中 statCard/chart 显示 antd `Skeleton`
- **组件操作**: 三处 Grid 编辑模式均支持「复制组件」；`utils/widgetGridActions.ts` 提供 `copyWidgetConfig` / `exportWidgetsJson` / `parseWidgetsImport`
- **渲染**: `renderWidget` 用 `widget-align` 容器应用 `resolveProp` 字号、`resolveAllColors` 颜色与 `data-align` 对齐；SVG 图表 `preserveAspectRatio="xMidYMid meet"` 防拉伸；图表区高度按 `chartSize%`
- **图表调色**: pieChart 从 `[chartColor, iconColor||var(--success), var(--warning)]` 派生多色；lineChart/barChart/radarChart 用 chartColor 描边/填充
- **GridStack 重建**: `useEffect` 依赖 `layoutSignature`（`widgets.map(w=>id_w_h).join(',')`），Edit Modal 改宽高后触发网格按新尺寸重建

### 11.13 导航栏三态与顶栏刷新状态（阶段 E）

- **导航栏三态**: `AppLayout` 以 `width` 控制 expanded(240)/collapsed(60)/hidden(不渲染 Sider)；`SidebarToggleTab` 边缘标签循环三态；`sidebar-right` 切换时短暂禁用 transition 防闪动
- **顶栏刷新状态**: 绿点旁固定宽度状态文案；设置页显示数据最后更新时间与新鲜度指示（Phase 2）

### 11.14 存储总览可配置与 CPU 独立页面（Phase 3）

- **属性继承复用**: 存储页与 CPU 页总览复用 Phase 1 的 `DashboardWidgetConfig` + `resolveProp`/`resolveAllColors`（`utils/dashboardResolve.ts`）
- **类型与默认值**（`utils/presets.ts`）:
  - `StorageOverviewSettings` + `DEFAULT_STORAGE_OVERVIEW_WIDGETS` → `localStorage.webae_storage_overview_config`
  - `CpuOverviewSettings` + `DEFAULT_CPU_OVERVIEW_WIDGETS` → `localStorage.webae_cpu_overview_config`
  - `mergeOverviewSettings()` / `overviewAsDashboardSettings()` 供 Drawer/Modal 复用
- **共享组件**:
  - `components/dashboard/OverviewWidgetGrid.tsx` — GridStack 12 列、`cellHeight:56`、固定 2 行高（112px）；编辑模式添加/删除/拖拽/设置/自动排列/重置
  - `components/dashboard/WidgetContent.tsx` — statCard / progressBar / gauge 渲染
  - `utils/overviewDataSources.ts` — 存储/CPU 数据源聚合（含 `cpuActiveTotal`、`totalCpuStoragePercent`、`totalCoProcessors`、`parallelCrafting`）
  - `utils/cpuColumns.tsx` — CPU 表格列定义 + `estimateRemainingMs()`
- **存储页** (`pages/Storage.tsx`): 移除 CPU Tab；顶部 `OverviewWidgetGrid`；子表 items/fluids/essentia
- **源质页** (`pages/Essentia.tsx`，Phase 5): 侧栏 `PageId: essentia`（`ExperimentOutlined`）；源质种类/总量摘要 + `useNetworkMetrics` 的 `essentiaCountHistory` 趋势（`ChartTrendSvg`）；aspect 表格搜索/排序；多网络 merged 合并；Ctrl+K 命中源质跳转 `?page=essentia` 并预填搜索
- **CPU 页** (`pages/Cpu.tsx`): 侧边栏 `PageId: cpu`（`HddOutlined`）；可配置总览 + 可点击 CPU 表格 + 详情 `Drawer`；多网络 Tabs（split）或合并（merged）；历史合成暂显示「暂无历史」
- **后端 DTO 扩展** (`StorageDto.CpuEntry`): `x/y/z/dim`（合成链接坐标）、`monitorX/Y/Z/Dim`、`remainingItems`/`startItems`；`AeSnapshotCollector.collectCpus` 写入
- **CSS**: `.overview-widget-grid .overview-grid-item-content { height:112px }`；`.stat-card-equal` 改为固定 `height:112px`

### 11.15 电力页可配置与防闪烁（Phase 4）

- **属性继承复用**: 电力页复用 Phase 1 的 `DashboardWidgetConfig` + `resolveProp`/`resolveAllColors`
- **类型与默认值**（`utils/presets.ts`）:
  - `PowerSettings` + `DEFAULT_POWER_WIDGETS` → `localStorage.webae_power_config`
  - 默认 widgets：EU 占用 gauge、EU 入/出 statCard、蒸汽 progressBar（`steamPercent`）、电力趋势 lineChart（`powerHistory`）、蒸汽入/出 statCard
- **共享组件**:
  - `components/dashboard/PowerWidgetGrid.tsx` — GridStack 12 列、`cellHeight:64`、可变高度；编辑模式添加/删除/拖拽/设置/自动排列/重置
  - `components/dashboard/PowerWidgetContent.tsx` — statCard / progressBar / gauge / lineChart（内联 SVG 双序列 EU+蒸汽，避免 Plots 轮询闪烁）
  - `utils/powerDataSources.ts` — `PowerSnapshot`、`getPowerDataSourceValue()`、11 个电力数据源枚举
- **电力页** (`pages/Power.tsx`): 多网络 Tabs；顶部工具栏「编辑布局」；轮询时趋势图不卸载、不加 Card loading 遮罩
- **防闪烁**:
  - `useSnapshotData` 区分 `initialLoading` 与 `refreshing`；`loading` 向后兼容为 `initialLoading`
  - 趋势 widget 仅在 `initialLoading && !snapshot` 时显示 Spin；空历史保留容器 + `.chart-no-data-watermark` 水印
  - CSS：`.power-chart-stable`（`contain: layout style`）、`.power-trend-chart` 最小高度

### 11.16 科技风主题 + 图表动效 + 图标渲染优化（Phase 8 — 路线图最终阶段）

- **新主题**（`theme/colors.ts` + `styles/global.css`）:
  - `hologram` — 全息投影：青色玻璃态 + 扫描线 overlay
  - `plasma` — 等离子：紫红径向粒子脉冲背景
  - `neon-pulse` — 霓虹脉冲：荧光色 + 卡片边框呼吸动画
  - `quantum` — 量子：深蓝 + 漂移网格背景
  - `crystal` — 晶体：棱镜渐变文字 shimmer
  - 后端 `/api/config` `themeColors` / `themeLayouts` / `pageStyles` 与前端目录同步（128 / 30 / 126）；设置页 i18n `themeColor_*` / `themeLayout_*` / `pageStyle_*` 键；缩略预览见 `ThemePreviewMini`
- **图表持续动效**（`data-effects-level=full` 门控）:
  - Dashboard SVG：`.chart-flow-line`（stroke-dash 流动）、`.chart-svg-area`（面积脉冲）、`.chart-pie-group`（慢速旋转）、`.chart-radar-data`（雷达脉冲）、`.chart-bar-segment`（柱亮度脉冲）
  - Power 趋势：`PowerWidgetContent.tsx` 内联 SVG 复用相同 CSS 类
  - `@media (prefers-reduced-motion: reduce)` 关闭全部 Phase 8 动效
- **图标渲染优化**（`IconRenderer.java` + `IconItemEnumerator.java` + `IconExportResolver.java` + `IconRenderGuard.java`）:
  - A：128×128 FBO + 4× 缩放，64×64 读回降采样至 32×32
  - B：平面 icon quad UV inset（1/16 图集边距）
  - C：ItemBlock 失败时 `RenderBlocks.renderBlockAsItem` 3D 路径
  - D：`IconExportResolver` 空白检测 → 多级回退（含强制原版 RenderItem + 图集采样）→ 占位 PNG（含缩写 itemId）；自定义 `IItemRenderer` 不再因 FBO 空白而跳过写入
  - E：批次开始前绑定 items/blocks 图集
  - F：NEI 不可用时 `Item.getSubItems` 枚举 meta 变体
  - G：`IconRenderGuard` 每 slot/page/GL 回退后复位 Tessellator；`IconLazyRenderQueue` 懒加载 2/tick 限流

### 11.17 网络拓扑图（频道车道 ae_budget_v2 + 磁盘持久化）

- **包**: `webae/topology/` — `NetworkStatusEnumerator`（网络工具同源 + subtype 分类 + 驱动器样板计数）、`TopologyRules`（细粒度 subtype + `podKind`/`layer`）、`TopologyFacilityGrouper`（`subtype|itemId` 聚合）、`ChannelBranchAllocator`（4×8 智能车道；同 subtype / podKind 偏好车道；溢出可超配并标记 `overflow`）、`LogicalTopologyBuilder`（**ae_budget_v2** 频道预算树：dense trunk 32 → 4× smart lane 8 → 角色舱 pod → 设备；零频道设备挂 hub orbit；`buildStar()` 双环摘要仍可用）、`TopologySnapshotStore`（`TeXTech/WebAE/topology/<owner>-<network>.json`）、`CraftingCpuTopologyCollector`、`SimulatedLayoutBuilder`（已弃用 Manhattan pathPoints）、`TopologySnapshot`、`TopologyCache`
- **定位**: **频道预算规划图**，不是真实 AE 线缆/路径仿真。心智模型：一块控制器容量 ≈ 致密干 32 → 最多 4 路智能 8
- **logical 布局 (ae_budget_v2)**:
  - 控制器 hub；Energy Cell/Acceptor 左右 orbit（0 频道）
  - dense trunk（32ch）→ 始终可见的 4× smart lane（各 8ch，空车道也保留）
  - 耗频道设备（含终端/总线/接口/监视/P2P 等）按 `ChannelBranchAllocator` 分入车道，再按 `podKind`（access/io/craft/sense/tunnel/…）收成角色舱
  - Drive/Chest/IO/CPU/Quantum 挂控制器 **零频道环**（orbit），不占四车道
  - 边 `kind`：`capacity_trunk` / `capacity_lane` / `pod_uplink` / `device_link` / `orbit_link`；容量边带状 `used/max`，超配 `overflow`
  - `meta.channelModel=ae_budget_v2`；`meta.lanes[]`；`meta.orbitCounts`
- **节点 DTO**: `subtype`、`layer`、`podKind`、`parentId`、`patternCount`、`patternSlots[]`、`branchIndex`、`layoutSector`
- **前端**: `TopologyCytoscapeGraph`（**preset 频道车道布局**；主干/车道不再折叠）；`topologyLaneLayout.ts`；`topologyAbstractEdges` 保留容量脊骨；设备清单按 pod 分组；设置：空车道 / 折叠舱 / 隐藏主干
- **REST**: `GET /api/network/topology`（读磁盘/内存快照，不自动重建；logical 为频道预算图）；`POST /api/network/topology/snapshot?force=1`（OP 强制采集）
- **配置**: `[webConsole] topologyEnabled`（默认 true）、`topologyCacheTtlMs`（默认 **10000** 毫秒 / 10 秒）、`worldMapSnapshotCooldownMs`（默认 **10000**，世界地图客户端快照请求冷却）、`topologySnapshotPersist`（默认 true）、`topologySimulatedEnabled`（默认 **false**，线缆模拟已弃用）；世界地图见 §11.26

### 11.18 页面可见性轮询优化（Phase 4b）

- **Hooks**:
  - `hooks/usePageVisibility.ts` — 监听 `visibilitychange` + `focus`/`blur`，返回 tab 是否可见
  - `hooks/useVisibilityAwarePolling.ts` — 基于 `useInterval`；`pauseWhenHidden=true` 且 tab 隐藏时 `delay=null` 暂停；恢复可见时立即 poll 一次
- **设置**: `localStorage.webae_pause_refresh_when_hidden`（默认 `true`）；Settings → 数据更新时间 →「标签页隐藏时暂停刷新」开关
- **全局刷新** (`AppContext`): `autoRefresh` 定时器与 TopBar 倒计时在 `refreshPaused` 时冻结；tab 恢复可见时 `refreshTick++` + 重置倒计时
- **覆盖轮询点**:
  - `AppContext` 连接心跳 30s
  - `useWebAlerts` 10s
  - `useSnapshotData`（依赖 `refreshTick`，随全局刷新暂停）
  - `useNetworkMetrics` dashboard 指标
  - `usePlayers` 玩家/在线趋势
  - `pages/Chat.tsx` chat 2.5s + players 10s
  - `pages/AeOrdering.tsx` 订单 3s（`autoRefreshOrders`）
- **TopBar UX**: 暂停时显示「已暂停」替代倒计时（`aria-live`）

### 11.19 GT 页图表与状态可视化（Phase 4c）

- **工具**: `utils/gtChartData.ts` — `getGtStatusBreakdown`（复用 `overviewDataSources`）、`getGtRecipeMapBreakdown`（Top 8 + Other）、`isGtMachineErrorRow`
- **组件**: `components/gt/GtSummaryCharts.tsx` — 双列 Card：状态饼图（active/error/idle）+ 配方图水平条形图；纯 SVG/CSS，无外部图表库
- **表格增强**: `GtMachines.tsx` 进度列改用 antd `Progress`（异常状态 `exception`）；`rowClassName` 高亮 Error/Problem 行
- **多网络**: `useSnapshotData` 合并 `selectedNetworks` 后传入摘要图表与表格

### 11.20 告警规则 Web 编辑（Phase 4d）

- **校验**: `webae/alerts/WebAlertsConfigValidator.java` — 轮询 1–300s、CPU 卡住 1–120min、频道阈值范围、库存规则 itemId/fluidName 二选一
- **持久化**: `ConfigWebAlertsLoader.save()` 写入 `web-alerts.json` 并刷新 30s 内存缓存；`WebAlertEngine` 每 tick 经 `get()` 读取
- **REST**: `PUT /api/alerts/rules` 需 `WebAuthOpCheck.isOp(actorUuid)`；`GET /api/alerts` 响应 `canEditRules`
- **前端**: `components/settings/AlertsRulesEditor.tsx` — Settings 告警 Tab 可编辑表单（开关/阈值/库存规则增删）；非 OP 只读 Descriptions

### 11.20a 外向集成：Webhook + 服务器健康（参考计划 Phase 2）

- **Webhook**（学 AE2 Web Integration）：`web-alerts.json` 扩展 `webhooks[]`（`id`/`url`/`enabled`/`events`/`mention`）；`WebhookDispatcher.java` 单线程 `BlockingQueue` 异步 POST（Discord embed 或通用 JSON）；队列上限 1000；HTTP 超时 5s；`WebAlertStore.recordNew` 时入队；URL 仅存服务端，API 响应经 `sanitizeForClient` 掩码
- **事件类型**: `inventory_threshold` / `cpu_stuck` / `gt_error` / `order_complete` / `channel_overload` / `server_tps_below` / `automation_craft`
- **TPS 告警**: `serverTpsBelowEnabled` + `serverTpsThreshold`（默认 15）+ `serverTpsDurationSeconds`（默认 60）；`WebAlertEngine` 读 `ServerHealthSampler`
- **健康采样**: `webae/health/ServerHealthSampler.java` — 读 `MinecraftServer.tickTimeArray` 最近 100 tick 均值（与 `/forge tps` Overall 同公式：`mspt = mean(ns)×1e-6`，`tps = min(20, 1000/mspt)`）；`HandlerTick` 每 tick 刷新，约 1s 写入 300s 滚动窗口；`GET /api/server/health`（`ServerHealthHandler`）；性能诊断页顶部 TPS/MSPT 同源
- **性能诊断**: `webae/perf/WebAePerfProfiler.java` + `SnapshotWorkerPool` — tick 分段 / HTTP 路由 / 快照采集耗时；single-flight + `queueDepth≥48` 背压；500ms 软超时仅 WARN，持闸门至任务完成（硬等上限 30s）；`GET /api/server/diagnostics`（含 `snapshotWorkerBusy` / timeouts / skips）；`[debug] webaePerf` → `logs/textech/webae-perf.log`（慢 tick≥5ms / 慢 HTTP≥200ms 始终记一条）；前端 `pages/Diagnostics.tsx` + `useServerDiagnostics`。**新增 `/api/*` 必须接入诊断**，约定见 `.cursor/rules/webae-perf-diagnostics.mdc`。

### 11.20b 监控深化（参考计划 Phase 3）

- **流体专页**（3.1）：`pages/Fluids.tsx` 侧栏独立入口；种类/总量摘要 + `fluidCountHistory`；表格钉选最多 10 种流体；`GET /api/network/metrics/fluids` + `useFluidMetrics` 轮询 5 分钟窗口
- **电力降采样**（3.2）：`PowerSampler.toDto` 历史 >120 点时 `MetricDownsampleUtil` 服务端降采样；`GET /api/network/p2p` 响应增加 `powerChannels[]`（frequency / avgEuPerTick / endpointCount）；拓扑 P2P 模式 `P2pMapPanel` 电力摘要表
- **自动下单**（3.3）：`web-alerts.json` 扩展 `automationRules[]`（`craft_when_below` + itemId/threshold/patternId/cpuName/cooldown/requireCpuIdle/maxTriggersPerHour）；`OrderSubmitService` + `AutomationCooldownTracker`；`WebAlertEngine` 评估后服务端线程提交合成；历史 `type=automation_craft`；Settings `AlertsRulesEditor` 可编辑

### 11.21 材料树计算器（Phase 6）

- **包**: `webae/craft/` — `CraftTreeCalculator`（`RecipeCacheStore.searchByOutput` 递归，深度默认 8、上限 16，环检测）
- **存储缺口**: 读取 `SnapshotCache` 当前网络 storage snapshot 的 `available`/`missing`
- **REST**: `GET /api/craft/tree?item=&amount=&network=&maxDepth=` → `{success,networkId,amount,tree:{...}}`；HTTP 线程直接读配方缓存（无需主线程）
- **前端**: `components/patterns/CraftTreePanel.tsx`；`AeOrdering.tsx` 新增 `craftTree` Tab

### 11.22 SSE 告警流（Phase 9）

- **包**: `webae/events/EventStreamHub.java` — 按 owner 管理 `PipedOutputStream` 订阅；`WebAlertStore.upsert` 广播 `event: alert`
- **心跳**: `HandlerTick` 每 tick 调用 `tickHeartbeats()`（15s 间隔 `: heartbeat`）
- **REST**: `GET /api/events/stream` — `text/event-stream` chunked；连接关闭时 `unregister`
- **前端**: `hooks/useEventStream.ts`（`EventSource` + `?token=`）；与 `useWebAlerts` 10s 轮询并存

### 11.23 P2P 频道地图（Phase 10）

- **包**: `webae/topology/P2pTunnelEnumerator` — 过滤 `IGrid.getMachinesClasses()` 中 P2P 类；反射 `getFrequency`/`isOutput`
- **DTO**: `P2pTunnelDto` + `P2pMapSnapshot.fromTunnels` 按 frequency 分组
- **REST**: `GET /api/network/p2p?network=<id>` — **缓存直读**（scheduler 预采集）；需 `topologyEnabled`；`?refresh=1` 异步重建
- **前端**: `NetworkTopology.tsx` 视图 `p2p` + `P2pMapPanel.tsx`（卡片网格布局，按频率卡片展示端点 IN/OUT 标签与坐标，支持搜索/排序）

### 11.24 监视器折线预览（Phase 11）

- **包**: `webae/monitor/MonitorPreviewCollector` — 读取 `TileEntityAdvanceDataMonitor.getDoubleValues(slot)`
- **REST**: `GET /api/monitor/preview?dim=&x=&y=&z=&slot=` — 主线程 10s 超时；校验 owner
- **前端**: `MonitorBindings.tsx` 每槽 Preview 按钮 + Drawer + `ChartTrendSvg`

### 11.25 PWA 与移动端（Phase 12）

- **manifest**: `webae-frontend/public/manifest.webmanifest` → Vite 构建到 `assets/textech/webae/`
- **HTML**: `index.html` 增加 `theme-color`、Apple mobile meta、`link rel=manifest`
- **CSS**: `styles/global.css` `@media (max-width: 768px/480px)` — 侧栏 fixed、内容 padding、表格字号
- **Layout**: `AppLayout` 根 `Layout` 增加 `webae-layout` 类

### 11.26 世界地图视图（三源采集 / 客户端快照 / SP 直读）

- **默认模式** `worldMapSnapshotMode=client_only`：**服务端不渲染**地形/AE 瓦片；多人服由同意 upload 的**玩家客户端**按优先级逐块采集并上传快照（`TeXTech/WebAE/map-snapshots/{owner}/{network}/v{n}/`）。
- **三源采集链**（`worldMapSnapshotSourcePriority`，默认 `dynmap,journeymap,client_gl`，仅 `textech.cfg` 可改）：**逐 chunk 降级**——有 Dynmap 用 Dynmap，否则 JourneyMap，再否则客户端 GL。Manifest `source` 为单源 id 或 `mixed`；`sourceStats` 记录各源 chunk 数。
  - **Dynmap（客户端）**：先裁剪本地 `dynmap/web/tiles/`；缺失时 HTTP GET `{dynmapBaseUrl|worldMapDynmapClientFetchUrl}/tiles/{world}/flat/{z}/{x}_{y}.png`（每 tick 最多 1 次 HTTP）。
  - **JourneyMap**：读 `journeymap/data`（`WorldMapJourneyMapFsReader` 供 SP 服务端直读复用）。
  - **GL 兜底**：使用 job `tilePx` 对应质量档，不再固定 `worldMapClientFallbackQuality=low`。
- **AE 透视层**：快照 upload 与 SP 直读均走客户端 `WorldMapAeVectorOverlayRenderer`（拓扑 placement + **设备 block 顶面纹理 category-ID 栅格化**；线缆/附件线宽 `worldMapAeCableWidthBlocks` 默认 0.25 方块）。服务端 legacy 路径 `WorldMapAeOverlayRenderer` 对 air-block 设备使用 dot marker，有实体 block 按顶面纹理 rasterize 类别 ID。
- **瓦片拼接（前端）**：西北角锚定 + `WORLD_MAP_TILE_FLIP_Y`（`scaleY(-1)`，仅翻转南北）；单元测试见 `worldMapTerrain.test.ts`。
- **AE 透明度（前端）**：`worldMapAeOverlayOpacity`（**0–1**）仅写入 AE 着色像素 alpha；层间 `isolation` + AE `mix-blend-mode: normal`；re-tint 保留旧图直至新图就绪。
- **快照保留**：服务端/客户端仅保留 **current + previous** 两版；瓦片 API 与 IndexedDB 支持上一版回退。
- **单人直读**（`worldMapSpDirectServe=true`）：集成服快照缺失时，`GET /api/worldmap/tiles` 直读本地 Dynmap/JM 或经 `PacketWorldMapDirectCaptureRequest/Response`（ID 45/46）触发客户端 GL；响应头 `X-WorldMap-Tile-Status=direct`、`X-WorldMap-Tile-Source=...`；短 TTL 内存缓存 `worldMapSpDirectCacheTtlSec`。
- **前端**：统一 `TopologyWorldMapView` 快照 chunk 瓦片；已移除 Leaflet Dynmap 地形视图（「在 Dynmap 中打开」外链按钮保留）。Meta 只读暴露 `snapshotSourcePriority`、`snapshotSourceStats`、`spDirectServe`。
- **更新触发（仅手动）**：Web 工具栏 **「更新地图快照」** → `POST /api/worldmap/snapshot/request`（切换 Tab/刷新不会自动重复请求）；游戏内 `/textech web wm up` 或 `/textech web worldmap upload`；附近玩家聊天可点击 **接受/拒绝** 或 `/textech web wm y`。pending 请求去重，同一网络不会重复刷屏 Offer。请求冷却 `worldMapSnapshotCooldownMs`（默认 10 秒）。
- **网络包**：37–46（含 SP 直读 45/46）；详见 `network-packets.mdc`。
- **Legacy**：`worldMapSnapshotMode=legacy` 恢复旧服务端渲染 + Dynmap 桥接；`client_only` 下 `WorldMapTerrainFallback` 服务端 Dynmap 裁剪不参与快照。
- **Dev 依赖**：`dependencies.gradle` 以 `devOnlyNonPublishable` 引入 JourneyMap 5.2.18 与 **GTNH-Web-Map (GWM) 0.4-beta-1**（GTNH 版 Dynmap）；仅 `runClient`/`runServer` 生效，发布 jar 不捆绑。

## 12. 前端资源

前端源码位于项目根目录 `webae-frontend/`（React + TypeScript + Ant Design + Vite），构建产物输出到 `src/main/resources/assets/textech/webae/`，由 NanoHTTPD 直接提供。此目录在客户端不需要（@SideOnly 无要求），仅用于服务端 HTTP 服务。

构建产物清单（Vite 打包，contenthash 文件名）：
- `index.html` — SPA 入口，引用 hashed JS/CSS + PWA meta
- `manifest.webmanifest` — PWA manifest（Phase 12）
- `assets/index-[hash].css` — 全局样式 + 高级/极简模式特效层
- `js/index-[hash].js` — 主 bundle（页面组件 + i18n + context + utils）
- `js/antd-[hash].js` — Ant Design + icons（~1.1MB，gzip ~353KB）
- `js/react-[hash].js` — React + ReactDOM（按需 chunk）
- `js/gridstack-[hash].js` — GridStack（tree-shaken）

总 gzip 体积约 450KB，完全离线打包（无 CDN 依赖）。构建命令：`cd webae-frontend && npm install && npm run build`。

## 13. 调试与故障排查

| 问题 | 可能原因 | 检查 |
|------|---------|------|
| 启动后 `localhost:8090` 无响应 | `enabled=false` | 检查 `config/textech/textech.cfg` `[webConsole] enabled` |
| 端口占用 | 其他服务占用 8090 | 修改 `port` 配置 |
| 认证失败 (401) | Token 过期或被撤销 | 运行 `/textech web issue` 重新生成；查看 `code` 字段（`token_expired` vs `invalid_token`） |
| 刷新返回 403 | 非 OP 调用管理员刷新 | 改用游戏内 `/textech web refresh` |
| 存储/电力短暂为空 | 缓存冷启动 | 等待一个 `refreshIntervalMs` 周期；调度器只采集活跃网络 |
| 存储/电力持续为空 | AE2 网络未连接或无活跃网络 | 打开网页控制台/选择网络，让 `SnapshotScheduler.markActive` 运行 |
| 配方搜索无结果 | 未上传 / 未点「获取配方」/ IndexedDB 空 / 用了 displayName 精确搜索 | OP `/textech web recipes upload snapshot`（或 `upload`）后，网页配方页点「获取配方」；开启 `[debug] debugWebae=true` 查看 NEI 采集日志 |
| 图标显示为文字缩写 | 认证失败 / pack 名不一致 / itemId 格式不匹配 | v3.0 已修复：`WebAuthMiddleware` 支持 `?token=` query 参数回退，前端 Icon 组件自动拼接 token；确认 `AeSnapshotCollector` 用 registry name；检查 `TeXTech/WebAE/icons/default-pack.txt` |
| 聊天消息不显示 | 未上传或 Token 无 playerUuid | 确认 `ChatMessageStore` 已持久化 `web-chat.json`；检查 `/api/chat/since` 响应；游戏内消息需 `sendChatMsg` 广播 |
| 玩家头像回退首字母 | 离线模式或 GameProfile 无 textures | `SkinUrlResolver` 对离线玩家返回 null 属正常；正版玩家检查 GameProfile textures property |
| CountDownLatch 超时 (503) | 主线程过载 | 增大超时参数或检查 `HandlerTick` 任务积压 |
| GT 机器列表为空 | `GtCompat` 未启用 | 检查是否安装了 GTNH GT 模组 |
