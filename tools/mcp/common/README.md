# Shared helpers for TeXTech stdio MCP servers.

- `stdio_mcp.py` — Content-Length JSON-RPC loop
- `webae_paths.py` — instance root / tokens / cfg discovery
- `../run-server.cmd` — Windows launcher used by `.cursor/mcp.json` (prefers installed CPython over Store stub)
