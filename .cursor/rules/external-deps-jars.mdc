---
description: 外部模组依赖与 Jar 知识库管理
alwaysApply: true
---

# 外部模组依赖与 Jar 知识库

## Jar 存放位置

项目根目录 `libs/` 文件夹用于存放本地 jar 依赖。如果该文件夹不存在，需要先创建。

## 依赖来源分类

### Maven 依赖（自动拉取，无需本地 jar）
这些通过 `dependencies.gradle` 从 GTNH Maven 仓库自动解析：

| 依赖 | 版本 | 作用 |
|------|------|------|
| GT5-Unofficial | 5.09.51.470 | GregTech 5 核心（`api` 作用域） |
| AE2FluidCraft-Rework | 1.3.7-gtnh | AE2 流体存储支持 |
| Thaumcraft | 1.7.10-4.2.3.5 | 神秘时代（`compileOnly`，可选依赖） |
| NewHorizonsCoreMod | 2.7.260 | GTNH 核心模组 |
| ArchitectureCraft | 1.10.2 | 建筑工艺 |
| BlockRenderer6343 | 1.2.12 | 方块渲染工具 |
| Avaritia | 1.61 | 无尽贪婪 |
| Avaritiaddons | 1.7.1-GTNH | 无尽贪婪扩展 |
| Eternal-Singularity | 1.2.1 | 永恒奇点 |
| Universal-Singularities | 8.7.0 | 通用奇点 |
| NotEnoughItems | 2.5.4-GTNH | NEI (仅运行时，不发布) |

### 本地 Jar 依赖（需要放在 `libs/` 下）
这些在 `dependencies.gradle` 中通过 `implementation files('libs/xxx.jar')` 声明：

| Jar 文件名 | 用途 | 状态 |
|-----------|------|------|
| Chisel-2.14.1-GTNH-dev.jar | 凿子模组 | 未添加 |
| Galacticraft-3.3.12-GTNH-dev.jar | 星系模组 | 未添加 |
| IC2NuclearControl-2.7.8-dev.jar | IC2 核电控制 | 未添加 |

已注释的备选依赖（不参与编译）：
- ArchitectureCraft-1.10.2-dev.jar
- TwistSpaceTechnology-0.5.0-dev.jar
- geckolib3-v3.0.0-dev.jar

## 工作流程

### 当需要查看其他 GTNH 模组（官方/社区）的类或方法时：

1. **先在 `libs/` 中查找**该模组的 jar 文件是否存在。
2. **如果 jar 已存在** — 直接读取 jar 中的类信息（通过反编译或 jar 内容列表工具），不要再次询问用户。
3. **如果 jar 不存在** — 询问用户："需要查看 xxx 模组的类，但 `libs/` 中没有对应的 jar 文件。是否需要手动添加 `xxx-dev.jar`，还是跳过直接编写？"
4. **用户添加 jar 后** — 将 jar 信息更新到此规则文件的依赖清单中，后续不再重复询问。

### 常用 GTNH 模组的类查找

需要查看以下模组的类时，优先从 Maven 本地缓存或 `libs/` 寻找：

- **AE2 相关**：`appeng.api.*`（AE2FluidCraft-Rework 包含）
- **GregTech 5 相关**：`gregtech.api.*`（GT5-Unofficial 包含）
- **NEI 相关**：`codechicken.nei.*`（NotEnoughItems 包含）
- **Thaumcraft 相关**：`thaumcraft.api.aspects.*`（Thaumcraft 包含，`compileOnly` 作用域）
- **原版/Forge**：`net.minecraft.*`、`cpw.mods.fml.*`
