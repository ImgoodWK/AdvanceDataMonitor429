---
description: 外部模组依赖与 Jar 知识库管理
globs: **/dependencies.gradle,**/build.gradle,**/settings.gradle,**/libs/**,**/compat/ae/**
alwaysApply: false
---

# 外部模组依赖与 Jar 知识库

## 目标整合包

**GTNH 2.9.0-beta-2**（不再兼容更旧的 GT5 / ModularUI2 / GTNHLib 栈）。

## Jar 存放位置

项目根目录 `libs/` 用于本地 jar。若无则先创建。

## Maven 依赖（`dependencies.gradle`）

| 依赖 | 版本 | 作用 |
|------|------|------|
| GT5-Unofficial | 5.09.54.20 | GregTech 5（`api`） |
| Applied-Energistics-2-Unofficial | rv3-beta-1000-GTNH | AE2（`api`） |
| AE2FluidCraft-Rework | 1.5.95-gtnh | AE2 流体附加 |
| StructureLib | 1.4.42 | 多方块库 |
| NewHorizonsCoreMod | 2.9.5 | GTNH 核心 |
| ArchitectureCraft | 1.12.14 | 建筑工艺 |
| BlockRenderer6343 | 1.4.17 | 方块渲染 |
| Avaritia | 1.97 | 无尽贪婪 |
| Chisel | 2.17.28-GTNH | 凿子 |
| Galacticraft | 3.4.31-GTNH | 星系 |
| NotEnoughItems | 2.8.111-GTNH | NEI（dev） |
| JourneyMap (Modrinth) | 5.2.18 | 旅行地图（dev；pack 5.2.19-fairplay） |
| GTNH-Web-Map | 0.4-beta-1 | Dynmap 瓦片 |
| ModularUI2 | 2.3.79-1.7.10 | UI（dev） |
| BetterQuesting | 3.8.72-GTNH | 任务书（dev） |
| GTNHLib | 0.11.24 | TitleAPI 等 |
| Thaumcraft | 1.7.10-4.2.3.5 | `compileOnly` |
| PinIn (shadow) | 1.6.0 (`libs/`) | 拼音搜索 |

**AE2 可选 API stub**：`tools/ae2-optional-stubs/` → `build/ae2-optional-api-stubs.jar`（`compileOnly`）。只为 javac 解析 AE2 `TileChest` 中的 Mek/RotaryCraft 接口；不进游戏、不进模组 jar。

**Dev 任务数据**：`dev-fixtures/betterquesting/` + `syncDevBetterQuesting`。

## 本地 Jar（`libs/`）

| Jar | 用途 |
|-----|------|
| PinIn-1.6.0.jar | 手册拼音（shadow） |
| IC2NuclearControl-2.7.8-dev.jar | IC2 核电控制（可选） |
| `mcef*.jar`（可选） | montoyo MCEF **1.7.10-0.6**（官网仍提供；GitHub Releases 只有 1.12.2 API）。下载：https://montoyo.net/wd3/?modid=mcef → MC 1.7.10 → 0.6。玩法放入 `mods/`；开发放入 `libs/`（仓库可已有该 jar）。`devOnlyNonPublishable`，不进发布 jar。放入后 `addon.late.gradle` 会加 ShutdownPatcher。运行时还需从 https://montoyo.net/jcef 拉 JCEF 原生库；官方声明 1.10.2 之前可能不兼容新版启动器（GTNH/Prism 上常装了也起不来）。无 MCEF 时监视器主路径为浏览器推帧（`browser-jpeg`），主机 Chrome/Edge 截 embed（`spa-jpeg`）作兜底。 |

## 查阅外部模组类

1. 先查 `libs/` 或 Gradle 缓存
2. 反编译输出写入 `.workspace/`
3. 缺失 jar 时再询问用户

权限策略见 `agent-execution-permissions.mdc`。
