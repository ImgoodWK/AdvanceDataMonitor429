---
description: GTNH 模组硬约束（JVM/Forge/注册/lang/路径）
alwaysApply: true
---

# GTNH 模组开发硬约束

- **项目**：TeXTech · modid `textech`（旧 `advancedatamonitor`）· 根包 `com.imgood.textech`
- **平台**：MC 1.7.10 / Forge 10.13.4.1614 / MCP stable_12 · Gradle + GTNH Convention

## API / 运行时

- **禁止** Java 8+ 标准库类（无 `stream`/`Optional` 等）；Jabel 仅语法糖
- Forge：`cpw.mods.fml.common.*`；**禁止** 1.8+ `net.minecraftforge.*` API
- 渲染：`Tessellator.instance`；OpenGL：`GL11`
- 侧：`@SidedProxy`；客户端代码放 `client/` / `renders/` / `gui/`，服务端勿加载客户端类
- 日志：`AdvanceDataMonitor.LOG`
- Debug：`config/textech/textech.cfg`（及 `ai-client-local.cfg` 密钥），**默认 false**；WebAE 分功能 debug 经 `ConfigWebaeDebugLoader` → `WebAeDebugLog`

## 注册与 lang

- 注册只走 `loader/`（Block/Item/TE/Handler/Render/Gui/Network 等），勿在构造函数分散注册
- 网络包 ID 权威：`network-packets.mdc`（`LoaderNetwork` postInit）
- lang：**同时**改 `assets/textech/lang/en_US.lang` + `zh_CN.lang`；key 前缀 `adm.`

## 运行时数据路径（摘要）

- cfg：`config/textech/textech.cfg`；客户端密钥：`ai-client-local.cfg`
- 数据根：`<实例>/TeXTech/WebAE|Assistant|Grapple/`（见 `TeXTechDataDir`；启动迁移 `TeXTechDataMigration`）
- 详细路径表与 Loader 时机：技术文档 / 原长表已收敛；缺路径时查 `TeXTechDataDir` 或 documentation-map
