---
description: WebAE 前端改动后必须 npm run build，否则游戏内无效果
globs: webae-frontend/**
alwaysApply: false
---

# WebAE 前端改完必须构建

Vite 构建到 `src/main/resources/assets/textech/webae/`，发布时由 Gradle 打入独立 `textech-*-webae.zip`，主 JAR 排除此目录。服务器优先加载实例 `TeXTech/WebAE/ui/`；源码开发运行才回退 classpath。任何环境都不会直接加载 `webae-frontend/src`。

**仅改源码、不 build → 游戏内无变化。**

凡本轮改了 `webae-frontend/`，宣称可验证前必须成功：

```bash
cd webae-frontend && npm run build
```

PowerShell：`Set-Location webae-frontend; npm run build`

检查 `src/main/resources/assets/textech/webae/index.html` 的 `js/index-*.js` 哈希已更新；发布任务还需确认 WebAE ZIP 内有 `TeXTech/WebAE/ui/index.html` 且主 JAR 无 `assets/textech/webae/`。总结写明已 build；提醒替换实例 UI 目录、重启游戏后浏览器 Ctrl+F5。

用户说「先别 build」或本轮未改 `webae-frontend/` 时可跳过。失败则先 `npm install` 再 build。勿手改产物目录。
