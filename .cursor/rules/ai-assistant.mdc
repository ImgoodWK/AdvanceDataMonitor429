---
description: AI 助手硬约束与必改文件；完整架构见 docs ai-assistant 开发指南
globs: **/assistant/**,**/assistant/ai/**,**/voice/**,**/gui/guiscreen/GuiAIChat.java,**/gui/guiscreen/GuiAISettings.java,**/command/CommandAssistant.java,**/command/CommandAIConfig.java,**/network/packet/PacketAssistantAction.java,**/network/packet/PacketAssistantResponse.java
alwaysApply: false
---

# AI 助手硬约束

**完整架构 / 数据流 / 意图枚举 / API**：只读 `docs/zh/ai-assistant/开发指南.md` 或 `docs/en/ai-assistant/development-guide.md`（单语）。本规则不重复长文。文档同步见 `ai-assistant-docs-sync.mdc`。

## 包边界

| 层 | 包 | 职责 |
|----|-----|------|
| 通信 | `assistant/ai/` | HTTP、流式、Provider |
| 逻辑 | `assistant/` | 意图、会话、调度 |
| 执行 | `AssistantServerServices` 等 | AE2 / 查询 / 下单 |
| 语音 | `voice/` | STT（Vosk / HTTP） |
| GUI | `GuiAIChat` / `GuiAISettings` | 聊天与设置 |
| 包 | `PacketAssistantAction` / `PacketAssistantResponse` | C↔S |

入口：`AssistantController.handleUserText`；意图：`AssistantAiIntentService`（AI）或 `AssistantIntentService`（本地 fallback）→ `AssistantIntentPlan`。

## 硬约定

- 用户可见助手文本：用 `AssistantServerServices.text(locale, zh, en)`（或等价双语路径）；lang 成对改 `en_US` + `zh_CN`
- 合成提交：异步 `Future` + 回主线程发消息；勿长时间堵主线程
- 网络/TE/`ItemStack`：查前判 null；`stack.getItem() != null`
- 单例：`AssistantSession.client()`、`AssistantCraftJobManager.instance()`、`OrderMemoryStore` / `PlanStore` / lexicon / feature config 的 `instance()`/`get()`
- 持久化路径经 `TeXTechDataDir` / Assistant 数据文件，勿写死旧 `config/textech/*.json` 布局
- 新增意图：改 `AssistantIntentType` → AI prompt / 本地解析 → `AssistantServerServices` switch → 必要时 lexicon / features JSON → GUI 能力说明

## 必改文件速查（摘要）

| 目的 | 必改 |
|------|------|
| 新意图 | `AssistantIntentType` + 解析服务 + `AssistantServerServices` |
| AI prompt | `AssistantAiIntentService` |
| 本地关键词 | `AssistantIntentService` + lexicon JSON |
| AE2 / 查询 / 下单 | `AssistantServerServices`（及 craft job / session） |
| 配置项 | `Config` + loader + 必要时 `GuiAISettings` |
| 语音 | `VoiceCaptureService` 等 |
| 功能菜单 | `assistant-features.json` + `AssistantFeatureConfig` |

更全表见开发指南 Part B。改功能后按 `ai-assistant-docs-sync.mdc` 更新 docs / features / lang / 本规则（若硬约束变化）。
