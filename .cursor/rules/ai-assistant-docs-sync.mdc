---
description: AI 助手功能变更时自动同步 Cursor 规则和 docs/ai-assistant/ 文档
globs: **/assistant/**,**/assistant/ai/**,**/voice/**,**/gui/guiscreen/GuiAIChat.java,**/gui/guiscreen/GuiAISettings.java,**/command/CommandAssistant.java,**/command/CommandAIConfig.java,**/network/packet/PacketAssistantAction.java,**/network/packet/PacketAssistantResponse.java
alwaysApply: false
---
# AI 助手功能文档同步规则

## 要求

每次新增或修改 AI 助手功能（`assistant/`、`ai/`、`voice/` 包或相关文件）时，**必须自动同步更新以下文件**，不得遗漏：

## 必须同步的文件清单

### 1. Cursor 规则文件
- `.cursor/rules/ai-assistant.mdc` — 更新意图枚举、会话类型、文件速查表、查询分发列表
- `.cursor/rules/project-structure.mdc`（核心索引）— 如果 `assistant/`、`ai/`、`voice/` 包中有文件新增/删除/重命名，更新顶层包概览与文件计数
- `.cursor/rules/project-structure-details.mdc`（详细清单）— 同步更新 `assistant/` 段的逐文件清单、文件计数（如 `25 文件` → `26 文件`）和关键文件列表

### 2. 项目 Markdown 文档（`docs/` 目录）

| 文件 | 更新内容 |
|------|---------|
| `docs/zh/ai-assistant/开发指南.md` · `docs/en/ai-assistant/development-guide.md` | Part A：功能列表、数据流、已知限制、文件职责表 · Part B：必改文件速查 · Part C：本地 STT |

### 3. 功能配置文件
| 文件 | 更新内容 |
|------|---------|
| `assistant-features.json` | 新功能的 key/displayName/description/keywords/aiInstruction |
| `assistant-lexicon.json` | 如需要新关键词表 |

### 4. 多语言文件
- `en_US.lang` + `zh_CN.lang` — 所有新增/修改文本的双语 key

### 5. GUI 功能说明
- `GuiAIChat.assistantCapabilityInstruction()` — 功能列表（中英文）

## 执行时机

此规则为 glob 按需加载（编辑 `assistant/`、`voice/` 或相关 GUI/命令/网络包文件时自动触发）。每次涉及 AI 助手功能的修改后，Agent 应主动检查并更新以上文件，而不是等用户提醒。

## 目的

避免每次进行 AI 助手相关功能开发时，Agent 需要大量阅读代码才能找到需要修改的类。通过这些文档和规则保持最新，新 Agent 可以快速定位相关文件。
