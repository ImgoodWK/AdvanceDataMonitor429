---
description: Git 推送与 Release 发布工作流经验
alwaysApply: false
---

# Git 推送与 GitHub Release 发布工作流

## 适用场景

当用户要求"推送代码到 GitHub"或"发布 Release"时，按以下流程执行。从 GitHub **拉取/以远程为准同步**见 **`git-pull-sync.mdc`**。

## 核心经验

### 1. Sandbox 权限策略

Git / `gh` 的 `required_permissions` 默认策略见 **`agent-execution-permissions.mdc`**（`alwaysApply`）：所有 Git 命令必须带 `["all"]`，否则 Windows 下常静默失败（`no exit status`）。

本文件以下章节只描述**推送与 Release 工作流**；权限选用表与 clone/下载策略以该规则为准。

### 2. 推送工作流

当用户说"以我上传的为准，完全和我上传的一样"时，意味着要**强制覆盖远程**：

```
1. git status          → 确认本地状态
2. git add -A          → 暂存所有更改（包括新增、修改、删除）
3. git commit -m "..." → 提交（提交信息中英文均可，用中文描述主要变更）
4. git push --force origin master → 强制推送，远程与本地完全一致
```

关键点：
- 使用 `--force` 而非普通 push，确保远程完全被本地覆盖
- 提交信息要概括主要变更（如"全面更新: AI助手系统集成与项目大重构"）
- 不要遗漏 `git add -A`，否则未跟踪文件（untracked）不会被提交
- **必须包含 `.cursor/rules/`**：Cursor Agent 规则（`*.mdc`）应随代码一并推送，便于协作者与 Agent 共享项目约定；新增规则文件后 `git add -A` 会自动纳入
- **排除 `.workspace/` 与 `.cursor/plans/`**：`.workspace/` 已在 `.gitignore`（Agent 临时/反编译产物）；`.cursor/plans/` 为本地 Agent 计划缓存，不必推送

### 3. CRLF 换行符警告

Windows 环境下 `git diff` 经常会输出 LF/CRLF 换行符警告，**这是正常的，可以忽略**。不需要修改 `core.autocrlf` 配置。

### 4. 发布前更新 README

每次推送前应检查并更新 `README.md` 与 `docs/README.md`，确保首页说明反映最新状态：
- 功能列表是否完整
- 文档索引链接是否有效（指向 `docs/` 子目录）
- 依赖清单是否最新

## GitHub Release 发布

### 使用 `gh` CLI 创建 Release

```bash
gh release create v1.0.0 --title "v1.0.0 - 初始正式版" --notes "<release notes>"
```

Release notes 建议包含：
- 🆕 新增功能列表
- 🔧 技术栈说明
- 🤖 AI 开发工具链说明（Cursor + Claude Code + GPT-5.5 + DeepSeek V4 Pro）
- 📦 构建方式
- ⚠️ 已知问题

### Release 命名规范

- 版本号格式：`v<major>.<minor>.<patch>`（如 `v1.0.0`）
- 标题格式：`v<版本号> - <简短描述>`（如 `v1.0.0 - 初始正式版`）

### 注意事项

- `gh release create` 基于当前 HEAD 创建 tag，确保先 commit 再 release
- Release 创建后 tag 会自动推送，不需要单独 `git push --tags`
- 如果本地已有同名 tag，需要先 `git tag -d v1.0.0` 删除再创建

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| `Shell` 返回 "no exit status" | Sandbox 限制了 Git 操作 | 见 `agent-execution-permissions.mdc`，Git 命令加 `["all"]` |
| `echo` 也返回 "no exit status" | Shell 环境本身异常 | 检查终端是否可用，必要时重启 |
| 推送后远程与本地不一致 | 用了普通 push 而非 `--force` | 使用 `git push --force origin master` |
| `gh` 命令未找到 | GitHub CLI 未安装或未登录 | 先执行 `gh auth status` 检查，必要时 `gh auth login` |
| CRLF warning 刷屏 | Windows 换行符差异 | 忽略，不影响功能 |
| 推送显示 "Everything up-to-date" | 本地与远程已同步，无新提交 | 正常情况，无需操作 |
| 推送前不确定是否有待推送内容 | 未检查本地与远程差异 | `git log origin/master..HEAD --oneline` 查看 |
