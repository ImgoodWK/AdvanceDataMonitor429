---
description: 项目结构速览，方便快速定位需要修改的类
alwaysApply: true
---

# 项目结构速览

## 包组织约定

项目顶层只允许两类包：

1. **标准 Forge 分层**（按 Minecraft 类型）：`blocks/`、`items/`、`entity/`、`tileentity/`、`gui/`、`renders/`、`handler/`、`network/`、`loader/`、`client/`、`config/`、`utils/`
2. **系统级功能包**（跨方块/物品/GUI/网络，非单物品）：`assistant/`（含子包 `assistant/ai/`）、`voice/`、`compat/`（AE 版本兼容）、`webae/`（嵌入式 Web 控制台）

**物品类及其紧密配套逻辑**归位标准分层，**禁止**在根包下为单物品新建与 `items/` 平级的功能包：

- Item 与 AE2 深耦合 → `items/cell/` 子包
- 服务端行为/状态机 → `handler/`（类名保留 `Grapple*`、`StarryCosmos*` 前缀便于检索）
- 手册 JSON 数据结构 → `gui/manual/`（与 `GuiManual` 同层）
- 扫描器 DTO → `items/` 根，与 `ItemAdvanceLinkScanner` 并列；服务 → `handler/LinkScannerService`

**客户端代码约定**：`@SideOnly(CLIENT)` 的输入/缓存/选点逻辑放 `client/`；所有 Render/TESR/HUD（含手册页面渲染）放 `renders/`；GUI 统一经 `gui/handler/GuiHandler` 的 `openGui` 打开。

## 包概览

逐文件清单、lang 键与游戏内名称详见 `project-structure-details.mdc`（编辑模组 Java 源文件时自动加载）。

| 包 | 文件数 | 作用 |
|----|--------|------|
| 根包 | 5 | `@Mod` 主类、`Config` 门面、`ClientProxy` / `CommonProxy` |
| `config/` | 15 | 配置分节加载（debug / compat / ai / voice / assistant / plannerHud / dataLoom / superOrange / matterBallDecompressor / grapple / webConsole + `ConfigWebaeDebugLoader` WebAE 分功能 debug 开关 + `ConfigWebAlertsLoader` 加载 `web-alerts.json` + `ConfigWebOrderTemplatesLoader` 加载 `web-order-templates.json`） |
| `compat/` | 21 | GTNH / AE2 版本兼容（`ae/` 门面 + `legacy/` + `native_/`）+ `gt/` GT 兼容 |
| `assistant/` | 45 | AI 助手核心（意图解析 / AE2 查询 / 合成下单 / 内置联网搜索 / 计划 / 菜单状态缓存），详见 `ai-assistant.mdc` |
| `items/` | 55 | 物品（含 `items/cell/` 33 文件；`AbstractPocketUpgradeCard` 口袋升级卡基类） |
| `blocks/` | 7 | 自定义方块（含可选调试方块 `BlockUiFrameworkDebug`） |
| `tileentity/` | 9 | TileEntity + 接口 + 辅助（含 `IOwnableTile`、`MatterBallDecompressorUpgrades`） |
| `gui/` | 61 | GUI 组件与界面（根 2 + `framework/` 15 + `custom/` 7 + `guiscreen/` 25 + `manual/` 6 + `container/` 5 + `handler/` 1），详见 `gui-guidelines.mdc` |
| `handler/` | 36 | 事件处理器与服务端逻辑（挂索 / 星空剑 / 口袋 / 扫描器 / 编织元件 / AE 连接器性能调度 / WebAE 服务生命周期 / WebAE 玩家追踪 / WebAE 聊天收集） |
| `entity/` | 8 | 实体类 only（不含 Render） |
| `renders/` | 35 | 全部客户端渲染（TESR / 实体 Render / 物品 Render / HUD / 语音助手 HUD / 手册页面 / 解压器 GUI） |
| `network/` | 30 | 网络通信（24 packet + 6 handler，含 `PacketHandlers`），详见 `network-packets.mdc` |
| `voice/` | 9 | 语音识别（Vosk / HTTP STT / 音频处理） |
| `loader/` | 10 | 注册加载器（Block / Item / TE / Entity / Handler / Gui / Render / Network / Recipe）+ `ModMigrationHandler` |
| `client/` | 24 | 客户端事件与缓存（挂索 / 口袋 / AI 偏好 / 按键 / 语音按键 / 星空剑预热 / 数据映录器滚动 / WebAE token 剪贴板与可点击聊天 / **Phase4** `client/worldmap/` HD 瓦片 Worker）；Phase 2 移除 WebAE 图标/配方上传快捷键（`HandlerWebIconUpload` 已删除，上传仅由 `/admweb` 指令触发，`KeyBindings.triggerIconUpload`/`uploadNeiRecipes` 为命令入口） |
| `command/` | 3 | 命令（`/admassistant`、`/admai`、`/admweb`） |
| `utils/` | 14 | 工具类（NBT / JSON / `BlockPos` / 物质团簇 / 解压器容量与速度 / AE 加速倍率 / AE 安全检查 / `ModLogFiles` 日志路径等） |
| `webae/` | 221 | WebAE 嵌入式 HTTP 控制台（NanoHTTPD 服务器、REST API、认证[`WebAuthSession` 主人/访客 Token + **Phase5** `WebLoginCodeStore` 6 位登录码/`POST /api/auth/exchange`/`/admweb login`；`WebAuthMiddleware` 支持 header + query token]、**Phase5** `GET /api/oc/summary` OC 只读轮询 + `docs/*/webae/oc-integration.md`、存储/电力/GT 机器监控、配方缓存、样板编码/注入/总览编辑/批量删除/browse 双源分页 API[`GET /api/patterns/browse` HTTP 只读缓存 + `POST .../browse/refresh` OP 强制重建；`SnapshotScheduler` 后台预采集；owner 离线 `getOwnerPlayerOrFake`] + Grid 详情[`GET /api/patterns/grid/<gridKey>`]、browse 失效由 `PatternBrowseInvalidationGridCache` 监听 `MENetworkCraftingPatternChange` 与 Web 变更主动失效、**网络拓扑**[`GET /api/network/topology` 读手动快照 + `POST /api/network/topology/snapshot` 采集；`NetworkStatusEnumerator` 网络工具同源 + `TopologyFacilityGrouper` subtype 聚合 + `ChannelBranchAllocator` + `LogicalTopologyBuilder` 树状/双环 + `TopologySnapshotStore` + `CraftingCpuTopologyCollector` ICraftingCPU 集群聚合 + `SimulatedLayoutBuilder` Manhattan 线缆；前端 `TopologyCytoscapeGraph`（cytoscape.js tree/star/spatial 布局 + 贝塞尔边）/`TopologyGraphSvg`（legacy 回退）/`TopologySimulatedView`（block 材质）/`TopologyDeviceList`（细粒度 subtype 分组 + 图↔表联动）/`DriveSimulatedGui` + `useTopologyDisplay`；**Phase10** `GET /api/network/p2p` P2P 频率地图 + 拓扑页 P2P 模式]、**Phase A/B/3 世界地图**[`GET /api/worldmap/meta`（views[] flat+oblique + obliqueDirections[]）+ `GET /api/worldmap/markers` + `POST /api/worldmap/invalidate` + `GET /api/worldmap/tiles/[view/]<dim>/<cx>/<cz>.png?network=`（flat + oblique_se/sw/ne/nw mineshot 风格斜视角；AE 设备 chunk + padding 精确范围；scope 外透明 PNG）；`webae/worldmap/` 含 `WorldMapView`/`WorldMapObliqueDirection`/`WorldMapTileInvalidator`/`WorldMapChunkSetBuilder`/`WorldMapFlatRenderer`/`WorldMapIsoRenderer`/`WorldMapBlockTextureSampler`（面采样）/`WorldMapBlockColorResolver`/`WorldMapTileQueue`/`WorldMapTileCache`（`config/textech/web-map-tiles/{view}/`；flat 旧路径双读回退）；`HandlerTick` 分 tick 预算渲染；前端 `TopologyWorldMapView` 俯视+斜视角双 Segmented + 设置抽屉斜视角方向 + 刷新/快照失效重渲染 + `WorldMapTerrainLayer` view 参数与 allowedChunks 裁剪 + `WorldMapMarkerLayer` supercluster 聚类]、**Phase6** `GET /api/craft/tree` 材料树 + AeOrdering Tab、**Phase9** `GET /api/events/stream` SSE + `useEventStream`、**Phase11** `GET /api/monitor/preview` 监视器折线预览 Drawer、**Phase12** PWA manifest + 移动端响应式 CSS、**Phase3 集成**[`GET /api/scanner/blocks` Link Scanner 镜像、`GET /api/monitor/bindings` 监视器绑定、`GET /api/planner/plans` 计划器只读、`POST /api/assistant/query` Web 助手、`GET /api/network/cells` 无限元件摘要、`GET /api/pocket/overview` OP 口袋概览]、**Phase3 告警**[`config/textech/web-alerts.json` + `WebAlertEngine` tick 检测 + `GET /api/alerts` + `GET /api/alerts/history` 历史 + **Phase4d** `PUT /api/alerts/rules` OP Web 编辑 + `AlertsRulesEditor.tsx`/`AlertsHistory.tsx`；库存阈值/CPU 卡住/GT 异常/下单完成/频道超载；每条 occurrence 仅弹窗一次]、**Phase4a 聚合搜索**[`GET /api/search` 合并 storage/recipe/gt/pattern；`webae/search/` + 500ms 限流；Command Palette Ctrl+K 优先调用 + snapshot fallback]、AE 合成下单[含按样板 patternId 下单]、在线玩家数采样历史、DTO、网络包、**物品图标 NEI 网格 FBO + 懒加载**[`IconExportResolver` 多级回退 grid→GL→vanilla→atlas→block→placeholder；`IconTileResolver` 解析 `mod:tile.BlockClass` 伪 id；`IconGridExporter`/`GuiIconExportScreen` 默认 mode `nei`；`/admweb icons upload snapshot`；404→`IconMissingQueue`→`PacketWebIconRequest`→`IconLazyRenderQueue`（客户端 2/tick 限流）；`IconRenderGuard` 离屏渲染后复位 Tessellator/GL；懒加载单图标完成不写聊天栏；bulk 开始时清空懒加载队列；`tools/icon-import/convert_icon_exporter.py`]、玩家信息收集 `player/`、聊天消息收集 `chat/`、皮肤 URL 解析、`debug/WebAeDebugLog` 分功能日志[阶段 3.3，gate by `[debug] webaeXxx` 开关]）。前端源码在项目根 `webae-frontend/`（React 18 + TypeScript + Ant Design 5 + Vite 5 + cytoscape.js，`npm run build` 输出到 `assets/textech/webae/`；Phase3 页面 LinkScanner/MonitorBindings/Planner/Assistant/AlertsHistory + Settings 告警规则 + `useWebAlerts`/`alertNotification` Toast/Notification + **Phase9** `useEventStream` SSE 告警） |
| `mixin/` | 7 | Mixin 注入（仅客户端：GUI / NEI 兼容 / tooltip Invoker / 口袋角标） |

## 资源文件 (src/main/resources/)

```
assets/textech/
 config/
   assistant-lexicon.json       # AI 助手词库配置
   assistant-features.json      # AI 助手功能配置
 lang/
  en_US.lang                   # 英文翻译（825+ 行，主 lang 文件）
  zh_CN.lang                   # 中文翻译（主 lang 文件）
 manual/
 index.json                   # 手册目录（章节列表）
 chapters/                    # 手册各章节内容JSON (11 章)
 model/                       # .obj 模型文件（AdvanceDataMonitor.obj, AdvanceNetworkLink.obj）
 textures/
 blocks/                      # 方块贴图
 items/                       # 物品贴图
 gui/                         # GUI 纹理
 voice/vosk/zh-small/         # Vosk 离线语音模型
 sound/                       # 音效文件（星空剑等）
 sounds.json                  # 音效注册表
```

## 工作区临时产物 (`.workspace/`)

不属于模组源码，已 gitignore，详见 `workspace-artifacts.mdc`。Agent 与开发者产生的临时文件只放此处，不得放在项目根目录或 `src/` 下。

## WebAE 前端源码 (`webae-frontend/`)

项目根目录下的 `webae-frontend/` 是 WebAE 控制台的 React + TypeScript + Ant Design 前端源码（永久项目部分，需提交 Git）。`npm run build` 构建产物输出到 `src/main/resources/assets/textech/webae/`。配方页组件位于 `webae-frontend/src/components/recipes/`（Phase 5：类别多选/搜索/收起 `HandlerCategoryFilter`、正方形缩略卡片 `RecipeThumbnailCard`、合并模式 `RecipeMergedCard`、详情弹窗 `RecipeDetailModal`、缩略/详细双布局 + CSS Grid 列独立、合成格、物品 U/R 侧栏）；合并分组逻辑在 `utils/recipe.ts`（`groupByPrimaryOutput`）。样板编辑器 `pages/PatternEditor.tsx`（Phase 6：复用 `groupByPrimaryOutput`/`RecipeDetailModal`、输入输出图标数量、输出倍率、接口 `existingPatterns`/`machineRecipeType` 展示）。AE 下单页 `pages/AeOrdering.tsx`（Phase 7：`components/patterns/PatternOrderCard`/`PatternDetailModal`/`VirtualPatternGrid`/`VirtualProductGrid` 虚拟滚动 + Grid 源 lazy fetch；CPU Select 下单前选择；混合进度轮询 `/api/order/list`；历史订单从 `history` 字段获取 + CPU 列 Tooltip；Phase 4e browse 缓存标签 + OP 强制刷新）。源质页 `pages/Essentia.tsx`（Phase 5：aspect 列表/搜索/排序 + `essentiaCountHistory` 趋势）。Phase 8（最终阶段）：`theme/colors.ts` 新增 hologram/plasma/neon-pulse/quantum/crystal 5 套科技风主题 + `global.css` 主题特效与 `.chart-flow-line` 等图表持续动效（`effectsLevel=full`）；`IconExportResolver.java` 统一图标导出回退链 + `IconGlFallback.renderVanillaNeiSlotIcon`；`IconAtlasSampler.java` 图集 CPU 采样 + `IconGlFallback.java` 自定义渲染器小 FBO 回退 + `IconRenderer.java` 异步分 tick 上传，`IconItemEnumerator` NEI 不可用时 `getSubItems` meta 枚举。仪表盘属性继承体系组件位于 `webae-frontend/src/components/dashboard/`（`AlignmentGrid` 9 宫格对齐选择器、`ColorField` 颜色输入、`DashboardSettingsDrawer` 全局设置抽屉、`EditWidgetModal` 组件编辑弹窗、`OverviewWidgetGrid` 存储/CPU 可配置总览网格、`PowerWidgetGrid`/`PowerWidgetContent` 电力页可配置 GridStack 与趋势图渲染、`WidgetContent` 总览组件渲染），属性解析器在 `webae-frontend/src/utils/dashboardResolve.ts`（`resolveProp`/`resolveAllColors`/`alignmentToCss`），总览数据源在 `utils/overviewDataSources.ts`，电力数据源在 `utils/powerDataSources.ts`，CPU 表格列在 `utils/cpuColumns.tsx`。前端页面含 `pages/Cpu.tsx`（Phase 3 独立 CPU 菜单）、`pages/Power.tsx`（Phase 4 可配置电力布局 + 防闪烁趋势图）、`pages/NetworkTopology.tsx`（Phase A 第 4 视图 `worldMap`：`TopologyWorldMapView` + `WorldMapMarkerLayer` + supercluster 聚类 + 网格背景）。Phase 4b：`hooks/usePageVisibility.ts` + `hooks/useVisibilityAwarePolling.ts` 标签页可见性感知轮询（`AppContext` `pauseRefreshWhenHidden`/`refreshPaused`；Settings `webae_pause_refresh_when_hidden`；TopBar「已暂停」）。前端开发规范见 `.cursor/rules/webae-frontend.mdc`（antd-only + WCAG 2.1 AA + GridStack 布局例外）。详见 `docs/zh/webae/开发者手册.md` §6 与 `webae-frontend/README.md`。旧 Alpine.js 前端备份在 `.workspace/webae-legacy-backup/`。

## 修改指南：改动某种功能时应该看哪些文件

| 需求 | 查看文件 |
|------|---------|
| 新增 Block | `loader/LoaderBlock.java` → `blocks/` → `tileentity/` → `assets/textech/lang/en_US.lang` + `zh_CN.lang` |
| 新增 Item | `loader/LoaderItem.java` → `items/` → `assets/textech/lang/en_US.lang` + `zh_CN.lang` |
| 新增 GUI | `loader/LoaderGui.java` → `gui/guiscreen/` → 选基类：`AdmItemConfigScreen` / `AbstractMonitorSubGui` / `ADM_UiContainer` → `AdmGuiTextures` → lang；详见 `docs/zh/developer/new-feature-checklist.md` |
| 新增网络包 | `network/packet/` → `network/handler/` → `loader/LoaderNetwork.java` → 完整数据同步链路见 `network-packets.mdc` |
| 新增渲染 | `renders/`（实体 Render 也放此包）→ `loader/LoaderRender.java` |
| 修改/新增 AI 助手功能 | 详见 `ai-assistant.mdc` → `docs/zh/ai-assistant/开发指南.md` / `docs/en/ai-assistant/development-guide.md` → `assistant/AssistantController.java` |
| 修改挂索 | `docs/zh/subsystems/挂索节点系统设计.md` / `docs/en/subsystems/grapple-system-design.md` → `handler/Grapple*.java` + `client/HandlerGrappleClient.java` |
| 修改次元口袋 | `handler/PocketState.java`（状态 + 堆叠升级字段）/ `handler/PocketStore.java`（压缩 NBT 存储）/ `handler/PocketInventory.java`（IInventory 实现，动态堆叠上限）/ `handler/PocketUpgradeRules.java`（升级校验，含堆叠/无尽堆叠规则）+ `client/PocketOverlayHandler.java`（覆层渲染/鼠标路由，通过自定义包通信）/ `client/GuiPocketOverlay.java`（悬浮窗渲染 + hover tooltip）/ `client/PocketClientCache.java`（客户端缓存，含堆叠字段）+ `items/ItemDimensionalPocket.java`（口袋物品）/ `items/ItemSpaceUpgradeCard.java` / `items/ItemPageUpgradeCard.java` / `items/ItemStackUpgradeCard.java` / `items/ItemInfiniteStackUpgradeCard.java` + `gui/container/ContainerDimensionalPocket.java`（升级配置容器，4 槽位：空间/翻页/堆叠/无尽堆叠）/ `gui/container/ContainerPocketStorage.java`（存储容器）+ `gui/guiscreen/GuiDimensionalPocketConfig.java`（升级配置 GUI，4 槽位）/ `gui/guiscreen/GuiPocketStorage.java`（存储 GUI，右键主入口）+ `network/packet/PacketPocketAction.java`（C→S 动作，含堆叠升级/存取操作类型）/ `network/packet/PacketPocketSync.java`（S→C 同步，含堆叠字段）。**GUI 打开约定**：口袋存储/配置 GUI 必须由**服务端** `player.openGui` 触发（存储 GUI 在 `ItemDimensionalPocket.onItemRightClick` 用 `if(!world.isRemote)`；配置 GUI 经 `PacketPocketAction.OPEN_CONFIG_GUI` 由服务端 `handleServer` 调用 `openGui`），否则服务端 `openContainer` 停留为 `ContainerPlayer`(windowId=0)，物品/升级卡点击会落到错误容器而弹回背包。 |
| 修改计划器 | `docs/zh/developer/技术文档.md` §5.11 / `docs/en/developer/technical-documentation.md` §5.11 → `items/ItemAdvancePlanner.java` |
| 更新玩家/开发者文档 | `docs/zh/player/用户手册.md` / `docs/en/player/player-guide.md` · `docs/zh/developer/技术文档.md` / `docs/en/developer/technical-documentation.md` · `docs/README.md` |
| 修改语音识别 | `voice/VoiceCaptureService.java` → `voice/VoskSpeechToTextClient.java` |
| 新增命令 | `command/` → `TeXTech.java` (serverStarting) |
| 修改配置项 | `Config.java` + `config/Config*Loader.java` + `ConfigDescriptions.java` → `gui/guiscreen/GuiAISettings.java` |
| 新增复杂功能子系统 | 评估是否需系统级包（`assistant/`、`voice/`、`webae/`）→ 物品逻辑归 `items/`、`handler/`、`gui/` → `loader/` → `config/`（如需）→ lang → 本规则 |
| 修改/新增手册 GUI（搜索、滚动、高亮） | `gui/guiscreen/GuiManual.java` → `gui/manual/ManualSearch*.java` / `ManualTextHighlighter.java` → `renders/ManualPageRenderer.java` → `getting_started` lang 文案 → `docs/` §3.12 / §5.10 |
| 新增手册内容 | `assets/textech/manual/` JSON (12 章: `getting_started` / `monitor_blocks` / `monitor_tools` / `data_monitor` / `dimensional_pocket` / `data_loom_cells` / `grapple_system` / `combat_special` / `ai_assistant` / `planner` / `config_reference` / `web_console`) → `gui/guiscreen/GuiManual.java` → `renders/ManualPageRenderer.java` → `assets/textech/lang/en_US.lang` + `zh_CN.lang` → 本规则 |
| 新增 AE2 存储元件 | `items/cell/` → `loader/LoaderItem.java` → `TeXTech.java` (postInit) → `assets/textech/lang/en_US.lang` + `zh_CN.lang` → 手册 JSON |
| 修改/新增 Web 控制台功能 | `webae-frontend/`（React+antd 前端源码，`npm run build` 输出到 `assets/textech/webae/`）→ `webae/WebConsoleServer.java` → `webae/api/WebApiRouter.java` → `webae/auth/`（`WebAuthMiddleware` 支持 header + query token）→ `config/ConfigWebaeLoader.java` → `command/CommandWebConsole.java` → `handler/WebAeServerHandler.java` → `assets/textech/webae/`（构建产物）→ lang → 手册 JSON → `docs/zh/webae/` + `docs/en/webae/` |

## 本规则同步维护要求

本文件是 Agent 快速定位代码的核心索引。**每次在项目中新增、删除或重命名文件时，必须同步更新本规则与 `project-structure-details.mdc`**，不得遗漏：

- 本规则（核心索引）：更新「包概览」表的文件计数与一句话作用
- `project-structure-details.mdc`（详细清单）：更新对应包的逐文件清单、lang 键、关键文件列表

执行时机：与 `ai-assistant-docs-sync.mdc`、`docs-sync.mdc` 配合，当该规则触发文档同步时应同时检查本规则与详细清单是否需要更新。其他非 AI 助手功能的文件变更后，Agent 应主动检查并更新本规则。
