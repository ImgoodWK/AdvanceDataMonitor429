---
description: 项目结构速览（包约定 + 短索引）；详细清单按需 Read project-structure-details
alwaysApply: true
---

# 项目结构速览

## 包组织约定

顶层只允许：

1. **标准 Forge 分层**：`blocks/`、`items/`、`entity/`、`tileentity/`、`gui/`、`renders/`、`handler/`、`network/`、`loader/`、`client/`、`config/`、`utils/`
2. **系统级功能包**：`assistant/`（含 `assistant/ai/`）、`voice/`、`compat/`、`webae/`

禁止为单物品在根包新建与 `items/` 平级的功能包。Item+AE2 → `items/cell/`；服务端状态机 → `handler/`；手册 JSON → `gui/manual/`。  
客户端：`client/` 放输入/缓存；`renders/` 放全部 Render/TESR/HUD；GUI 经 `gui/handler/GuiHandler` 打开。临时产物只进 `.workspace/`（见 `workspace-artifacts.mdc`）。

## 包概览

逐文件清单 / lang 键：按需 Read `project-structure-details.mdc`（**不自动注入**）。改某包时先 Read 该包章节。  
「改什么看哪些文件」权威表：`docs/zh/developer/documentation-map.md`（或 en）。

| 包 | 约数 | 职责 |
|----|------|------|
| 根包 | 7 | `@Mod`、`Config`、数据目录迁移、Proxy |
| `config/` | 15 | 分节 cfg / WebAE JSON 加载 |
| `compat/` | 25 | AE2 / GT / BQ 兼容 |
| `assistant/` | 46 | AI 助手（细节见 `ai-assistant.mdc` + docs） |
| `items/` | 55 | 物品（含 `cell/`） |
| `blocks/` | 5 | 方块 |
| `tileentity/` | 7 | TE |
| `gui/` | 64 | GUI（见 `gui-guidelines.mdc`） |
| `handler/` | 39 | 服务端逻辑 / WebAE 生命周期等 |
| `entity/` | 8 | Entity only |
| `renders/` | 33 | 全部客户端渲染 |
| `network/` | 28 | 网络包（ID 表见 `network-packets.mdc`） |
| `voice/` | 9 | 语音 |
| `loader/` | 11 | 统一注册（含 `LoaderCommand`） |
| `client/` | 30 | 客户端事件/缓存/世界地图采集等 |
| `command/` | 6 | `/textech <域>` 分发；旧 `/adm*` 兼容别名 |
| `utils/` | 14 | 工具 |
| `webae/` | 294+ | Web 控制台（前端：`webae-frontend/` → build 到 `assets/textech/webae/`） |
| `mixin/` | 7 | 客户端 Mixin |

资源：`assets/textech/`（lang 双语、`manual/`、textures、voice 等）。

WebAE 仪表盘组件设置改动需联查 `EditWidgetModal.tsx`、`WidgetPinEditor.tsx`、`utils/dashboardColumns.ts` 与所有渲染入口（主 `Dashboard.tsx`、共享 `WidgetContent.tsx`、专用表组件）。数据表 `columns` 约定：`undefined`=默认列，空数组=明确隐藏全部列；禁止用 `columns?.length` 回退默认值。新增列或钉选行为须同步中英文 i18n、用户/开发文档及回归测试。

## 同步维护

新增/删除/重命名源文件时：更新本表计数与一句话 + `project-structure-details.mdc` 对应章节。与 `docs-sync.mdc` 配合。
