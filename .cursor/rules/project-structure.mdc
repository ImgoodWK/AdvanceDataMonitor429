---
description: 项目结构速览，方便快速定位需要修改的类
alwaysApply: true
---

# 项目结构速览

## 包组织约定

项目顶层只允许两类包：

1. **标准 Forge 分层**（按 Minecraft 类型）：`blocks/`、`items/`、`entity/`、`tileentity/`、`gui/`、`renders/`、`handler/`、`network/`、`loader/`、`client/`、`config/`、`utils/`
2. **系统级功能包**（跨方块/物品/GUI/网络，非单物品）：`assistant/`（含子包 `assistant/ai/`）、`voice/`、`compat/`（AE 版本兼容）

**物品类及其紧密配套逻辑**归位标准分层，**禁止**在根包下为单物品新建与 `items/` 平级的功能包：

- Item 与 AE2 深耦合 → `items/cell/` 子包
- 服务端行为/状态机 → `handler/`（类名保留 `Grapple*`、`StarryCosmos*` 前缀便于检索）
- 手册 JSON 数据结构 → `gui/manual/`（与 `GuiManual` 同层）
- 扫描器 DTO → `items/` 根，与 `ItemAdvanceLinkScanner` 并列；服务 → `handler/LinkScannerService`

**客户端代码约定**：`@SideOnly(CLIENT)` 的输入/缓存/选点逻辑放 `client/`；所有 Render/TESR/HUD（含手册页面渲染）放 `renders/`；GUI 统一经 `gui/handler/GuiHandler` 的 `openGui` 打开。

## 包概览

逐文件清单、lang 键与游戏内名称详见 `project-structure-details.mdc`（编辑模组 Java 源文件时自动加载）。

| 包 | 文件数 | 作用 |
|----|--------|------|
| 根包 | 5 | `@Mod` 主类、`Config` 门面、`ClientProxy` / `CommonProxy` |
| `config/` | 10 | 配置分节加载（debug / compat / ai / voice / assistant / plannerHud / dataLoom / superOrange / grapple） |
| `compat/` | 20 | GTNH / AE2 版本兼容（`ae/` 门面 + `legacy/` + `native_/`） |
| `assistant/` | 41 | AI 助手核心（意图解析 / AE2 查询 / 合成下单 / 传送 / 计划），详见 `ai-assistant.mdc` |
| `items/` | 53 | 物品（含 `items/cell/` 33 文件：数据编织元件 AE2 集成） |
| `blocks/` | 5 | 自定义方块（监视器 / 链接器 / 挂索节点） |
| `tileentity/` | 5+2 | TileEntity（含 `IOwnableTile` 接口工具） |
| `gui/` | 35 | GUI 组件与界面（`custom/` / `guiscreen/` / `manual/` / `container/` / `handler/`），详见 `gui-guidelines.mdc` |
| `handler/` | 28 | 事件处理器与服务端逻辑（挂索 / 星空剑 / 口袋 / 扫描器 / 编织元件） |
| `entity/` | 8 | 实体类 only（不含 Render） |
| `renders/` | 33 | 全部客户端渲染（TESR / 实体 Render / 物品 Render / HUD / 手册页面） |
| `network/` | 24 | 网络通信（19 packet + 5 handler），详见 `network-packets.mdc` |
| `voice/` | 9 | 语音识别（Vosk / HTTP STT / 音频处理） |
| `loader/` | 9 | 注册加载器（Block / Item / TE / Entity / Handler / Gui / Render / Network / Recipe） |
| `client/` | 18 | 客户端事件与缓存（挂索 / 口袋 / AI 偏好 / 按键） |
| `command/` | 2 | 命令（`/admassistant`、`/admai`） |
| `utils/` | 10 | 工具类（NBT / JSON / `BlockPos` / 校验等） |
| `mixin/` | 6 | Mixin 注入（仅客户端：GUI / NEI 兼容 / tooltip Invoker） |

## 资源文件 (src/main/resources/assets/advancedatamonitor/)

```
assets/advancedatamonitor/
 config/
 assistant-lexicon.json       # AI 助手词库配置
 assistant-features.json      # AI 助手功能配置
 lang/
 en_US.lang                   # 英文翻译
 zh_CN.lang                   # 中文翻译
 manual/
 index.json                   # 手册目录（章节列表）
 chapters/                    # 手册各章节内容JSON
 model/                         # .obj 模型文件（方块 3D 模型）
 textures/items/                # 物品贴图
 voice/vosk/zh-small/           # Vosk 离线语音模型
```

## 工作区临时产物 (`.workspace/`)

不属于模组源码，已 gitignore，详见 `workspace-artifacts.mdc`。Agent 与开发者产生的临时文件只放此处，不得放在项目根目录或 `src/` 下。

## 修改指南：改动某种功能时应该看哪些文件

| 需求 | 查看文件 |
|------|---------|
| 新增 Block | `loader/LoaderBlock.java` → `blocks/` → `tileentity/` → `en_US.lang` + `zh_CN.lang` |
| 新增 Item | `loader/LoaderItem.java` → `items/` → `en_US.lang` + `zh_CN.lang` |
| 新增 GUI | `loader/LoaderGui.java` → `gui/guiscreen/` → `en_US.lang` + `zh_CN.lang` |
| 新增网络包 | `network/packet/` → `network/handler/` → `loader/LoaderNetwork.java` → 完整数据同步链路见 `network-packets.mdc` |
| 新增渲染 | `renders/`（实体 Render 也放此包）→ `loader/LoaderRender.java` |
| 修改/新增 AI 助手功能 | 详见 `ai-assistant.mdc` → `docs/zh/ai-assistant/开发指南.md` / `docs/en/ai-assistant/development-guide.md` → `assistant/AssistantController.java` |
| 修改挂索 | `docs/zh/subsystems/挂索节点系统设计.md` / `docs/en/subsystems/grapple-system-design.md` → `handler/Grapple*.java` + `client/HandlerGrappleClient.java` |
| 修改次元口袋 | `handler/PocketState.java`（状态 + 堆叠升级字段）/ `handler/PocketStore.java`（JSON存储）/ `handler/PocketInventory.java`（IInventory 实现，动态堆叠上限）/ `handler/PocketUpgradeRules.java`（升级校验，含堆叠/无尽堆叠规则）+ `client/PocketOverlayHandler.java`（覆层渲染/鼠标路由，通过自定义包通信）/ `client/GuiPocketOverlay.java`（悬浮窗渲染 + hover tooltip）/ `client/PocketClientCache.java`（客户端缓存，含堆叠字段）+ `items/ItemDimensionalPocket.java`（口袋物品）/ `items/ItemSpaceUpgradeCard.java` / `items/ItemPageUpgradeCard.java` / `items/ItemStackUpgradeCard.java` / `items/ItemInfiniteStackUpgradeCard.java` + `gui/container/ContainerDimensionalPocket.java`（升级配置容器，4 槽位：空间/翻页/堆叠/无尽堆叠）/ `gui/container/ContainerPocketStorage.java`（存储容器）+ `gui/guiscreen/GuiDimensionalPocketConfig.java`（升级配置 GUI，4 槽位）/ `gui/guiscreen/GuiPocketStorage.java`（存储 GUI，右键主入口）+ `network/packet/PacketPocketAction.java`（C→S 动作，含堆叠升级/存取操作类型）/ `network/packet/PacketPocketSync.java`（S→C 同步，含堆叠字段）。**GUI 打开约定**：口袋存储/配置 GUI 必须由**服务端** `player.openGui` 触发（存储 GUI 在 `ItemDimensionalPocket.onItemRightClick` 用 `if(!world.isRemote)`；配置 GUI 经 `PacketPocketAction.OPEN_CONFIG_GUI` 由服务端 `handleServer` 调用 `openGui`），否则服务端 `openContainer` 停留为 `ContainerPlayer`(windowId=0)，物品/升级卡点击会落到错误容器而弹回背包。 |
| 修改计划器 | `docs/zh/developer/技术文档.md` §5.11 / `docs/en/developer/technical-documentation.md` §5.11 → `items/ItemAdvancePlanner.java` |
| 更新玩家/开发者文档 | `docs/zh/player/用户手册.md` / `docs/en/player/player-guide.md` · `docs/zh/developer/技术文档.md` / `docs/en/developer/technical-documentation.md` · `docs/README.md` |
| 修改语音识别 | `voice/VoiceCaptureService.java` → `voice/VoskSpeechToTextClient.java` |
| 新增命令 | `command/` → `AdvanceDataMonitor.java` (serverStarting) |
| 修改配置项 | `Config.java` + `config/Config*Loader.java` + `ConfigDescriptions.java` → `gui/guiscreen/GuiAISettings.java` |
| 新增复杂功能子系统 | 评估是否需系统级包（仅 `assistant/`、`voice/`）→ 物品逻辑归 `items/`、`handler/`、`gui/` → `loader/` → `config/`（如需）→ lang → 本规则 |
| 修改/新增手册 GUI（搜索、滚动、高亮） | `gui/guiscreen/GuiManual.java` → `gui/manual/ManualSearch*.java` / `ManualTextHighlighter.java` → `renders/ManualPageRenderer.java` → `getting_started` lang 文案 → `docs/` §3.12 / §5.10 |
| 新增手册内容 | `assets/.../manual/` JSON (11 章: `getting_started` / `monitor_blocks` / `monitor_tools` / `data_monitor` / `dimensional_pocket` / `data_loom_cells` / `grapple_system` / `combat_special` / `ai_assistant` / `planner` / `config_reference`) → `gui/guiscreen/GuiManual.java` → `renders/ManualPageRenderer.java` → `en_US.lang` + `zh_CN.lang` → 本规则 |
| 新增 AE2 存储元件 | `items/cell/` → `loader/LoaderItem.java` → `AdvanceDataMonitor.java` (postInit) → `en_US.lang` + `zh_CN.lang` → 手册 JSON |

## 本规则同步维护要求

本文件是 Agent 快速定位代码的核心索引。**每次在项目中新增、删除或重命名文件时，必须同步更新本规则与 `project-structure-details.mdc`**，不得遗漏：

- 本规则（核心索引）：更新「包概览」表的文件计数与一句话作用
- `project-structure-details.mdc`（详细清单）：更新对应包的逐文件清单、lang 键、关键文件列表

执行时机：与 `ai-assistant-docs-sync.mdc`、`docs-sync.mdc` 配合，当该规则触发文档同步时应同时检查本规则与详细清单是否需要更新。其他非 AI 助手功能的文件变更后，Agent 应主动检查并更新本规则。
