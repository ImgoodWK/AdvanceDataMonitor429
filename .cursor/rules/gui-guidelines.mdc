---
description: GUI 编写规范，必须参考现有 GUI 的风格编写
globs: **/gui/**
alwaysApply: false
---

# GUI 编写规范

## 基类选择

### 1. 小型配置对话框 → 继承 `AdmItemConfigScreen`
手持物品 NBT、挂索节点等小型配置窗优先使用 `AdmItemConfigScreen`（位于 `gui.custom`）。

- 自动居中、`Keyboard.enableRepeatEvents`、保存/取消按钮工厂、`AdmGuiTextures` 纹理。
- 参考 `GuiGrappleHookConfig`、`GuiGrappleAnchorConfig`、`GuiSuperOrangeConfig`。

### 1b. 监视器绑定子页 → 继承 `AbstractMonitorSubGui`
数据监视器 per-binding 子界面继承 `AbstractMonitorSubGui`，勿复制 `drawTextFieldBackground` / `autoTextField` 样板。

- 参考 `GuiSubAEAdvanceStorageLink` 及同系列 `GuiSub*`。
- 纹理使用 `AdmGuiTextures`；同步走基类 `saveAndSync` / `openMainGui`。
- `GuiSubWebDashboard` 也是 per-binding 子页，但属于无目标坐标的被动快照；正文必须走 `PacketMonitorWebSurface` 单绑定有界上传，不得塞回常规 `PacketSynTileEntity` 全 TE 同步。

### 1c. 其他配置/设置界面 → 优先 `AdmUiScreen`，旧屏可暂留 `ADM_GuiScreen`
**新**复杂配置屏优先继承 `AdmUiScreen`（`gui.framework.host`）：Flex 组件树 + `UiStyle`，见 `docs/zh/developer/ui-framework.md`。

保留旧布局的屏幕（AI 聊天、计划器等）仍可用 `ADM_GuiScreen`（`gui.custom`）：

- 提供 `setBackgroundTexture()` / `setPosition()` / `setSize()` / `setStretch()` 链式调用。
- 背景纹理使用 `AdmGuiTextures.BACKGROUND_SUB` 等共享常量；基类会把旧主背景、子面板、按钮与输入框常量桥接为 `UiThemes.ADM` 的 Meowa 图集区域，勿重复定义 `ResourceLocation` 或直接拉伸历史 PNG。
- 仍需固定首选尺寸的旧屏必须用 `GuiResponsiveLayout.fitCentered(...)` 将实际面板约束进 scaled viewport，并让控件、滚动区域、文本换行都引用运行时宽高；不得只移动背景而保留超屏坐标。
- 触达时再迁 Flex 树；**禁止**新屏继续堆绝对坐标样板。

### 1d. Flex UI 框架（配置 + 容器统一）
除**次元口袋**全链路外，统一使用 `gui/framework` 组件树（`UiWidget` / `UiFlex` / `UiStyle`）。权威设计与移植指南：`docs/zh/developer/ui-framework.md`。

### 2. 容器界面（有物品槽位） → 继承 `ADM_UiContainer`（新界面强制）
用于需要 `InventoryPlayer` + `Container` 的标准容器界面。

- **新容器 GUI 必须**继承 `ADM_UiContainer` + `UiThemes.ADM`（口袋主题 stub 不纳入迁移）。
- 优先挂 `UiWidget` 树；命令式 `UiPanel`/`UiButton` 仅兼容旧代码。
- 参考 `GuiMatterBallDecompressor.java`、`GuiUiFrameworkDebug.java` + `docs/zh/developer/ui-framework.md`。
- `GuiAdvanceStorageLink` 等既有容器也必须使用主题面板/槽位；口袋 GUI **永久排除**本批次。
- 标准尺寸：`xSize = 176`, `ySize = 184`（固定布局容器）。
- 重写 `drawGuiContainerBackgroundLayer` 和 `drawGuiContainerForegroundLayer`。

### 2b. UI 框架速查（`gui/framework/`）

| 场景 | 使用 |
|------|------|
| Flex 布局 | `UiFlex.row()` / `column()` + `UiFlexLayoutEngine` |
| 样式 | `UiStyle`（padding/margin/gap/background） |
| 配置屏宿主 | `AdmUiScreen` + `buildUi()` |
| 容器宿主 | `ADM_UiContainer` + 可选 `setUiRoot` |
| 9-slice 主面板 | `UiPanel.draw` 或 `UiStyle.backgroundNineSlice` |
| 内嵌列表/预览/tooltip | `UiPanel.drawSection` |
| 分隔线 | `UiPanel.drawDivider(UiThemes.ADM, ...)` |
| 标题/标签 | `UiLabel` / `UiText` |
| 按钮 | `UiButtonWidget`（树）或命令式 `UiButton`；四态含 pressed |
| 物品槽位 | `UiSlot.drawTheme(UiThemes.ADM, ...)` |
| 滚动 | `UiScrollPanel`；手工列表使用主题 `scrollTrack` / `scrollThumb` |
| 主题 / 图集 | `UiThemes.ADM`；`textures/gui/adm_ui_atlas.png` |

**排除**：次元口袋 GUI / Overlay / Mixin。不硬依赖 Qz-UILib / 不整库迁 ModularUI2。

程序色块仅用于选中、hover、交替行、HUD 拖动等瞬时状态；面板外壳必须用 `mainPanel` / `sectionPanel`。AE2 `GuiImgButton` 模式按钮和升级卡列保留原生图标/状态语义。

新增或修改 `gui/guiscreen/` 时必须通过 `GuiThemeCoverageTest`：除 `GuiDimensionalPocketConfig`、`GuiPocketStorage` 两个固定排除项外，界面必须继承 ADM 宿主或显式绘制 `UiThemes.ADM` 面板，禁止实例化原版 `GuiButton`，禁止直接绑定独立旧 GUI PNG。涉及大面板时还必须通过 `GuiLowResolutionLayoutTest` 的 427×240 合同。修改 `adm_ui_atlas.png` 或 `AdmUiTheme` UV 时必须同步批准哈希并通过 `AdmUiAtlasContractTest`。

### 3. 简单消息弹窗 → 继承原版 `GuiScreen`
仅用于提示/确认对话框。参考 `GuiScreenMessage.java`。

## 构造函数模式

### 子配置界面（编辑已有数据条目）
```java
public GuiSubXxx(EntityPlayer player, World world, TileEntityAdvanceDataMonitor tileEntity, int index)
```
- `player` / `world` — 用于获取语言设置和世界信息
- `tileEntity` — 数据源 TileEntity
- `index` — 数据条目索引（编辑第几个数据配置）

### 新增配置界面（创建新数据条目）
```java
public GuiSubBind(EntityPlayer player, World world, TileEntityAdvanceDataMonitor tileEntity)
```
- 无 `index` 参数，因为是新条目。

### 独立界面（不依赖 TileEntity）
```java
public GuiAIChat(GuiScreen parent)   // 接收父界面引用，用于返回
public GuiAdvancePlanner(ItemStack plannerStack, EntityPlayer player)  // 接收 ItemStack
```

### 容器界面
```java
public GuiAdvanceStorageLink(InventoryPlayer playerInventory, TileEntityAdvanceStorageLink tile)
```

## drawScreen 编写模板

```java
@Override
public void drawScreen(int mouseX, int mouseY, float partialTicks) {
    // 1. 列表、预览、tooltip 等内嵌区域先绘制主题 sectionPanel
    UiPanel.drawSection(UiThemes.ADM, panelX, panelY, panelW, panelH);

    // 2. 标签颜色从主题取得；仅强调信息使用 textAccent
    fontRendererObj.drawStringWithShadow(
        I18n.format("adm.label.xxx"),
        leftLabelX, currentY, UiThemes.ADM.textPrimary());

    // 3. 数值/百分比等强调信息紧跟文本框
    fontRendererObj.drawStringWithShadow(
        currentAlpha + "%",
        field.xPosition + field.width + 4, field.yPosition + 5, UiThemes.ADM.textAccent());

    // 4. 调用 super.drawScreen() 绘制按钮和文本框
    super.drawScreen(mouseX, mouseY, partialTicks);

    // 5. 绘制输入框提示文字（当输入框聚焦时）
    for (Entry<ADM_GuiTextField, String> entry : fieldHints.entrySet()) {
        ADM_GuiTextField field = entry.getKey();
        if (field.isFocused() || ...) {
            String hintKey = entry.getValue();
            // 在屏幕底部绘制提示
            fontRendererObj.drawStringWithShadow(
                I18n.format(hintKey), leftLabelX, hintY, UiThemes.ADM.textDisabled());
        }
    }
}
```

## initGui 编写模板

```java
@Override
public void initGui() {
    super.initGui();
    Keyboard.enableRepeatEvents(true);   // 允许长按重复输入
    buttonList.clear();                   // 清除默认按钮
    fieldList.clear();                    // 如果有自定义字段列表

    // 1. 设置界面布局
    setPosition(guiLeft, guiTop);
    setSize(guiWidth, guiHeight);

    // 2. 初始化文本框；默认纹理会自动桥接到 ADM 主题
    fieldName = new ADM_GuiTextField(fontRendererObj, x, y, w, h);
    fieldName.setMaxStringLength(...)
        .setText(currentValue)
        .setHintText(I18n.format("adm.hint.xxx"));
    fieldList.add(fieldName);

    // 3. 初始化按钮；默认即使用 ADM 四态主题
    ADM_GuiButton btnSave = new ADM_GuiButton(0, x, y, w, h, I18n.format("adm.button.save"));
    btnSave.setTextColor(UiThemes.ADM.textPrimary())
        .setTextHoverColor(UiThemes.ADM.textAccent());
    buttonList.add(btnSave);

    // 4. Alpha +/- 按钮对（如果界面包含 alpha 配置）
    // 用 "-" / "+" 作为文字，宽度约 12，紧贴对应字段右侧
}
```

## 颜色规范

| 用途 | 颜色值 | 说明 |
|------|--------|------|
| 主文本色 | `UiThemes.ADM.textPrimary()` | 常规标签与按钮文字 |
| 强调 / Hover | `UiThemes.ADM.textAccent()` | 数值、选中与鼠标悬停强调 |
| 灰色文本 | `0xAAAAAA` / `0x888888` / `0x666666` | 次要信息、时间戳、提示文字 |
| 成功/启用 | `0x55FF55` | 绿色，已完成状态、启用状态 |
| 错误/禁用 | `0xFF5555` | 红色，错误提示、禁用状态 |
| 禁用文本 | `UiThemes.ADM.textDisabled()` | 不可用按钮文字 |
| 状态叠层 | `0x30004040` / `0x20002020` 等 | 仅列表交错行、选中/hover、HUD 拖动；不得充当面板外壳 |

## 文本框组件 (ADM_GuiTextField)

- 构造函数会关闭原版背景，并默认使用 `AdmGuiTextures` 的输入框常量；绘制时自动桥接到 ADM normal/focused 主题区域。
- 仅确有独立功能纹理时才调用 `setBackgroundTexture()` / `setFocusedBackgroundTexture()` 覆盖默认主题。
- 通过 `setHintText()` 设置失焦时的占位提示。
- 文本框坐标有偏移：`yPosition = y - 8`, `xPosition = x - 21`（在构造函数中自动处理）。

## 按钮组件 (ADM_GuiButton)

链式调用模式：
```java
new ADM_GuiButton(id, x, y, w, h, text)
    .setTextColor(UiThemes.ADM.textPrimary())
    .setTextHoverColor(UiThemes.ADM.textAccent());
```

默认 `AdmGuiTextures.BUTTON*` 会自动使用 ADM normal / hover / pressed / disabled 区域。仅功能控件拥有独立美术语义时才设置自定义纹理。

特殊效果：
- `setUseRGBEffect(true)` — 按钮文字自动循环彩虹色（用于选中/激活状态的按钮）
- 按钮 ID 必须唯一，建议在 `initGui` 末尾调用 `checkUsedButtonIds()` 检测重复。

## 数据保存与网络同步

所有数据配置类界面通过统一模式保存。**当 GUI 涉及网络包时，必须同时完成网络包 Handler 的编写和注册，参见 `network-packets.mdc`。**

```java
private void saveAndSync() {
    NBTTagCompound nbt = tileEntity.getDisplayData(index);
    // 写入所有配置字段...
    nbt.setString("displayName", fieldDisplayName.getText());
    nbt.setDouble("scale", Double.parseDouble(fieldScale.getText()));
    // ...
    tileEntity.setDisplayData(index, nbt);
    tileEntity.writeToNBT(new NBTTagCompound());  // 持久化到磁盘
    TeXTech.ADMCHANEL.sendToServer(
        new PacketSynTileEntity(tileEntity.xCoord, tileEntity.yCoord, tileEntity.zCoord, nbt));
}
```

- **必须** `writeToNBT` 确保数据持久化。
- **必须** 通过 `ADMCHANEL.sendToServer(new PacketSynTileEntity(...))` 同步到服务端。
- **如果新增网络包**：同步编写 Handler（服务端 readFromNBT → markDirty → syncData）并在 `LoaderNetwork.registerNetWorks()` 注册。详见 `network-packets.mdc`。
- 大正文例外：WebAE 仪表盘正文由 `PacketMonitorWebSurface` 上传/按哈希拉取；`TileEntityAdvanceDataMonitor.writeSyncNBT()` 必须继续从普通同步剥离 `webDashboardPayload`。

## 语言本地化

- 所有用户可见文本使用 `I18n.format("adm.xxx")`，禁止硬编码。
- 格式化码使用 `§` 符号：`§l` (粗体)、`§n` (下划线)、`§m` (删除线)。
- 标题使用 `§l` 加粗：`adm.title.xxx=§lAdvance Data Monitor`。

## 输入校验

- 使用 `ContentsHelper` 工具类进行字段校验：
  - `isValidDouble(s)` / `isValidInteger(s)` / `isValidPosFormat(s)` / `isValidHexColor(s)`
- 校验失败时显示 `adm.error.xxx` 对应的错误提示。
- 保存按钮 `actionPerformed` 中依次校验所有字段，通过后才执行 `saveAndSync()`。

## 必须的覆写

- `doesGuiPauseGame()` → 返回 `false`（不暂停游戏）。
- `onGuiClosed()` → 恢复 `Keyboard.enableRepeatEvents(false)`。
- 需要滚轮支持时覆写 `handleMouseInput()`，检查 `Mouse.getEventDWheel()`。
