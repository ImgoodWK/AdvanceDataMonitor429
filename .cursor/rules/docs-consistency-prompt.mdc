---
description: 任务结束时，若可能涉及文档变动，询问用户是否检查文档与代码一致性
alwaysApply: true
---

# 文档一致性检查询问

## 执行时机

在本轮对话**完成主要工作后、给出最终总结之前**，若本次工作**可能**影响项目文档或与文档描述不一致，须**主动询问用户**（中文），而不是默认全量扫描或静默跳过。

## 可能涉及文档的判定（任一即触发询问）

- 新增/删除/重命名 Java 源文件、包或资源（含 `manual/` JSON、`lang`）
- 修改功能行为、配置项、调试开关、网络包、GUI、渲染或子系统逻辑（挂索、计划器、编织元件、AI 助手等）
- 调整项目结构、目录约定或 Cursor 规则索引（`project-structure.mdc` 等）
- 用户明确要求改代码但未提及文档，且改动范围超出纯内部重构（无对外行为/结构变化）

**不触发**：仅改注释、格式化、纯问答无代码改动、或用户已明确说「不用动文档」。

## 询问方式

用简短中文询问，例如：

> 本次改动可能影响项目文档。是否需要我检查 `docs/`、手册 JSON、lang 及 Cursor 规则与当前代码是否一致，并帮你更新？

- **一次即可**：同一轮对话末尾问一次；用户已在本轮明确答复过的，不必重复追问。
- **不代替同步规则**：仍须遵守 `docs-structure-sync.mdc`、`ai-assistant-docs-sync.mdc` 等在变更当时应做的必要更新；本规则是在**额外**提供可选的全局一致性核对。

## 用户同意后的检查范围

按本次改动类型优先核对，典型路径：

| 类型 | 优先检查 |
|------|----------|
| 通用 / 结构 | `docs/zh/`、`docs/en/`、`README.md`、`project-structure.mdc` |
| AI 助手 | `docs/zh/ai-assistant/`、`assistant-features.json`、`ai-assistant.mdc` |
| 玩家向 | `docs/zh/player/`、`manual/chapters/`、`lang` |
| 配置 | `ConfigDescriptions.java`、手册 `config_reference`、开发者文档配置章节 |

发现不一致时：简要列出差异，经用户确认或按用户指示更新；更新后说明改了哪些文件。

## 用户拒绝或暂不处理

尊重选择，在总结中可一句提示「文档尚未核对，需要时可再说」即可，勿反复劝说。
