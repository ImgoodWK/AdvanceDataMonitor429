---
description: 文档与规则同步：变更时强制同步；任务结尾陈述已改/缺口，不追问
alwaysApply: true
---

# 文档与规则同步

## Agent 读文档策略（减 token）

- **默认单语**：中文任务只读 `docs/zh/`；英文任务只读 `docs/en/`。不要同时全量读双语。
- **禁止默认读 L3 / 归档**：除非用户点名愿景/设计/归档，禁止 Read `docs/*/design/`、`docs/*/archive/`。
- 功能域应对文件见 `docs/zh/developer/documentation-map.md`（或 en 同名）；详细核对用 skill `textech-doc-sync-pr`。

## 变更时强制同步

| 变更类型 | 必须更新 |
|---------|---------|
| 新增/删除/重命名 Java 源文件或包 | `project-structure.mdc`（包计数/一句话）+ `project-structure-details.mdc`（逐文件；按需 Read 后更新） |
| 渲染类迁移 | `project-structure-details.mdc` 的 `renders/` / `entity/` |
| Config 增删改 | `Config.java`、`config/Config*Loader.java`、`ConfigDescriptions.java`；影响手册则改 `manual/` |
| 调试开关变更 | `gtnh-mod-context.mdc`；必要时技术文档 §16 |
| AI 助手功能变更 | 另遵循 `ai-assistant-docs-sync.mdc` |

约定：`entity/` 仅 Entity；`renders/` 放全部客户端渲染；Config 门面在 `Config.java`，分节在 `config/`；debug 默认 `false`。

## 任务结尾提醒（不询问）

总结时用 **1～2 句陈述**已同步路径，或缺口建议路径；**不要**问「要不要再改文档」。

**触发**：增删类/lang/manual、行为或配置变更、结构规则变更。  
**不触发**：纯注释/格式化/纯问答，或用户说「不用动文档」。

仅用户明确要求全量核对时再扫 docs/manual/lang/rules。

## PR 前校验

涉及 Config、网络包、WebAE、lang 或 `docs/` 时：

```bash
python tools/doc-check/doc-consistency-check.py
```

warn-only；不阻断 Gradle。
