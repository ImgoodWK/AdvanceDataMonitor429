---
description: 各包逐文件清单、lang 键、游戏内名称（project-structure 核心索引的详细附录；按需 Read，不自动注入）
alwaysApply: false
---

# 项目结构详细清单

本文件是 `project-structure.mdc`（核心索引）的详细附录，列出每个包的逐文件清单、lang 键与游戏内名称。**不自动注入**；新增/删改/重命名类，或需要逐文件定位时，再 Read 本文件对应包章节。

## 包结构 (src/main/java/com/imgood/textech/)

### 根包 — 模组入口
| 类 | 作用 |
|----|------|
| `TeXTech.java` | @Mod 主类，注册调度中心 |
| `Config.java` | 配置公共门面（字段 + 保存 API）；分节加载见 `config/` 包。含 `[webConsole]` 图标、Spark、截图清晰度/带宽/并发/保留上限等字段 + `[debug]` WebAE 分功能开关；`reloadConfiguration()` 复用 `activeConfigFile` 重载分节（供 `/admweb reload`） |
| `ConfigDescriptions.java` | 配置项双语说明（英文 / 中文，供 .cfg 注释与手册引用） |
| `ClientProxy.java` | 客户端代理（按键注册；FML bus 注册 `IconRenderer` + `IconLazyRenderQueue`） |
| `TeXTechDataDir.java` | 运行时数据目录统一入口（`TeXTech/WebAE|Assistant|Grapple/`） |
| `TeXTechDataMigration.java` | preInit 启动迁移：`config/textech/`、`config/advancedatamonitor/`、`TeXTechWebAE/` → `TeXTech/`（移动，config 仅保留 `.cfg`） |
| `CommonProxy.java` | 通用代理（空实现或共用逻辑） |

### config/ — 配置分节加载 (16 文件)
- `ConfigDebugLoader.java` — `[debug]` 通用/GUI/WebAE 调试开关（含 `debugWebae` 控制 NEI 配方采集器诊断日志）
- `ConfigCompatLoader.java` — `[compat]` AE profile 旧配置兼容读取；2.0 始终 NativeFluid，`legacy` override 被忽略
- `ConfigAiLoader.java` / `ConfigAiMutators.java` — `[ai]`
- `ConfigVoiceLoader.java` — `[voice]`
- `ConfigAssistantLoader.java` — `[assistant]`
- `ConfigPlannerHudLoader.java` — `[plannerHudLimits]`
- `ConfigDataLoomLoader.java` — 编织元件各节 + `[dataLoomCell]`
- `ConfigSuperOrangeLoader.java` — `[superOrange]`
- `ConfigMatterBallDecompressorLoader.java` — `[matterBallDecompressor]`
- `ConfigGrappleLoader.java` — `[grapple]`
- `ConfigWebaeLoader.java` — `[webConsole]`（含 WebAE 告警总闸 `alertsEnabled`、阶段 3.1 图标三开关、可选 Spark 配置 + **Phase6** `dynmapBaseUrl`）
- `ConfigCardBattleLoader.java` — `[cardBattle]`（独立卡牌服务 URL + 私密 Bridge Token；两者为空时关闭）
- `ConfigWebaeDebugLoader.java` — `[debug]` 节 WebAE 分功能 debug 开关（阶段 3.3：`webaeIcons`/`webaeChat`/`webaeDashboard`/`webaeSynthesis`/`webaePatterns`，默认 false，gate `WebAeDebugLog`）
- `ConfigWebAlertsLoader.java` — 加载/保存 `TeXTech/WebAE/web-alerts.json`（WebAE 自动化告警规则）
- `ConfigWebOrderTemplatesLoader.java` — 加载/保存 `TeXTech/WebAE/web-order-templates.json`（按 ownerUuid 批量下单模板）

### compat/ — GTNH / AE2 / 可选社区模组兼容 (26 文件)
- **compat/ae/** — `AeCompat` 门面、`GtnhEnvironmentProbe`、`GtnhVersion`、`AeCellStatsAdapter` 等
- **compat/ae/legacy/** — Native 适配器仍复用的混合格式/迁移辅助源码；不代表支持旧整合包
- **compat/ae/native_/** — 2.0 实际绑定的 GTNH 2.9.0-beta-2+ AE2 原生流体路径
- 详见 `docs/zh/developer/ae-compat-290.md` / `docs/en/developer/ae-compat-290.md`
- **compat/gt/** — `GtCompat.java`：GT 机器状态读取兼容层
- **compat/programmablehatches/** — `ProgrammableHatchesCompat.java`：无编译期依赖的 Programmable Hatches V3 编程电路包装/解包

### assistant/ — AI 助手核心逻辑 (47 文件：根 39 + ai 8)
- **最大的包**，负责 AI 意图解析、AE2 物品查询、合成下单、传送、计划管理
- **assistant/ai/** — AI 聊天 HTTP 客户端与内置联网搜索（8 文件）：`DeepSeekChatClient.java` / `ChatRequestOptions.java` / `ChatStreamListener.java` / `ChatResponse.java` / `AiProviderProfiles.java` / `WebSearchService.java` / `WebSearchData.java` / `WebSearchResult.java`
- **意图系统**: `AssistantIntentType.java` / `AssistantIntent.java` / `AssistantIntentTask.java` / `AssistantIntentPlan.java` / `AssistantOrderLine.java`
- **AI 解析**: `AssistantAiIntentService.java` / `AssistantAiIntentJsonParser.java`
- **本地解析**: `AssistantIntentService.java` (关键词匹配 fallback)
- **主控制器**: `AssistantController.java`
- **会话管理**: `AssistantSession.java` / `AssistantSessionKind.java`
- **AE2 核心**: `AssistantServerServices.java` / `AssistantCraftJobManager.java` / `CraftSubmitHooks.java` / `CraftingCandidate.java` / `WithdrawSubmitOutcome.java` / `AssistantCandidateDelivery.java` / `CandidateQueryResult.java` / `CandidateBatchMeta.java`
- **词库/配置**: `AssistantLexicon.java` / `AssistantFeatureConfig.java`
- **订单记忆**: `OrderMemoryStore.java`
- **计划管理**: `PlannerServerService.java` / `PlanStore.java`
- **传送服务**: `TeleportService.java` / `TeleportDestination.java`
- **AE2 查询**: `WirelessPowerQuery.java` / `WirelessSteamQuery.java`
- **格式化/工具**: `AssistantFormatter.java` / `AssistantOperationsBriefing.java` / `AssistantTimeParser.java` / `AssistantTextNormalizer.java`
- **物品处理**: `ItemStackUtils.java` / `PlayerInventoryUtil.java` / `PatternDetailFormatter.java`
- **注册/调试**: `AssistantMonitorRegistry.java` / `AssistantDebugLog.java`
- **数据文件**: `TeXTechDataDir.java` + `TeXTechDataMigration.java`（`<实例>/TeXTech/<feature>/` 统一路径；启动时从旧路径移动）+ `AssistantDataFiles.java`（按文件名路由到 WebAE/Assistant/Grapple 子目录）

### items/ — 物品 (55 文件：根 22 + cell 33)
| 类 | 游戏内名称 (ZH / EN) | lang 键 |
|----|---------------------|---------|
| `ItemAdvancePlanner` | 高级计划器 / Advance Planner | `item.advancePlanner.name` |
| `ItemAdvanceLinkScanner` | 高级链接扫描器 / Advance Link Scanner | `item.advanceLinkScanner.name` |
| `ItemAdvanceStorageLinkCell` | 高级存储链接元件 / Advanced Storage Link Cell | `item.advanceStorageLinkCell.name` |
| `ItemDataImprint` | 数据映录器 / Data Imprint Tool | `item.dataImprint.name` |
| `ItemManual` | 高级数据监视器手册 / TeXTech Manual | `item.manual.name` |
| `ItemSuperOrange` | 超能砂糖桔 / Super Orange | `item.orange.name` |
| `ItemGrappleHook` | 挂索器 / Grapple Hook | `item.grappleHook.name` |
| `ItemStarryCosmosSword` | 至高天圣裁 / Empyrean Holy Judgment | `item.starryCosmosSword.name` |
| `ItemHolyJudgment` | 圣裁 / Holy Judgment | `item.holyJudgment.name` |
| `ItemDimensionalPocket` | 次元口袋 / Dimensional Pocket | `item.dimensionalPocket.name` |
| `AbstractPocketUpgradeCard` | 口袋升级卡基类（4 张子类共享 tooltip 格式） | — |
| `ItemSpaceUpgradeCard` | 空间升级卡 / Space Upgrade Card | `item.spaceUpgradeCard.name` |
| `ItemPageUpgradeCard` | 翻页升级卡 / Page Upgrade Card | `item.pageUpgradeCard.name` |
| `ItemStackUpgradeCard` | 堆叠升级卡 / Stack Upgrade Card | `item.stackUpgradeCard.name` |
| `ItemInfiniteStackUpgradeCard` | 无尽堆叠升级卡 / Infinite Stack Upgrade Card | `item.infiniteStackUpgradeCard.name` |
| `ItemBlockGrappleAnchor` | 挂索节点（方块物品） | `tile.grappleAnchor.name` |
| `PlannerEntry` / `PlannerMergeMode` | （数据结构，与计划器同层） | — |
| `LinkScanEntry` / `LinkScanBlockType` | （扫描器数据结构/枚举） | `adm.scanner.type.*` |
| **items/cell/** 子包 (33 文件) | 见下 | `item.data*LoomCell.name` 等 |

#### items/cell/ — 数据编织元件 AE2 集成 (33 文件)
- 负责织尘/织形/涌流/织源元件的 AE2 驱动器/箱子兼容、时间基物品/流体/源质编织、能量消耗
- `ItemDataDustLoomCell.java` — 织尘元件（仅粉类，不可标记本模组物品）
- `ItemDataFormLoomCell.java` — 织形元件（任意物品，不可标记本模组物品）
- `ItemDataFlowCell.java` — 涌流元件（任意流体，5 种类；`FlowLoomCellConfig` 拒绝本模组物品标记）
- `ItemDataTideLoomCell.java` — 织潮元件（涌流升级，63 种类，附魔光效；其余同涌流元件）
- `ItemDataSourceLoomCell.java` — 织源元件（仅神秘时代源质 aspect 流体）
- `AbstractDataLoomFluidCell.java` — 流体编织元件基类（`DataLoomFluidCellConfig`）
- `ItemWeaveAmplifier.java` / `ItemSuperWeaveAmplifier.java` — 编织增幅卡（默认 4×/16× 每张，Config 注册时写入 `DataLoomAmplifierRates`）
- `DataLoomCellTooltipCache.java` — 元件 tooltip 增幅/有效速率 NBT 缓存（仅工作台变更时刷新）；公共 footer 含 高级存储链接器 排除说明
- `DataLoomAmplifierRates.java` — 增幅倍率静态常量（Config.load 后初始化）
- `DataLoomCellConfig.java` / `FormLoomCellConfig.java` / `FlowLoomCellConfig.java` / `DataLoomFluidCellConfig.java` — 工作台分区槽
- `DataLoomDebugLog.java` — `Config.dataLoomCellDebugLogging` 为 true 时编织 trace 日志
- `DataLoomCellHandler.java` — ICellHandler 注册
- `DataLoomItemInventory.java` / `DataLoomFluidInventory.java` — IMEInventoryHandler 实现
- `DataLoomCellUpgrades.java` — 仅允许编织增幅卡的升级槽
- `AbstractDataLoomItemCell.java` / `DataLoomCellUtil.java` / `DustLoomCellConfig.java` / `IWeaveAmplifierCard.java`

### blocks/ — 自定义方块 (5 文件)

> 原 `BlockAdvanceCraftingLink` / `BlockAdvanceStorageLink` / `BlockAdvanceNetworkLink` 三类 Link 已合并为统一 `BlockAdvanceNetworkLink`（注册名 `advNetworkLinkBlock`）。

| 类 | 游戏内名称 (ZH / EN) | lang 键 |
|----|---------------------|---------|
| `BlockAdvanceDataMonitor` | 高级数据监视器 / Advance Data Monitor | `tile.advDataMonitor.name` |
| `BlockAdvanceNetworkLink` | 高级网络链接器 / Advanced Network Linker | `tile.NetworkLinkBlock.name` |
| `BlockGrappleAnchor` | 挂索节点 / Grapple Anchor | `tile.grappleAnchor.name` |
| `BlockMatterBallDecompressor` | 物质球解压器 / Matter Ball Decompressor | `tile.matterBallDecompressor.name` |
| `BlockUiFrameworkDebug` | UI 框架调试方块 / UI Framework Debug Block | `tile.uiFrameworkDebug.name`（`[debug] uiFrameworkBlock=true` 时注册） |

### tileentity/ — TileEntity 与辅助工具 (7 文件)

> 原 `TileEntityAdvanceCraftingLink` / `TileEntityAdvanceStorageLink` 已合并入 `TileEntityAdvanceNetworkLink`（网络统计 + 36 槽存储 + 合成 CPU）。

- `IOwnableTile.java` / `OwnableTileUtil.java` — 方块放置者游戏名（`OwnerName` NBT）
- `MatterBallDecompressorUpgrades.java` — 解压器 AE 升级槽（`Upgrades.SPEED` / `SUPERSPEED`）
- `TileEntityAdvanceDataMonitor.java` — **核心 TE**，数据绑定（`MAX_DATA_BINDINGS = 36`）、NBT 持久化、AE2 网络集成；按目标坐标分组采样并 fan-out，多 binding 复用一次读取；维护不持久化的阈值样本/迟滞状态和 strong OR / weak max 聚合缓存，过期时主动归零；被动 `web_surface` 跳过 tick 采样；常规运行只发 revision delta，完整同步留给装载/GUI 保存/结构变化；首次打开可补录所属者
- `TileEntityAdvanceNetworkLink.java` — 统一 AE2 链接器（全网存储统计 + 36 槽 `ItemAdvanceStorageLinkCell` + 合成 CPU 监控）
- `TileEntityGrappleAnchor.java` — 挂索节点（贴面放置、悬挂锚点计算）
- `TileEntityMatterBallDecompressor.java` — 物质球解压器（AE 网络、9 输入 + 81 缓存、4 加速卡槽）

### monitor/ — 监视器共享契约、阈值运行态与数据预算 (4 文件)
- `MonitorWidgetSpec.java` — Java NBT 侧 `MonitorWidgetSpec` 规范化：七类共享 kind、六类 `sourceKind`、`dataType`/legacy alias 迁移、disabled `threshold` 旧存档补齐、`columns` 缺失与空列表语义、preview 判别类型
- `MonitorDownsampleUtil.java` — 客户端渲染与 WebAE preview 共用点数预算 `min(history, visibleWidth, 240)`，并提供 difference 序列变换
- `MonitorThresholdEvaluator.java` — 纯逻辑 `gte` / `lte` 迟滞判定、0–15 线性映射与二值退化
- `MonitorThresholdRuntime.java` — 每台监视器的不持久化最近样本、active 状态、新鲜度和 strong/weak 聚合缓存

### gui/ — GUI 组件和界面 (93 文件)
- **gui/根** — `GuiResponsiveLayout.java`（固定首选尺寸面板在 scaled viewport 内的纯 Java 居中适配）、`MatterBallDecompressorGuiLayout.java`（解压器 GUI 布局）、`UiFrameworkDebugLayout.java`（UI 框架调试 GUI 布局）
- **gui/framework/** — UI 框架（命令式 + Flex 树）：根包 `GuiBlitUtil` / `AtlasRegion` / `SparseFrameRegion` / `FixedAspectButtonFamily` / `UnderlineFieldRegion` / `UiFeedbackArea` / `NineSliceRegion` / `TiledFrameRegion` / `TiledBarRegion` / `UiViewportTransform` / `UiTheme` / `AdmUiTheme` / `PocketUiTheme` / `UiThemes` / `UiPanel` / `UiText` / `UiIcon` / `UiButton` / `UiToggleButton` / `UiSlot` / `UiTextField` / `UiLayoutContext` / `UiTooltip`；ADM 主路径以四角-only 稀疏框和双层 cover 背景组成面板，旧四边 UV 区域透明，使用向请求矩形内拟合的七比例完整按钮、四态线式输入框、精确槽位/滚动/开关/复选控件和 normal/hover 16×16 图标网格；旧九宫格与平铺类仅供兼容主题。`layout/` Flex 引擎；`style/` `UiStyle`；`widget/` `UiWidget`/`UiFlex`/`UiLabel`/`UiScrollPanel`/`UiButtonWidget`；`host/` `AdmUiScreen`；图集 `textures/gui/adm_ui_atlas.png`；`AdmUiAtlasContractTest` / `SparseRegionContractTest` / `UiViewportTransformTest` 锁定图集、稀疏绘制与缩放合同，`GuiThemeCoverageTest` 动态约束全部非口袋 GUI；详见 `docs/zh/developer/ui-framework.md`
- **gui/custom/** — 自定义 GUI 基础组件（7 个）
  - `ADM_GuiScreen.java` — 自定义 GuiScreen 基类（final 统一绘制/输入坐标变换、透明外部、逻辑视口 tooltip；旧主背景/子面板/输入框常量自动桥接 ADM 主题）
  - `ADM_GuiButton.java` — 自定义按钮（旧按钮纹理自动桥接 ADM 七比例完整 normal/hover/pressed/disabled 按钮壳；链式设置、RGB 效果）
  - `ADM_GuiTextField.java` — 自定义文本框（旧输入框纹理自动桥接 ADM normal/focused/invalid/disabled 线式输入框；hint 提示和文本变化清除 invalid）
  - `ADM_UiContainer.java` — 容器 GUI 基类（`UiTheme` + `drawMainPanel` + 可选 `setUiRoot` Flex 树）
  - `AdmGuiTextures.java` — 共享 ADM GUI 纹理 `ResourceLocation` 常量
  - `AbstractMonitorSubGui.java` — 数据监视器 per-binding 子界面基类
  - `AdmItemConfigScreen.java` — 小型配置对话框基类（物品/节点 NBT 设置）
- **gui/guiscreen/** — 具体 GUI 界面（30 个），详见 `gui-guidelines.mdc`
  - `GuiManual.java` — 模组手册 GUI（左侧搜索框 + 可滚动章节导航 + 右侧内容区 + 翻页；搜索高亮与拼音匹配）
  - `GuiNbtViewer.java` — NBT 树查看器（映录器用；折叠节点与截断值提供操作/完整内容 tooltip）
  - `GuiAdvanceLinkScanner.java` — 链接扫描器（主题 sectionPanel + 主题滚动条；列表/筛选/备注/传送）
  - `GuiSubLinkDisplayTypeSelect.java` — 统一网络链接器绑定显示类型选择（网络图表 / 存储显示 / 合成监控）
  - `GuiSubWebDashboard.java` — 监视器 WebAE 仪表盘快照配置（剪贴板导入/复制、变换、透明度、光照、256/512/1024px 质量）
  - `GuiDimensionalPocketConfig.java` — 次元口袋配置 GUI（升级插槽：空间/翻页/堆叠/无尽堆叠 + 开关 + 容量统计 + 堆叠倍数显示）
  - `GuiPocketStorage.java` — 次元口袋存储 GUI（原生 Container，口袋格 + 玩家背包 + 翻页/升级配置按钮；右键口袋主入口）
  - `GuiSuperOrangeConfig.java` — 超能砂糖桔设置（铭牌、物质球开关、拾取合并、丢弃合并、倍率）
  - `GuiMatterBallDecompressor.java` — 物质球解压器（`ADM_UiContainer` + `UiPanel`/`UiText`/`UiIcon`/`UiButton`；缓存格经 renderer 使用 `UiSlot.drawTheme`）
  - `GuiUiFrameworkDebug.java` — UI 框架组件展示（稀疏框、完整按钮、四态输入线、主题槽位及完整 atlas UV 对照；调试方块右键打开）
  - `GuiGrappleSavePrompt.java` — 挂索路径保存命名弹窗
  - `GuiPlannerHudConfig.java` — 计划器 HUD 配置（显示项目/位置/透明度）
  - `GuiPlannerMergeConfirm.java` — 计划器合并确认弹窗
  - `GuiScreenMessage.java` — 通用消息提示弹窗（正文自动换行、面板随内容增高；按钮 ID/返回行为保持不变）
  - `GuiScreenshotGallery.java` — 主题主面板 + sectionPanel 内的客户端本地截图历史预览（较新/较旧、发送 WebAE；动态纹理关闭时释放）
- **gui/manual/** — 手册数据结构（6 个）：`ManualChapter.java` / `ManualPage.java` / `ManualDataLoader.java` / `ManualSearchUtil.java` / `ManualSearchIndex.java` / `ManualTextHighlighter.java`
- **gui/container/** — `ContainerAdvanceStorageLink.java`、`ContainerDimensionalPocket.java`（次元口袋配置容器，4 槽位：空间/翻页/堆叠/无尽堆叠）、`ContainerPocketStorage.java`（次元口袋存储容器，原生槽位 + 翻页）、`ContainerMatterBallDecompressor.java`（物质球解压器容器，9 输入 + 9×9 缓存 + 4 加速卡槽）、`ContainerUiFrameworkDebug.java`（UI 框架展示，无槽位）
- **gui/handler/** — `GuiHandler.java`（IGuiHandler；GUI ID 0–10，含 `UI_FRAMEWORK_DEBUG_GUI_ID=10`）

### handler/ — 事件处理器与物品服务端逻辑 (40 文件)
- `HandlerTick.java` — 服务端/玩家 Tick 事件（任务队列每 tick 最多执行 64 个且不预取丢弃下一项、计划提醒、SuperOrange 无人机管理；世界地图分 tick；`qqBot` 独立 perf 阶段每秒复制只读服务器快照并调度定时播报）
- `ConnectorTickService.java` — AE 连接器 debounced 刷新调度（网络/合成/存储链接器）+ `[debug] connectorProfile` 统计
- `NetworkLinkGridStatsCache.java` — 同 AE grid 多网络链接器共享存储统计缓存
- `StorageLinkWatchRegistry.java` — 存储链接器被监视器绑定时的引用计数注册表（无监视器时跳过周期扫描）
- `StorageLinkWatchSync.java` — 数据监视器绑定变更时同步 `StorageLinkWatchRegistry`
- `ItemCountSyncCoordinator.java` — StorageLink GUI 数量同步：主线程队列 + 200ms 去抖
- `HandlerLoot.java` — 战利品事件（SuperOrange 地牢宝箱 0.1%、生物击杀掉落 0.05%）
- `HandlerPlayerJoin.java` — 玩家首次加入事件（赠送手册物品 + NBT标记）
- `HandlerSuperOrange.java` — 超能砂糖桔能力（投掷物免疫 + 挖掘掉落 + 拾取/丢弃合并物质团簇）
- `HandlerGrapple.java` — 挂索节点索引/挂接 tick/区块加载 TE map 扫描（非 65k getBlock）
- `HandlerDataLoomCell.java` — 编织元件事件
- `HandlerStarryCosmosSword.java` — 星空剑事件
- `HandlerPocketWorldLoad.java` — 世界加载时设置次元口袋存档目录（`PocketStore.setWorldDirectory`）
- **挂索服务端逻辑**（原 handler/）：`GrappleNodeIndex.java` / `GrapplePlayerState.java` / `GrappleAnchorPositions.java` / `GrappleQueuedHop.java` / `GrapplePathStore.java` / `GrappleRouteSync.java` / `GrappleRouteMatcher.java` / `GrapplePlanningSession.java`
- `StarrySwordSpawnScheduler.java` — 星空剑技能实体生成调度
- **星空剑技能逻辑**（原 handler/）：`StarryCosmosSwordUtil.java` / `StarryCosmosSwordConstants.java` / `StarryEntityMotionUtil.java` / `StarryPlayerLookup.java` / `ThrownImpactPoint.java` / `StarryCosmosSounds.java` / `DamageSourceStarryCosmos.java`
- `LinkScannerService.java` — 链接扫描器已加载区块扫描、NBT 合并、安全传送
- `WebAeServerHandler.java` — WebAE HTTP 服务器生命周期管理（FMLServerStartedEvent/FMLServerStoppedEvent）
- `HandlerWebPlayerTracker.java` — WebAE 玩家登录/登出追踪（`@SubscribeEvent` 监听 `PlayerLoggedInEvent`/`PlayerLoggedOutEvent`，写入 `PlayerInfoStore`；登录时 `NetworkRegistry.onPlayerLogin`；每 tick 调用 `reconcileOnline` + `tickSave` + `NetworkRegistry.tickHealthCheck`）
- `HandlerWebTileEntityIndex.java` — WebAE chunk load/unload 同步 `TileEntityIndex` 与 `NetworkRegistry`
- `HandlerWebChatCollector.java` — WebAE 游戏内聊天收集（`@SubscribeEvent` 监听 `ServerChatEvent`，不取消事件，镜像写入 `ChatMessageStore` 作为 `source=game`）
- **次元口袋服务端逻辑**：`PocketState.java`（按玩家绑定的口袋状态）/ `PocketStore.java`（per-player NBT 压缩存储单例，`<世界存档>/textech/pocket-<uuid>.dat`）/ `PocketInventory.java`（`IInventory` 实现，动态堆叠上限）/ `PocketSlotInteraction.java`（存储 GUI 与悬浮窗共享的槽位点击逻辑）/ `PocketUpgradeRules.java`（升级拆装校验，含堆叠/无尽堆叠规则）

### entity/ — 实体类 only (8 文件，不含 Render)
- `EntitySuperOrangeDrone.java` — 超能砂糖桔无人机
- `EntityGrappleSlide.java` — 挂索滑移实体
- `EntityStarrySwordLineSlash.java` / `EntityStarrySwordLineStab.java` / `EntityStarrySwordThrown.java` / `EntityStarrySwordRain.java` / `EntityStarrySwordRainField.java` / `EntityStarrySwordSlam.java` — 星空剑技能实体

### renders/ — 全部客户端渲染 (43 文件)
- **约定**：实体 Render、TESR、物品 Render、HUD、手册页面渲染均在此包；`entity/` 不放 Render 类
- `AdvanceBlockCubeRenderUtil.java` — 占位方块立方体/挂索节点薄板几何（模型未完成前）
- `StatCardRenderer.java` / `ProgressBarRenderer.java` / `GaugeRenderer.java` / `LineChartRenderer.java` / `BarChartRenderer.java` / `PieChartRenderer.java` / `DataTableRenderer.java` — 七类共享监视器组件；折线族共用 timeseries 支撑，progress/gauge 无真实 max 时只接受显式 `targetValue > 0`
- `MonitorRenderCache.java` / `MonitorRenderSupport.java` — 按 monitorPos + bindingIndex + revision 缓存 VBO/FloatBuffer/网格轴与共享读取、下采样逻辑；仅数据或配置变化时失效重建
- `CraftingInfoRenderer.java` / `StorageInfoRenderer.java` / `WebSurfaceRenderer.java` — 专用监视器与网页纹理表面；`renderType` 优先、缺失回退 `dataType`；`WebSurfaceRenderer` 支持 `ResourceLocation` 与 MCEF 原始 GL texId（可 V 翻转）
- `PlannerHudRenderer.java` / `GrappleHudRenderer.java` / `GrappleTravelLineRenderer.java` / `WebAlertHudRenderer.java` — HUD
- `ManualPageRenderer.java` — 手册页面渲染（text/item_showcase/config_ref）
- `OrangeNameplateRenderer.java` / `SuperOrangeHaloRenderer.java` / `SuperOrangeHeadRenderUtil.java` — 超能砂糖桔
- `RenderSuperOrangeDrone.java` — 无人机实体渲染
- `RenderStarrySword*.java`（4 个）/ `CosmicStarrySwordRenderer.java` / `StarryCosmosWorldRenderer.java` / `StarryCosmicRenderUtil.java` — 星空剑
- `RenderAdvanceDataMonitor.java` / `RenderGrappleAnchor.java` / `RenderGrappleSlide.java` — 方块/实体 TESR
- `RenderAdvanceDataMonitorBlockItem.java` / `RenderAdvanceNetworkLinkBlockItem.java` / `RenderDataImprintItem.java` / `RenderItem.java` — 物品渲染
- `RenderMatterBallDecompressor.java` — 物质球解压器 TESR
- `MatterBallDecompressorGuiRenderer.java` — 解压器 GUI 背景/分隔线/缓存格渲染（ADM 主题面板 + divider + `UiSlot.drawTheme`；AE2 模式按钮/升级列保持原生）
- `RenderController.java` — 监视器内容渲染器注册（七类共享 kind + crafting / storage / web_surface；legacy alias 由契约规范化）
- `HudRenderUtil.java` / `IADMRender.java` / `RenderAdvanceNetworkLink.java` / `AdvanceBlockCubeRenderUtil.java`

### network/ — 网络通信 (30 文件)
- **network/packet/** — 网络包实体（24 个）
  - `PacketSynTileEntity.java` — TE 初次装载、GUI 保存与 chunk/load 恢复的完整同步；不再承担监视器每次采样广播
  - `PacketAssistantAction.java` / `PacketAssistantResponse.java` — AI 助手操作/响应
  - `PacketAssistantMenuStateQuery.java` / `PacketAssistantMenuStateResponse.java` — AI 助手菜单状态查询/响应
  - `PacketItemCountSync.java` / `PacketRequestItemCountSync.java` — 物品数量同步
  - `PacketItemNBT.java` — 物品 NBT 同步
  - `PacketGrappleAction.java` / `PacketGrappleSync.java` — 挂索挂接/移动同步
  - `PacketGrapplePathAction.java` / `PacketGrapplePathSync.java` — 挂索路径保存/下发
  - `PacketGrappleHookConfig.java` / `PacketGrappleAnchorConfig.java` — 挂索器/节点配置
  - `PacketMonitorRecord.java` — 快捷键打开附近监视器 AI 时登记监视器坐标
  - `PacketMonitorBindingDelta.java` — packet ID 54 S→C：binding index + revision + 小字段 patch + 追加历史点；客户端按 revision 应用并失效对应渲染缓存
  - `PacketLinkScannerAction.java` — 链接扫描器扫描/同步/传送
  - `PacketPocketAction.java` — 次元口袋 C→S 动作（开关/翻页/拖动位置/升级增减/堆叠升级/存取物品/打开配置GUI）
  - `PacketPocketSync.java` — 次元口袋 S→C 同步（全量/单页 `ItemStack[][]`）
  - `PacketSuperOrangeConfig.java` — 超能砂糖桔设置 GUI 保存
  - `PacketMatterBallDecompressorToggle.java` — 物质球解压器输出模式/阻塞模式切换
  - `PacketMonitorWebSurface.java` — packet ID 53 同 ID 双向：网页表面正文按哈希请求、单绑定 24 KiB 内上传与 ACK；服务端距离/所属者/数量/总字节校验
  - `PacketWebRecipeUpload.java` / `PacketWebRecipeUploadAck.java` — WebAE 配方上传/确认（阶段 1，ID 26/27）
  - `PacketWebIconUpload.java` / `PacketWebIconUploadAck.java` — WebAE 图标上传/确认（阶段 3.1，ID 28/29，复用分片模式，itemId→base64PNG）
- **network/handler/** — 包处理器（6 个）
  - `PacketHandlers.java` — 服务端线程调度工具（`runOnServer` / `runOnServerThread`）
  - `HandlerNetwork.java` / `HandlerSynTileEntity.java` / `HandlerLinkScannerAction.java` / `HandlerPlannerSync.java` / `HandlerPlannerMerge.java`

### voice/ — 语音识别 (9 文件)
- `VoiceCaptureService.java` — 语音采集入口，管理热键和麦克风
- `SpeechToTextClient.java` — STT 接口
- `VoskSpeechToTextClient.java` — Vosk 离线语音识别（嵌入式模型）
- `HttpSpeechToTextClient.java` — HTTP STT（OpenAI 兼容 API）
- `EmbeddedVoiceModelManager.java` — Vosk 模型：classpath / `textechvoice` companion jar / 本地目录；发布主 jar 不含模型
- `VoiceStatusListener.java` — 语音状态回调接口
- `StandaloneVoskTranscriber.java` — Vosk 独立转录器
- `PcmAudioUtil.java` / `WavEncoder.java` — 音频格式处理

### voiceextras/ — 可选语音资源包标记 (1 文件)
- `VoiceExtrasMod.java` — `@Mod(modid=textechvoice)`；主 jar 打包时排除，仅进入 `*-voice.jar`（另含 `src/voiceExtras/resources/mcmod.info` + Vosk 模型）

### loader/ — 注册加载器 (11 文件)
- `LoaderBlock.java` / `LoaderItem.java` / `LoaderTileEntity.java` / `LoaderEntity.java` / `LoaderHandler.java` / `LoaderGui.java` / `LoaderRender.java` / `LoaderNetwork.java` / `LoaderRecipe.java` / `LoaderCommand.java`（服务端/客户端聊天命令：`/textech` 域分发 + 旧 `/adm*` 别名；Proxy 委托）
- `ModMigrationHandler.java` — 存档迁移兼容处理（旧 modid `advancedatamonitor` → `textech`）

### client/ — 客户端事件与挂索/次元口袋/截图/网页表面逻辑 (44 文件)
- `VoiceAssistantKeyHandler.java` — V 键语音助手热键处理
- `KeyBindings.java` — O/P/H/F10 快捷键（AI聊天/计划器/HUD/窗口截图）；F10 用 ClientTick 边沿，任意 GuiScreen 内仍可截图；其余含 `uploadNeiRecipes()`、图标 upload/local/pull
- `client/screenshot/ClientScreenshotService.java` — 只读 Minecraft Framebuffer；本地 `TeXTech/Screenshots/` JPEG 历史、后台编码/双上限清理、24 KiB/tick 受控上传队列
- `WebSurfaceClientCache.java` — 网页表面压缩正文缓存、按哈希请求、单 daemon 后台 AWT 栅格化、渲染线程 DynamicTexture 上传与 8 项/约 6M 像素 LRU
- `websurface/` — `WebSurfaceFrame`（`ResourceLocation` 或原始 GL texId）；`WebSurfaceSource` 路由 MCEF 反射 OSR（池上限 2）→ CDP 占位 → HttpFrame → Snapshot；世界卸载关闭 browser 池
- `HandlerGrappleClient.java` — 挂索 Shift 取消 / 右键移动（客户端输入）
- `GrappleClientCache.java` — 挂索客户端状态缓存（接收 `PacketGrappleSync`）
- `GrappleSelectionUtil.java` — 挂索节点磁吸选点、路径预览坐标
- `GrappleClientNodeIndex.java` — 客户端挂索节点 chunk 索引（区块加载时维护）
- `GrappleClientRouteCache.java` — 挂索路径客户端缓存（接收 `PacketGrapplePathSync`）
- `GrappleRoutePickerHud.java` — 挂索路径选择 HUD
- `StarryCosmosClientWarmup.java` — 星空剑客户端资源预热
- `ItemClientGui.java` — 物品打开 GUI 的客户端辅助（避免 items/ 导入 Minecraft；含 `openPocketConfigGui`）
- `AiClientPreferences.java` — 客户端本地 AI/语音密钥与偏好（`config/textech/ai-client-local.cfg`，不上传服务端）
- `AssistantDataFiles.java` — 运行时数据文件路径路由（`TeXTech/WebAE/`、`TeXTech/Assistant/`、`TeXTech/Grapple/`；启动时从 `config/textech/` 等旧路径迁移）
- `PocketClientCache.java` — 次元口袋客户端镜像缓存（接收 `PacketPocketSync`，按玩家绑定；含堆叠升级字段）
- `PocketOverlayHandler.java` — 次元口袋客户端事件中枢（覆层渲染 + 鼠标路由；右键打开的 `GuiPocketStorage` / `GuiDimensionalPocketConfig` 上不显示浮窗）
- `PocketPortalGuiRenderer.java` — 传送门 9-slice 面板渲染（`textures/gui/pocket_portal_panel.png`，随升级格数缩放）
- `GuiPocketOverlay.java` — 次元口袋悬浮窗（传送门边框 + 原版 inventory 槽位；拖动/翻页/收起）
- `PocketStackSizeFormat.java` — 口袋槽位数量角标格式化（1K / 1M / 1G，对齐 SNL 便携无尽箱）
- `PocketStackOverlayRenderer.java` — 悬浮窗物品 + 角标渲染
- `DataImprintScrollHandler.java` — 数据映录器滚轮滚动事件处理
- `WebConsoleClientChat.java` — WebAE `/admweb issue`/`guest` 客户端可点击 token/URL 聊天（S→C `PacketWebConsoleTokenNotify`）
- `WebConsoleClipboard.java` — WebAE token 剪贴板复制（AWT SystemClipboard）
- `worldmap/WorldMapTileRenderWorker.java` — **Legacy** 消费 S→C `PacketWebMapTileJob`（client_only 模式下队列 no-op）
- `worldmap/WorldMapChunkCaptureHandler.java` — **Legacy** 探索预热（client_only 下禁用）
- `worldmap/WorldMapCaptureClientState.java` — 客户端最新 consent requestId 缓存（`/admweb wm y` 省略 id）
- `worldmap/WorldMapCaptureJobAssembler.java` — `PacketWorldMapCaptureJob` 客户端分页重组（最多 8 个 pending、2 分钟 TTL；metadata/offset/总数完整后原子发布）
- `worldmap/WorldMapSnapshotCaptureWorker.java` — **客户端快照** JM/GL 双源 terrain+ae 采集、本地 map-cache、C→S 上传
- `worldmap/WorldMapSnapshotLocalCache.java` — MC 客户端 `TeXTech/WebAE/map-cache/` 读写
- `worldmap/WorldMapSnapshotDownloadHandler.java` — 登录后 packet 同步服务端快照到本地
- `worldmap/journeymap/WorldMapJourneyMapTileReader.java` — 读取 JM 本地最高清 day 瓦片（无硬依赖）
- `worldmap/WorldMapChunkGlRenderer.java` — **Phase 4 + AE overlay** 单 chunk FBO 离屏 GL 渲染（`renderTerrain` + `renderAeOverlay`；复用 `IconRenderGuard` 复位）
- `worldmap/WorldMapOrthoCamera.java` — **Phase 4** 正交/look-at 相机预设（flat + mineshot 风格 oblique 四向 orbit）

### command/ — 命令 (7 文件)
- `TeXTechCommandBase.java` — 命令共享基类（i18n 发送、help 行渲染、OP/玩家校验、tab 过滤、`sliceArgs`）
- `CommandTeXTech.java` — `/textech` 服务端分发（`web`/`assistant`/`help`；别名 `/adm`、`/txt`）
- `CommandTeXTechClient.java` — 客户端分发（`ai`/`assistant`；`web` 提示服务端）
- `CommandAssistant.java` — `/admassistant` 与 `/textech assistant` 词库热重载（`reload` / `reload-lexicon`）
- `CommandAIConfig.java` — `/admai` 与 `/textech ai` AI 本地配置（仅客户端；kebab 别名 clear-key/search-key/search-base）
- `CommandWebConsole.java` — `/admweb` 与 `/textech web` WebAE 令牌/配方/图标/世界地图/HTTP/QQ；`qq send` 仅 OP、只入有界机器人队列
- `CommandScreenshot.java` — 客户端 `/admscreenshot` / `/textech screenshot`：capture/list/preview/send web/send qq/status；QQ 发图由服务端再次校验 OP

### utils/ — 工具类 (18 文件)
- `NBTDataHelper.java` / `NBTJsonParser.java` / `NBTJsonParserHelper.java` — NBT 与 JSON 互转
- `CraftingTemplateParser.java` — 合成模板解析
- `ContentsHelper.java` — 内容验证工具（坐标、数字、颜色格式等）
- `DataBound.java` — 监视器数据绑定 DTO
- `TileEntityTypeHelper.java` — TileEntity 类型识别
- `NetworkValidationUtil.java` — 网络包距离/权限校验
- `NetworkPacketCodec.java` — 网络包不可信字段编解码边界：byte/string/VarString 长度、严格 UTF-8、Forge 1.7.10 signed-short NBT framing 与有限 `NBTSizeTracker`
- `ModLogFiles.java` — 模组日志文件路径（`logs/textech/`）
- `BlockPos.java` — 自定义 BlockPos（1.7.10 无原版 BlockPos）
- `MatterBallClusterUtil.java` — Avaritia 物质团簇插入/提取
- `MatterBallDecompressorCapacityUtil.java` — 物质球解压器容量计算
- `MatterBallDecompressorSpeedUtil.java` — 物质球解压器速度计算
- `AeUpgradeSpeedUtil.java` — AE 加速卡倍率（分子装配室档位 + 超级加速）
- `AeSecurityCheck.java` — AE 安全终端权限检查
- `WebDashboardSnapshotCodec.java` — `textech-webae-display-snapshot` v1 校验、GZIP（原始 96 KiB / 压缩 24 KiB）、SHA-256 与有界绘制元素 DTO
- `WebDisplayBindingCodec.java` — `textech-webae-display-binding` v1（dashboard_live / live_url）校验与绑定哈希

### mixin/ — Mixin 注入 (7 文件，仅客户端)
- `MixinGuiScreen.java` / `MixinGuiContainer.java` — GUI 输入/容器钩子
- `MixinPocketDrawSlotOverlay.java` — 口袋存储 GUI 槽位角标 K/M/G 格式化（对齐 SNL 便携无尽箱）
- `GuiScreenTooltipAccess.java` — Invoker：`GuiScreen.renderToolTip` / `drawHoveringText`（悬浮窗槽位 tooltip 与容器槽位同路径）
- `MixinNeiRenderToolTips.java` / `MixinNeiLayoutManager.java` / `MixinNeiItemZoom.java` — NEI tooltip/布局兼容
- 配置：`src/main/resources/mixins.textech.json`

### manual/ — （已迁入 gui/manual/）
- 页面渲染见 `renders/ManualPageRenderer.java`；GUI 入口 `gui/guiscreen/GuiManual.java`

### webae/ — WebAE 嵌入式 HTTP 控制台 (422 文件，含 access/ACL、服务端后台命令、QQ 群机器人、截图附件、样板网页缓存)
- `webae/WebConsoleServer.java` — NanoHTTPD 服务生命周期 + 有界 4–32 worker/128 queue + active socket 回收；bind 成功后才置 started，stop/restart 回收 executor、Webhook、QQ bot 与 shutdown hook；静态资源优先实例 `TeXTech/WebAE/ui/`，源码开发回退 classpath `assets/textech/webae/`（发布主 JAR 排除 UI，独立 WebAE ZIP 提供），外部路径含穿越防护
- `webae/auth/WebAuthSession.java` — 认证会话 DTO（`ownerUuid` 网络归属 + `actorUuid`/`actorName` 聊天身份 + `type` owner/guest + 可选 `allowedNetworkKeys`）
- `webae/auth/WebAuthToken.java` — Token 生成/校验/持久化（主人 Token 每 owner 一条；访客 Token 按 owner+guest；可选 `allowedNetworkKeys`；禁用玩家校验；`revokeAllForPlayer`；含可选 TTL：`webTokenLifetimeHours`）
- `webae/auth/WebAuthMiddleware.java` — HTTP 请求认证中间件（返回 `WebAuthSession`；401 响应带 `code`：过期/无效/`webae_disabled`）
- `webae/access/WebAeNetworkKeys.java` — 稳定网络键 `{dim}:{x}:{y}:{z}` ↔ runtime `networkId` 映射
- `webae/access/WebAeNetworkSuspendStore.java` — 网络级 WebAE 停用（`web-network-suspends.json`，含网主）
- `webae/access/WebAeNetworkAclStore.java` — actor 级 deny 覆盖（`web-network-acl.json`）
- `webae/access/WebAeNetworkAccess.java` — 统一可见网/访问校验（suspend → guest allowlist → ACL deny）
- `webae/auth/WebAuthOpCheck.java` — OP 等级校验工具（管理员刷新端点用，按 playerUuid 查在线玩家并校验 OP>=2）
- `webae/auth/WebAuthAdminCheck.java` — 统一管理员校验：grant 有效且 actorUuid 匹配 或 在线 OP≥2
- `webae/auth/WebAdminBootstrapStore.java` — 一次性 bootstrap 码存储（`wae-adm-` 前缀，支持 --reuse）
- `webae/auth/WebAdminGrantStore.java` — 设备 adminToken 长期授权存储（绑定 ownerUuid/actorUuid）
- `webae/auth/WebLoginCodeStore.java` — 6 位登录码生成/兑换（5 分钟 TTL、单次使用；`/admweb login` + `POST /api/auth/exchange`）
- `webae/oc/OcSummaryRateLimiter.java` — OC summary API 每 owner 1 req/s 限流
- `webae/craft/WebAeCraftService.java` — 按 ownerUuid+networkId 从 `NetworkGroup.craftingLink` 查候选/下单（`submitCraftTracked` 支持 jobId + `CraftSubmitHooks` 绑定 `ICraftingLink`）
- `webae/cpu/` — `CpuHistoryService`/`CpuHistoryStore` 保存 owner/network 隔离的有界 CPU 作业生命周期与 `StorageDto.cpus` 快照；`CpuCapacityPlanner` 从有效区间计算并发、分位耗时、繁忙率、存储压力和只读 CPU 估算；持久化路径为 `TeXTech/WebAE/cpu-history/`，稳定网络键处理 runtime networkId 重排
- `webae/craft/CraftSubmitResult.java` — WebAE 下单结构化结果（calculating / failed）
- `webae/order/WebAeOrderProgressService.java` — 订单进度：`ICraftingLink.isDone/isCanceled` + CPU `startItemCount/remainingItemCount`，按 networkId + 短 TTL 缓存
- `webae/order/OrderSubmitService.java` — Phase 3.3 告警自动下单
- `webae/order/WebOrderTemplate.java` / `WebOrderTemplateItem.java` / `WebOrderTemplatesStore.java` / `WebOrderTemplatesValidator.java` — 批量下单模板
- `webae/api/WebApiRouter.java` — REST 路由（认证/OC/存储/电力/配方/样板/GT/下单/配置/图标/聊天/玩家/集成/搜索/材料树/SSE/P2P/拓扑/网络健康与 CPU 历史/世界地图 meta·markers·progress·tiles·invalidate·versions·diff·annotations/Spark/AI/服务端后台命令/QQ 群机器人）；annotation POST/PUT 独立使用 16 KiB 有界 body reader，body `networkId` 与可选 query `network` 必须一致
- `webae/api/handler/AdminConsoleHandler.java` — 管理员服务端后台 API：轻量玩家快照、命令执行、共享 preset、审计摘要/详情；动态路由规范化后进入性能诊断
- `webae/console/AdminCommandService.java` / `AdminConsoleStore.java` — 单并发主线程命令桥接、高风险确认/冷却/队列门禁、有界输出与 `admin-console.json` 后台合并持久化
- `webae/api/handler/WebAiAdminHandler.java` — 管理员 AI Provider/API Key 配置、删除与连通性测试；响应不返回明文或密文
- `webae/api/handler/QqBotAdminHandler.java` — 管理员 QQ 机器人 settings/status/audit/restart/send/clear API；ClientSecret 不回显，外部工作只入队或异步重连
- `webae/assistant/WebAiConfigStore.java` / `WebAiHttpClient.java` / `WebAiCompletionService.java` — 共享有序多 profile（AES-GCM）+ 故障切换 + 共享联网搜索；`aiServerKeyEnabled`/`aiBrowserKeyEnabled` 独立开关；浏览器直连见 `webae-frontend/src/utils/personalAi.ts`
- `webae/assistant/WebAssistantService.java` / `WebAssistantPendingStore.java` — 与游戏内共用 intent schema 的 Web 助手、规则回退、服务端工具执行与 actor/owner 绑定短期确认令牌
- `webae/qqbot/QqBotConfig.java` / `QqBotConfigValidator.java` / `QqBotConfigStore.java` — 默认关闭的 QQ 机器人设置、限幅/HTTPS 校验与 ClientSecret AES-GCM 持久化（`qq-bot.json` + `qq-bot-master.key`）
- `webae/qqbot/QqBotGatewayClient.java` / `QqBotMessage.java` — QQ 官方 Gateway 心跳/自动重连底层、group/C2C/channel 入站标准化与文本发送；group/C2C JPEG 经 `/files` + `msg_type=7` 富媒体发送
- `webae/qqbot/QqBotSnapshot.java` / `QqBotCommandRouter.java` / `QqBotService.java` / `QqBotIntentClassifier.java` — 主线程只读 TPS/人数/内存快照、本地命令、可选 AstrBot 意图互斥、共享 AI 多轮会话、白名单/去重/双冷却、有界队列、定时播报与内存审计
- `webae/api/handler/AuthExchangeHandler.java` — `POST /api/auth/exchange` 登录码 → owner Token
- `webae/api/handler/AuthAdminElevateHandler.java` — `POST /api/auth/admin/elevate` 一次性码兑换长期 adminToken；`GET /me` 当前管理状态与能力位；`GET /grants` 列出本 owner 授权设备；`POST /revoke-self` 撤销本设备提权
- `webae/api/handler/OcSummaryHandler.java` — `GET /api/oc/summary` OC Internet Card 只读轮询（1 req/s）
- `webae/api/handler/StorageHandler.java` — AE 存储快照 REST handler（只读缓存 + 管理员刷新 + 批量）
- `webae/api/handler/StoragePagedHandler.java` — 存储 cursor 分页（`GET /api/storage/items|fluids|essentia`；内存切片 + snapshotVersion cursor）
- `webae/api/handler/PowerHandler.java` — 无线电力/蒸汽 REST handler（只读缓存 + 管理员刷新 + 批量）
- `webae/api/handler/RecipeHandler.java` — 配方 REST（sync manifest/chunk + browse/search/suggest/status/handlers/单条详情；兜底经 `ensureLoaded`）
- `webae/api/handler/PatternHandler.java` — 样板编码/注入/接口枚举 REST handler
- `webae/api/handler/PatternListHandler.java` — 样板总览/编辑/物理移动 REST handler（接口与部件 id、管理员 move/swap、网页缓存 take/place；写操作校验 owner network、主线程执行并回滚，触发 `MENetworkCraftingPatternChange`）
- `webae/api/handler/PatternBrowseHandler.java` — 样板 browse REST handler（`GET /api/patterns/browse` HTTP 只读缓存 + `cached`/`timestamp`；`POST /api/patterns/browse/refresh` OP 强制重建；后台 `SnapshotScheduler` 按 `patternCacheTtlMs` 预采集；owner 离线经 `getOwnerPlayerOrFake`；配置项 `patternBrowsePageSize`/`patternBrowseMaxTotal`/`patternCacheTtlMs`）
- `webae/api/handler/PatternGridDetailHandler.java` — Grid 单条目详情 REST handler（`GET /api/patterns/grid/<gridKey>?network=`；`gridKey` 为 `grid:<index>`，与 browse 索引一致；返回完整 inputs/outputs）
- `webae/api/handler/NetworkMetricHandler.java` — AE 网络指标历史 REST handler（`GET /api/network/metrics?network=`）
- `webae/api/handler/NetworkMetricFluidHandler.java` — 钉选流体趋势 REST handler（`GET /api/network/metrics/fluids`；上限 cfg）
- `webae/api/handler/NetworkMetricItemHandler.java` — 钉选物品存量趋势（`GET /api/network/metrics/items`）
- `webae/api/handler/NetworkMetricEntityHandler.java` — 钉选 CPU/GT 数值趋势（`GET /api/network/metrics/entities`）
- `webae/api/handler/NetworkBalanceHandler.java` — 多网存储平衡 REST handler（`GET /api/network/balance`；Phase 8 只读建议）
- `webae/api/handler/TopologyHandler.java` — 网络拓扑 REST（`GET /api/network/topology` 读上次手动快照；`POST /api/network/topology/snapshot` 主线程采集 + 冷却；`topologyEnabled` 关闭时 503）
- `webae/api/handler/OrderHandler.java` — AE 合成订单 REST（`POST /api/order`/`batch`；下单绑定 `ICraftingLink`；`GET /api/order/list` 经 `WebAeOrderProgressService` 按 networkId 读步骤进度；history 按 owner 隔离；`GET /api/order/status` 未知 jobId→404；`POST /api/order/cancel` 取消 Future + `link.cancel()`）
- `webae/api/handler/CpuHistoryHandler.java` / `CpuCapacityHandler.java` — CPU history/capacity 只读 REST（owner/network ACL + stable key ACL；HTTP 仅读内存并标记 sampler active，不扫描 World/TileEntity/AE2 Grid）
- `webae/diagnostics/NetworkHealthDiagnosticDto.java` / `NetworkHealthDiagnosticProvider.java` / `NetworkHealthStatusEvaluator.java` — owner/network 隔离的网络健康证据 DTO、约 5 秒服务端快照 Provider 与固定 issue/status 判定；缺失或过期证据保持 `unknown`，HTTP/助手只读同一快照
- `webae/api/handler/NetworkHealthHandler.java` — `GET /api/network/health?network=` 只读诊断端点（复用 owner/network ACL，不在 HTTP 线程扫描 World/TileEntity/AE2 Grid）
- `webae/api/handler/OrderTemplatesHandler.java` — 批量下单模板 REST handler（`GET/PUT /api/order/templates`；`TeXTech/WebAE/web-order-templates.json` 按 ownerUuid 隔离）
- `webae/api/handler/GtMachineHandler.java` — GT 机器监控 REST handler（只读缓存 + 管理员刷新 + 批量）
- `webae/api/handler/WebConfigHandler.java` — `/api/config` 公开配置（refresh/theme/icon/debugFlags/topology/worldMapEnabled·max/defaultQualityTier/alerts/dynmapBaseUrl）
- `webae/WebUiDefaultsStore.java` — 读取实例/jar `ui-defaults.json` 默认 UI 布局
- `webae/api/handler/WebUiDefaultsHandler.java` — 公开 `GET /api/ui-defaults`（WebConsoleServer 免认证路由）
- `webae/api/handler/IconHandler.java` — 图标 REST handler（`GET /api/icon` 返回 PNG+ETag+Cache-Control、404 直渲桥接、`GET /api/icon/packs` 含 `defaultPack`、`GET /api/icon/sync/manifest` + `GET /api/icon/sync/bulk` 浏览器 bulk 同步、`POST /api/icon/pack` zip 上传含 Zip Slip 防护）
- `webae/api/handler/AeCableTextureHandler.java` — `GET /api/ae2/cable-texture`（**已弃用**；需 `topologySimulatedEnabled`）
- `webae/topology/AeCableTextureLoader.java` — AE2 Fluix 线缆 part classpath 加载（线缆模拟；默认关闭）
- `webae/api/handler/ChatHandler.java` — 聊天 REST handler（history/since/send）+ 鉴权 `GET /api/chat/attachment?id=` 磁盘 JPEG 直读（Router 统一计时）
- `webae/api/handler/PlayerHandler.java` — 玩家 REST handler（`GET /api/players` 返回 `{online:[...],offline:[...]}` 每项含 uuid/name/online/onlineMs/lastLogin/lastLogout/skinUrl、`GET /api/players/since=<ts>` 增量、`GET /api/players/online/history` 在线人数趋势 `[{"ts":,"count":}]`、**Phase6** `GET /api/players/locations` 在线玩家坐标）
- `webae/api/handler/AuthGuestInviteHandler.java` — `POST /api/auth/guest-invite` 主人生成可分享访客 Token（可选 body `networkKeys` 限定网络；owner 禁用拒绝签发）
- `webae/api/handler/AdminPlayerHandler.java` — 管理端玩家：列表/禁用启用/清缓存；`GET .../access`；网络 suspend/resume；guest ACL deny/allow；撤销 guest token
- `webae/api/handler/SearchHandler.java` — 聚合搜索 REST handler（`GET /api/search?q=&limit=&offset=&types=storage,recipe,gt,pattern&network=`；500ms 限流；pattern 类型读 browse 缓存，HTTP 线程无阻塞；Phase 4a）
- `webae/api/handler/QuestHandler.java` — BetterQuesting 任务书 REST（meta/lines/progress/search/detail/analysis/detect/submit/submit-craft/claim/chain-plan/submit-chain/chain-jobs）
- `webae/api/handler/SparkHandler.java` — 可选 Spark REST（`GET /api/spark` 历史/当前状态、`POST /api/spark/profile` 启动、`POST /api/spark/stop` 停止、`POST /api/spark/analyze` 显式 AI 分析、历史详情/删除；控制与 AI 均需管理员）
- `webae/spark/SparkService.java` / `SparkProfile.java` / `SparkProfileStore.java` / `SparkAiAnalysisService.java` — Spark 模组检测、Forge 命令桥接、bounded JSON 历史与可选有界聚合 AI 诊断（不发送原始输出/Viewer URL/API Key）；无 Spark 或配置关闭时不暴露功能
- `webae/quest/QuestDataCollector.java` — 任务只读采集门面
- `webae/quest/QuestChainService.java` — 前置拓扑计划 + 同步链式提交（skipMissing）
- `webae/quest/QuestChainOrchestrator.java` — 异步链式合成后提交 job 轮询
- `webae/quest/QuestRewardClaimService.java` — 纯物品/多选一奖励领取至选定 AE 网络（选择校验、AE 预检、BQ 官方 claim、背包差量入库）
- `webae/quest/QuestSubmitService.java` / `QuestCraftOrchestrator.java` / `QuestRequirementAnalyzer.java` / `QuestTaskDeserializer.java` / `QuestFluidIconResolver.java` / `QuestFluidEquivalence.java` / `QuestCacheStore.java` / `QuestSubmitLog.java` / `QuestInventoryEscrow.java` — 单任务提交/合成/分析/反序列化/流体单元展示图标解析（LegacyAe+FCR+GT FluidDisplay）/流体↔单元等价（DETECT 加总、SUBMIT 空单元装填、真流体 partial drain）/缓存/审计/AE 虚拟 escrow（lock/lockPartial/appendLock/release/commit/超时退回；合成期预锁）
- `webae/dto/Quest*.java` — 任务书 DTO（含 `QuestRelationDto`/`QuestChainPlanDto`/`QuestChainSubmitResultDto`/`QuestClaimResultDto`；`QuestDetailDto` 含 `prerequisites[]`/`dependents[]`/`webClaimable`；`QuestRewardDto` 含 choice 分组字段；图节点含 `iconItemId`/`mainQuest`/`ghost`）
- `webae/api/handler/ServerHealthHandler.java` — 服务器健康 REST handler（`GET /api/server/health` TPS/MSPT/在线人数/300s 历史；参考 Phase 2）
- `webae/api/handler/ServerDiagnosticsHandler.java` — 性能诊断 REST（`GET /api/server/diagnostics`：tick 分段、队列深度、快照采集、慢 HTTP、路由 Top、Config 摘要）
- `webae/perf/WebAePerfProfiler.java` — HandlerTick 分段计时（含 `qqBot`）+ HTTP 路由聚合 + 慢请求环缓；`[debug] webaePerf` → `logs/textech/webae-perf.log`
- `webae/perf/SnapshotWorkerPool.java` — 单线程 daemon 快照调度（single-flight `inFlight`；500ms 软超时仅告警、最多再等 30s 才释放闸门；`HandlerTick` queueDepth≥48 拒收非 forced；diagnostics 暴露 busy/timeouts/skips；AE API 经主线程任务队列）
- `webae/context/NetworkRegistry.java` — 事件驱动玩家→AE 网络注册表（Link/Monitor/chunk/登录钩子 + 5s 健康检查 + `healthy` 状态）
- `webae/context/TileEntityIndex.java` — 维度分组 TE 索引（chunk load/unload 增量维护；由 `HandlerWebTileEntityIndex` 驱动）
- `handler/HandlerWebTileEntityIndex.java` — chunk 事件同步 `TileEntityIndex` + `NetworkRegistry`
- `webae/cache/SnapshotCache.java` — 线程安全 TTL 快照缓存（TTL = `webRefreshIntervalMs * 3`，含 getStale/timestampOf/invalidateType/invalidateTypeSuffix）
- `webae/cache/SnapshotScheduler.java` — 定速分摊快照调度器（经 `SnapshotWorkerPool` 异步投递；storage/GT/pattern/p2p/cells/patterns_rich 仅 `networkId>=0`；owner 级 networks+monitor+scanner；只采集 2 分钟活跃；submit 成功才记 lastCollect；含 forceCollect*）
- `webae/dto/StorageDto.java` — 存储快照 DTO（`CpuEntry` 含 name/进度/存储/协同/产物/坐标 x-y-z-dim/监视器 monitorX-Y-Z-Dim/remainingItems/startItems，Phase 3 扩展）
- `webae/dto/StoragePagedDto.java` — 存储分页响应 DTO（items/fluids/essentia 切片、nextCursor、totalEstimate、summary 字段）
- `webae/dto/PowerDto.java` — 电力/蒸汽快照 DTO
- `webae/dto/RecipeDto.java` — NEI 配方 DTO
- `webae/dto/PatternDto.java` — 样板编码/注入/接口枚举 DTO
- `webae/dto/PatternListEntryDto.java` — 样板总览条目 DTO（patternId/sourceInterface/sourceInterfaceName/slotIndex/crafting/substitute/beSubstitute/author/inputs[27 槽位,null 表空]/outputs/encodedNbt，对应 `GET /api/patterns` 响应项）
- `webae/dto/PatternBrowseEntryDto.java` — browse API 统一条目 DTO（source grid|interface、gridKey、精简 inputs/outputs、displayName/registryName）
- `webae/dto/NetworkMetricHistoryDto.java` — 网络指标历史 DTO（供 `/api/network/metrics`）
- `webae/dto/NetworkMetricFluidHistoryDto.java` — 钉选流体趋势 DTO（`/api/network/metrics/fluids`）
- `webae/dto/NetworkMetricItemHistoryDto.java` — 钉选物品存量趋势 DTO（`/api/network/metrics/items`）
- `webae/dto/NetworkMetricEntityHistoryDto.java` — 钉选 CPU/GT 数值趋势 DTO（`/api/network/metrics/entities`）
- `webae/dto/OrderRequest.java` — 合成订单请求 DTO（含可选 `cpuName`）
- `webae/dto/OrderBatchRequest.java` — 批量订单请求 DTO，含内部 OrderItem（itemName/amount/patternId[可选,按样板下单]）+ 可选 `cpuName`
- `webae/dto/OrderResult.java` — 订单结果 DTO
- `webae/dto/OrderStatus.java` — 订单状态 DTO（含 `cpuName`/`cpuInfo`/`finalProgress`；list 响应 history 数组项）
- `webae/dto/GtMachineDto.java` — GT 机器状态快照 DTO
- `webae/dto/GtMachineListDto.java` — GT 机器列表 DTO
- `webae/dto/SearchResultDto.java` — 聚合搜索单条结果 DTO（`GET /api/search`；type storage/recipe/gt/pattern + 导航字段）
- `webae/dto/PlayerDto.java` — 玩家 DTO（uuid/name/online/onlineMs/lastLogin/lastLogout/skinUrl，对应 `/api/players` 响应项）
- `webae/dto/PlayerLocationDto.java` — 玩家坐标 DTO（uuid/name/x/y/z/dim/online，对应 `/api/players/locations`）
- `webae/WebConsoleUrlHelper.java` — WebAE 控制台 URL 构建（服务端 guest 邀请链接等）
- `webae/onboarding/WebConsoleOnboarding.java` — NetworkLink 放置后 S→C 可点击 onboarding 聊天
- `webae/dto/ChatMessageDto.java` — 聊天消息 DTO（基础字段 + 可选 attachmentId/name/mime/width/height/bytes）
- `webae/power/PowerSampler.java` — 电力采样器（5 秒滑动窗口速率计算，CountDownLatch 主线程采集）
- `webae/snapshot/AeSnapshotCollector.java` — AE2 快照采集器（主线程阻塞 + 回调模式；itemId 改用 registry name，meta 仅在 !=0 时拼接）
- `webae/snapshot/GtSnapshotCollector.java` — GT 机器快照采集器（主线程阻塞 + 回调模式）
- `webae/gt/GtMachineStateReader.java` — GT 机器状态读取器（进度/配方/电力/仓室）
- `webae/gt/GtMachineBinding.java` — GT 机器绑定数据结构
- `webae/metric/NetworkMetricSampler.java` — AE 网络指标历史采样（滑动窗口，供 NetworkMetricHandler；Phase 3.1 per-fluid 钉选）
- `webae/metric/MetricDownsampleUtil.java` — 时序降采样（Phase 3.2 Power 历史 >120 点）
- `webae/recipe/NeiRecipeCollector.java` — NEI handler 反射采集（主路径之一；gridSlots、GT 参数；handlerName 经 RecipeDisplayNames 本地化；itemId 含 meta；客户端主线程）
- `webae/recipe/GameRecipeCollector.java` — vanilla/GT RecipeMap 采集（补充路径；ShapedRecipes grid、GT EU/流体；handlerName 本地化）
- `webae/recipe/RecipeDisplayNames.java` — 客户端 handler 显示名本地化（`合金炉 (gt.recipe.alloysmelter)` 格式）
- `webae/recipe/RecipeItemEntries.java` — ItemStack → RecipeDto.ItemEntry（itemId 对齐 AE：`registry:meta`）
- `webae/recipe/RecipeCacheStore.java` — 服务端配方缓存（无启动全量加载；`ensureLoaded` 懒加载；**禁止**在 Server tick 线程同步全量解析，改走 `WebAE-RecipeCache-Load`；HTTP 线程可阻塞等待；写盘 `web-recipes.json` + meta + chunks；默认上传后清堆；full/lru 仅内存已加载时生效）
- `webae/recipe/RecipeDiskMeta.java` — 磁盘配方目录元数据 DTO（revision/chunkCount/handlers；供 sync manifest，不入全量堆）
- `webae/recipe/RecipeUploadSession.java` — 上传会话跟踪（仅首个 isStart 清缓存）
- `webae/recipe/RecipeSnapshotCollector.java` — 客户端 snapshot 范围配方采集
- `webae/WebAeLocalDataDir.java` — 实例根目录下 `TeXTech/WebAE/` 本地数据目录（NESQL 导入默认路径 + 客户端配方导出；旧 `TeXTechWebAE/` 由 `TeXTechDataMigration` 迁移）
- `webae/recipe/RecipeLocalExporter.java` — 客户端 NEI 配方导出至 `TeXTech/WebAE/web-recipes.json`（纯 JSON）
- `webae/network/RecipeUploadBatcher.java` — 配方上传分批（FML 32767B 上限）
- `webae/network/RecipeUploadThrottler.java` — 客户端 tick 节流上传（`webRecipeUploadBatchesPerTick`）
- `webae/network/PacketWebRecipeUpload.java` — C→S 分批配方上传（packet ID 26；30 KiB body / 29,488-byte JSON；真实玩家 + OP + 主线程；严格顺序 UploadSession）
- `webae/network/PacketWebRecipeUploadAck.java` — S→C 配方上传确认（packet ID 27）
- `webae/search/RecipeSearchRateLimiter.java` — 配方模糊搜索 per-owner 限流（`webRecipeSearchMinIntervalMs`）
- `webae/icon/NesqlIconImporter.java` — NESQL 预渲染图标导入（`/admweb icons import-nesql`）
- `webae/icon/IconNesqlStyleRenderer.java` — **活跃** NESQL 同款单图标 FBO（`GuiContainerManager.drawItem`，64×64 PNG 输出）
- `webae/icon/IconLocalStore.java` — 本机 `icons-local/` 写盘 + pull zip 解压
- `webae/icon/IconLazyConsentChat.java` — 懒加载补渲聊天同意（材质包知情）
- `webae/network/PacketWebUploadTrigger.java` — S→C 触发 recipes/icons/icons_local/icons_pull/icon_verify（packet ID 30）；item-ID JSON 仅接受数组，数量、UTF-8 长度、空值、控制字符和 surrogate 均有界校验，非法输入不启动扫描
- `webae/network/PacketWebIconPullZip.java` — S→C pull zip 分片（packet ID 49）
- `webae/network/PacketWebConsoleTokenNotify.java` — S→C WebAE token 下发与剪贴板复制（packet ID 31）
- `webae/network/PacketWebAlertNotify.java` — S→C owner 告警 HUD（packet ID 50；有界文本、时长/位置/提示音）
- `webae/network/PacketScreenshotUpload.java` / `PacketScreenshotUploadAck.java` — C→S 24 KiB 截图分片（51）与仅终态 S→C ACK（52）
- `webae/network/PacketWebIconExportScope.java` — S→C 图标导出范围 all/snapshot/list + itemId JSON（packet ID 32）；list 必须是受限数组，非法根类型/数量/ID 不更新 pending scope
- `webae/network/PacketWebIconRequest.java` — S→C 按需懒加载单图标渲染请求（packet ID 33；客户端入队 IconLazyRenderQueue，bulk 导出运行中忽略）
- `webae/network/PacketWebMapTileJob.java` — **Phase 4 + AE** S→C 世界地图 HD 瓦片渲染任务（packet ID 34；含 `layer=terrain|ae` + `quality` tier）
- `webae/network/PacketWebMapTileUpload.java` — **Legacy** C→S HD 瓦片上传（packet 35；legacy 模式）
- `webae/network/PacketWorldMapCaptureOffer.java` — S→C 快照上传 consent（37）
- `webae/network/PacketWorldMapCaptureAccept.java` — C→S 接受 consent（38）
- `webae/network/PacketWorldMapCaptureJob.java` — S→C 开始快照采集（39）
- `webae/network/PacketWorldMapSnapshotTileUpload.java` — C→S 快照瓦片/finalize（40）
- `webae/network/PacketWorldMapSnapshotSyncRequest.java` / `PacketWorldMapSnapshotSyncResponse.java` — MC 客户端版本同步（41/42）
- `webae/network/PacketWorldMapSnapshotTilePull.java` / `PacketWorldMapSnapshotTileData.java` — MC 客户端增量拉取瓦片（43/44）
- `webae/network/PacketWebIconResolveNack.java` — C→S 图标懒加载解析失败回执（packet ID 36；itemId + reason）
- `webae/icon/IconExportScope.java` — 导出范围枚举（all/snapshot/list）
- `webae/icon/IconSnapshotItemCollector.java` — 从 SnapshotCache 存储快照收集 itemId（服务端）
- `webae/icon/IconMissingQueue.java` — 404 缺失图标队列（仅 `iconLazyCaptureEnabled`）；聊天同意后向 provider 派发 `PacketWebIconRequest`；mode 强制 `nei`
- `webae/icon/IconDirectCaptureBridge.java` — HTTP 404 同步直渲桥接（latch 等待 PacketIconDirectCaptureResponse；mode 强制 `nei`）
- `webae/icon/IconLazyRenderQueue.java` — 客户端懒加载渲染队列（每 tick 最多 2 个；bulk 导出运行时暂停；`ClientProxy` FML bus 注册）
- `webae/icon/IconRenderGuard.java` — 离屏图标导出后复位 Tessellator/GL/Framebuffer（防止 GTNH 自定义渲染器遗留 `Already tesselating!`）
- `webae/icon/IconGridExporter.java` — **归档** NEI 网格批量 FBO（16×8）；活跃路径不再调用
- `webae/icon/IconExportResolver.java` — 活跃链：流体特殊 → NESQL drawItem → placeholder；`resolveLegacy` 保留旧多级回退
- `gui/guiscreen/GuiIconExportScreen.java` — 隔离 GUI 驱动 bulk 导出（每帧 `iconRenderPerTick` 个 NESQL 单渲；暂停世界渲染）
- `gui/guiscreen/GuiIconVerifyScreen.java` — 单物品三列核验 GUI（游戏内渲染 / 本次导出 PNG / 缓存 PNG；PNG 等比显示，动态纹理在打开/关闭边界创建与释放）
- `webae/icon/IconRenderMode.java` — 图标 mode 枚举（**仅 `NEI` 活跃并对外暴露**；其余常量 `@Deprecated` 仅归档代码用，命令/API/补全不列出）
- `webae/icon/IconRenderStrategy.java` + `IconRenderStrategies` + `IconRenderStrategy*` — 旧策略实现（归档，非活跃）
- `webae/icon/IconBlockRenderer.java` — ItemBlock + RenderBlocks FBO 迷你场景（归档回退）
- `webae/icon/IconNeiStyleRenderer.java` — NEI 槽位布局检测与渲染入口（归档）
- `webae/icon/IconUploadProgress.java` — 游戏内聊天/action bar 导出进度（节流 iconProgressChatIntervalMs）
- `webae/icon/IconItemId.java` — 图标缓存 canonical key 与 lookupCandidates（mod:id ↔ mod:id:0 双向；与 RecipeItemEntries.buildItemId / 前端 iconLookupIds 对齐）
- `webae/icon/IconTileResolver.java` — 解析 `mod:tile.BlockClass[:meta]` 伪 id（拓扑 BlockCableBus 等）→ Block ItemStack，供 `IconItemEnumerator` / `IconBlockRenderer` 方块正面导出
- `webae/icon/IconItemEnumerator.java` — 客户端图标上传任务枚举（优先 NEI ItemList.items 含 GT meta，回退 itemRegistry meta=0）
- `webae/icon/IconStore.java` — 服务端物品图标材质包存储单例（`TeXTech/WebAE/icons/<pack>/<mode>/<sanitizedItemId>.png` + `manifest.json`；legacy 根 PNG 迁移；单 PNG 512 KiB/2048px、整批 16 MiB；canonical/symlink/目标碰撞检查；整批 staging/promote/rollback 后单次刷新索引）
- `webae/icon/IconAtlasSampler.java` — 客户端图标图集 CPU 采样（归档回退；**流体 registry 键 `fluid:` 由 IconFluidRenderer GL 染色**）
- `webae/icon/IconFluidRenderer.java` — 流体/流体单元专用检测与 GL 染色（registry 流体 still 图标 + AE2FC `ae2fc:fluid_drop` 液滴外形 + `fluid.getColor(FluidStack)` 染色 + AE2 `POST_HOOKS` 叠加；流体存储单元等强制游戏内渲染路径）
- `webae/icon/IconGlFallback.java` — GL FBO 回退（流体特殊路径仍活跃；其余归档）
- `webae/icon/IconRenderer.java` — 客户端图标导出调度（GuiIconExportScreen + IconExportResolver/NESQL；固定 nei；snapshot/list 范围；bulk 开始时清空 IconLazyRenderQueue；ClientTick 异步上传；按需单图标 renderAndUploadSingle）
- `webae/network/PacketWebIconUpload.java` — C→S 分批图标上传（packet ID 28，30000 bytes 包体；24 KiB 严格顺序非空块；8 MiB/342 块；每真实玩家 2 会话、全局 16 会话/32 MiB 预留、120 秒 TTL；严格 JSON object/Base64/唯一 item ID；完整 bundle 事务提交后才 `recordDefaultPack` 与 ACK）
- `webae/network/PacketWebIconUploadAck.java` — S→C 图标上传确认（packet ID 29，回传进度+消息；懒加载单图标 `1 icons stored` 不写聊天栏，仅 bulk/多图标完成提示）
- `webae/network/PacketIconDirectCaptureRequest.java` — S→C 图标直渲请求（packet ID 47）
- `webae/network/PacketIconDirectCaptureResponse.java` — C→S 图标直渲 PNG 回传（packet ID 48）
- `client/icon/IconDirectCaptureClientWorker.java` — 客户端 tick 处理直渲队列，调用 `IconRenderer.renderPngBytes`
- `webae/player/PlayerInfo.java` — 玩家信息 DTO（uuid/name/firstLogin/lastLogin/lastLogout/totalOnlineMs/online）
- `webae/player/PlayerInfoStore.java` — 服务端玩家信息存储单例（内存 Map<UUID,PlayerInfo>，touchLogin/touchLogout/reconcileOnline/getOnlinePlayers/getAllPlayers/effectiveOnlineMs，防抖保存到 `TeXTech/WebAE/web-players.json`）
- `webae/player/PlayerOnlineSampler.java` — 在线人数历史采样单例（每 30s 采样一次 `MinecraftServer.getConfigurationManager().playerEntityList.size()`，维护最近 120 个采样点的滑动窗口，供 `GET /api/players/online/history` 趋势组件使用；由 `HandlerWebPlayerTracker.onServerTick` 触发）
- `webae/player/SkinUrlResolver.java` — 从 GameProfile textures property 取 base64 解码 JSON 取 `textures.SKIN.url`，离线玩家返回 null
- `webae/player/WebAePlayerState.java` — 每玩家 WebAE 管理状态 DTO（disabled / lastActive / request 计数）
- `webae/player/WebAePlayerStateStore.java` — `web-player-states.json`；`isDisabled` / setDisabled / setEnabled / touchRequest
- `webae/chat/ChatMessage.java` — 聊天消息 DTO（基础字段 + 可选截图附件元数据）
- `webae/chat/ChatMessageStore.java` — 服务端聊天环形缓冲与 `appendAttachment`，防抖持久化 `web-chat.json`
- `webae/screenshot/ScreenshotUploadService.java` — 每玩家单会话/冷却、全局内存/并发/队列限制、乱序拒绝、JPEG 头后台校验，空闲无 Tick 工作
- `webae/screenshot/ScreenshotAttachmentStore.java` — `TeXTech/WebAE/chat-attachments/` 原子写入与文件数/MiB 双上限惰性清理
- `webae/pattern/CraftingGridPatternQuery.java` — ICraftingGrid 全量样板枚举（不按产物去重；`getByIndex` 供 grid 详情 API；供 browse API 与助手共用逻辑）
- `webae/pattern/PatternBrowseService.java` — Grid+Interface 双源合并；主线程 `buildAndStoreCache` + HTTP 线程 `paginate`/`browse`（只读 TTL/stale 缓存）；`invalidateAll`/`invalidateNetwork`/`getGridEntry`（ownerUuid）
- `webae/pattern/PatternBrowseInvalidationGridCache.java` — AE `IGridCache`：监听 `MENetworkCraftingPatternChange` 与 grid split/join，调用 `PatternBrowseService.invalidateAll()`（debug：`webaePatterns`）
- `webae/pattern/PatternBrowseGridCacheRegistrar.java` — postInit 注册上述 grid cache（`AdvanceDataMonitor.postInit` 在 `AeCompat.init()` 之后调用）
- `webae/pattern/PatternEncoder.java` — 样板 NBT 编码器（保留 shaped 空槽、meta/item NBT、整数 Count；软反射 AE2FC 流体 drop；可选 PH 未消耗输入包装）
- `webae/pattern/BlankPatternHelper.java` — Phase 6：从 AE 网络库存扣除空白样板（`materials().blankPattern()`）
- `webae/pattern/InterfaceLocator.java` — 完整方块/线缆部件 ME 接口定位与跨维度枚举；经 `InterfaceDuality.getPatterns()` 读真实槽位，地址 `x:y:z:dim@SIDE`，统一保存/Grid 归属/样板变更事件
- `webae/pattern/PatternInjector.java` — 样板注入器（主线程；目标/槽位/重复校验后才按 `consumeBlank` 扣空白样板）
- `webae/pattern/PatternWebBufferStore.java` — owner/network 隔离的网页专用物理样板缓存，最多 54 条，原子持久化 `TeXTech/WebAE/web-pattern-buffer.json`
- `webae/balance/NetworkBalanceSuggestion.java` — 多网平衡建议 DTO（Phase 8）
- `webae/balance/NetworkBalanceCollector.java` — 对比缓存 storage snapshot 生成短缺/余量建议
- `webae/topology/TopologyNode.java` — 拓扑节点 DTO（subtype、patternCount、branchIndex、layoutSector、layout 坐标、simGrid、cellSlots[]、patternSlots[]、devices、cpuSummary）
- `webae/topology/CraftingCpuTopologyCollector.java` — 通过 `ICraftingGrid.getCpus()` 聚合 `CraftingCPUCluster` 为单节点设施（协处理器/存储/构成方块坐标）
- `webae/topology/TopologyEdge.java` — 假线缆边 DTO（cableType + branchIndex + emptyBranch + channelsSimulated/channelsReal + pathPoints[]）
- `webae/topology/AeCableTextureLoader.java` — AE2 默认 Fluix 线缆 part 材质 classpath 加载（拓扑线缆模拟；**已弃用**，`topologySimulatedEnabled` 默认关）
- `webae/topology/TopologyRules.java` — 细粒度 subtype 分类（含 `level_maintainer`）+ 频道 cost + tree/star 布局常量 + 64×64 spatial bin
- `webae/topology/NetworkStatusEnumerator.java` — 与游戏内网络工具相同：`getMachinesClasses()` + `getMachineRepresentation()`；subtype 分类；驱动器槽位元件/样板计数
- `webae/topology/TopologyFacilityGrouper.java` — 按 `subtype|representationItemId` 聚合（网络工具 merge-by-item parity）
- `webae/topology/ChannelBranchAllocator.java` — 4×8 智能车道分配（podKind 偏好车道 + 同 subtype 优先；溢出标记 overflow）
- `webae/topology/LogicalTopologyBuilder.java` — ae_budget_v2 频道预算树（dense 32 → 4×lane 8 → 角色舱 → 设备；零频道 hub orbit）+ `buildStar()` 双环摘要
- `webae/topology/TopologySnapshotStore.java` — 磁盘持久化 `TeXTech/WebAE/topology/<owner>-<network>.json`（logical/spatial 分字段 + lastCaptureAt）
- `webae/topology/NetworkDeviceEnumerator.java` — （legacy）`IGrid.getMachinesClasses()` 设备枚举
- `webae/topology/SimulatedLayoutBuilder.java` — tree/star 模拟网格 + Manhattan pathPoints（仅 `topologySimulatedEnabled`）
- `webae/topology/FakeChannelAllocator.java` — spatial 星形假线缆
- `webae/topology/TopologySnapshot.java` — 拓扑图构建器（logical=树状 + network tool 数据源；spatial=空间 bin + 星形）
- `webae/topology/TopologyCache.java` — 手动快照内存缓存 + 冷启动磁盘加载 + `topologyCacheTtlMs` 采集冷却（默认 10 秒）
- `webae/topology/TopologyInvalidationGridCache.java` — AE `IGridCache` 占位（手动快照模式不自动失效）
- `webae/topology/TopologyGridCacheRegistrar.java` — postInit 注册拓扑 grid cache（`AdvanceDataMonitor.postInit`）
- `webae/topology/P2pTunnelDto.java` / `P2pMapSnapshot.java` / `P2pTunnelEnumerator.java` — **Phase10** P2P 频率分组地图（`GET /api/network/p2p`；反射读 frequency）
- `webae/worldmap/WorldMapMarkerDto.java` — 世界地图标记 DTO（`dim:x:y:z` + nodeId/type/iconItemId/坐标）
- `webae/worldmap/WorldMapMetaDto.java` — 世界地图 meta 响应（维度 bbox、allowedChunks/minChunk*、tilePx、views[]、obliqueDirections[]、qualityTiers[]、max/defaultQualityTier、flatRenderEngine/obliqueRenderEngine、hdAvailable、markerCount、boundsTooLarge）
- `webae/worldmap/WorldMapQualityTier.java` — 四档清晰度 low/medium/high/ultra（tilePx/ppb；ultra 离线 256px 回退）
- `webae/worldmap/WorldMapMarkerBuilder.java` — 从 logical `TopologySnapshot.nodes[].devices[]` 扁平化 markers；`cpu`/`controller` 多方块各仅 1 anchor marker（CPU：监视器 > 协处理器 > 存储 > 首 unit；控制器：最低 Y 再 x/z）
- `webae/worldmap/WorldMapChunkSetBuilder.java` — AE 设备占用 chunk + Chebyshev padding 精确 allowed 集（L 形网络不填充空白 chunk）
- `webae/worldmap/WorldMapBoundsBuilder.java` — per-dim marker bbox + allowed chunk 集/meta 组装（paddingChunks/maxChunks clamp）
- `webae/worldmap/WorldMapHandler.java` — 世界地图 REST（meta/markers/tiles + **snapshot manifest/status/request**；client_only 下 tile 只读快照；snapshot request 的 live 玩家查询/建 job/Forge 发包经 `HandlerTick` 主线程，队列深度 128 背压、HTTP 15 秒有界等待且取消未开始的超时任务）
- `webae/worldmap/WorldMapSnapshotStore.java` — 按 network 版本化快照磁盘存储 `map-snapshots/`；对 manifest/current/tile 的大小、结构、坐标、layer、source、sha256 和 owner UUID 做有界校验，拒绝 snapshot root 及下方任一 symlink path segment，`loadCurrent()` 深度验证后才返回
- `webae/worldmap/WorldMapLogicalIndex.java` — snapshot 版本目录的有界 `logical-index.json` 侧车（version/timestamp/logicalAvailable/markers/aePlacements）；发布时 best-effort 写入，旧版本缺失时保持 unavailable，不以当前拓扑冒充历史
- `webae/worldmap/WorldMapSnapshotDiffOptions.java` / `WorldMapSnapshotDiffDto.java` / `WorldMapSnapshotVersionsDto.java` / `WorldMapSnapshotDiffService.java` — 仅比较 retained current/previous；tile 以 manifest key+SHA-256 判定，marker 支持 added/removed/changed/唯一 moved，支持维度/坐标/图层过滤；详情最多 1000 条但 summary 保留截断前总数，不可信 manifest 返回 `unknown`
- `webae/worldmap/WorldMapAnnotationDto.java` / `WorldMapAnnotationRequest.java` / `WorldMapAnnotationResult.java` / `WorldMapAnnotationStore.java` / `WorldMapAnnotationService.java` — owner/network 隔离的服务端注释 CRUD，持久化 `map-annotations/{owner}/{network}.json`；每网 500 条/2 MiB、Unicode code-point 与颜色/版本范围校验、共享锁、临时文件原子替换及 symlink/路径逃逸防护
- `webae/worldmap/WorldMapVersionHandler.java` / `WorldMapAnnotationHandler.java` — versions/diff 与 annotation REST adapter；每个入口独立复用网络 ACL，mutation 追加 guest 只读门禁并使用认证 actor，显式映射 unknown/conflict/not-found/validation 状态
- `webae/worldmap/WorldMapPacketAuthorization.java` — 世界地图 packet 的服务端授权与范围校验：canonical owner UUID、network/version/dimension/chunk/tile 上限、`terrain|ae` 闭集合，以及 owner/OP 或当前 active capture actor 判定
- `webae/worldmap/WorldMapCaptureCoordinator.java` — consent 状态机 + pending 去重 + 附近玩家筛选；最多 32 active jobs、90 分钟 idle / 2 小时 absolute TTL，断线/stop/restart 清理未发布版本，严格解析三源 `sourceStatsJson`
- `webae/worldmap/WorldMapJourneyMapFsReader.java` — SP/JourneyMap exact-world 文件读取；root→tile 每段拒绝 symlink，lexical/canonical containment，1 MiB/IHDR/CRC/尺寸预检后完整 ImageIO 解码复核
- `webae/worldmap/WorldMapConsentChat.java` — 服务端可点击聊天（接受/拒绝 → `/admweb wm y|n`）
- `webae/worldmap/WorldMapSnapshotMode.java` — `client_only` / `legacy` 模式判定
- `webae/worldmap/WorldMapAePlacementRecord.java` / `WorldMapAePlacementCollector.java` / `WorldMapAePlacementSupport.java` — logical 快照采集 AE 放置点（设备 + 线缆 + part）→ `TopologySnapshot.aePlacements`
- `webae/worldmap/WorldMapAeCategory.java` — AE 设备启发式类别（世界地图 ID 图 R 通道）
- `webae/worldmap/WorldMapAeOverlayRenderer.java` — 服务端 AE 类别 ID 透明瓦片（flat/oblique；缓存 `{view}/ae-id/`）
- `webae/worldmap/WorldMapTileLayer.java` — `terrain|ae` 层 id + 缓存路径 `{view}/ae/...`
- `compat/WorldMapBlockCompat.java` — 服务端 mod 方块/icon 颜色回退（GWM 风格扩展点）
- `webae/worldmap/WorldMapView.java` — 视角枚举（flat + oblique_se/sw/ne/nw mineshot 风格斜视角）+ `webWorldMapViewsEnabled` 解析 + UI `views[]`/`obliqueDirections[]`
- `webae/worldmap/WorldMapObliqueDirection.java` — 斜视角罗盘方向（se/sw/ne/nw）+ mineshot pitch/yaw + 软件渲染坐标 remap
- `webae/worldmap/WorldMapTileInvalidator.java` — 按 network 范围批量失效缓存瓦片（刷新/快照/设置变更后重渲染）
- `webae/worldmap/WorldMapBlockPalette.java` — **Phase B** block+meta → RGB 硬编码调色板（`knownColorFor` 供三级回退；未知 `#555555`）
- `webae/worldmap/WorldMapBlockTextureSampler.java` — **Phase 2/3** 服务端 mod JAR 方块纹理 PNG 读取 + LRU 平均色采样（顶/侧/底面候选路径）
- `webae/worldmap/WorldMapBlockColorResolver.java` — **Phase 2/3** 三级颜色回退 + `BlockFace` 面参数：纹理采样 → 调色板 → `#777777`/`#555555`
- `webae/worldmap/WorldMapFlatRenderer.java` — **Phase B/2** 单 chunk 俯视（flat）PNG 渲染（软方块穿透；主线程；chunk null → 斜纹占位）
- `webae/worldmap/WorldMapIsoRenderer.java` — **Phase 3 + Phase 1** mineshot 风格斜视角列投影软件渲染（`WorldMapChunkContext` 跨 chunk 墙高；四向 orbit + BlockColorResolver 面采样）
- `webae/worldmap/WorldMapSideRenderer.java` — **Legacy** 正交侧视（north/south/east/west；已由 oblique 取代，保留供参考）
- `webae/worldmap/WorldMapRenderSupport.java` — **Phase 3 + UV engine** 渲染器共享工具（world/chunk 访问、软方块判定、flat/oblique 分发；`webWorldMapRenderEngine` uv/legacy）；统一执行 bounded PNG signature/IHDR、尺寸（≤2048）和文件大小（≤1 MiB）校验
- `webae/worldmap/engine/WorldMapChunkContext.java` — **Phase 1** 3×3 chunk 快照（`webWorldMapChunkPadding`）世界坐标 block/light/biome 查询
- `webae/worldmap/engine/WorldMapTextureRegistry.java` — **Phase 1** mod JAR 方块面纹理 BufferedImage LRU（`webWorldMapTextureCacheMax`）
- `webae/worldmap/engine/WorldMapFaceRasterizer.java` — **Phase 1/2** ppb×ppb UV 栅格 + 单点 `sampleFaceRgb`（ray 命中）+ biome tint + 面光照
- `webae/worldmap/engine/WorldMapBiomeTint.java` — **Phase 1** 草/树叶/水 biome 乘色（MC 1.7.10 `getBiomeGrassColor`/`getBiomeFoliageColor`）
- `webae/worldmap/engine/WorldMapFaceLighting.java` — **Phase 1** 面朝向 × sky/block light 简易平滑光照
- `webae/worldmap/engine/WorldMapFlatUvRenderer.java` — **Phase 1** 俯视 UV 渲染器（软方块 alpha 叠层；替代平均色 painter 主路径）
- `webae/worldmap/engine/WorldMapObliqueProjection.java` — **Phase 2** 斜视角正交投影（mineshot 相机 → 世界射线）
- `webae/worldmap/engine/WorldMapAeObliqueRayRenderer.java` — AE 斜视角 ray（与地形共用 ObliqueProjection；输出 categoryId 像素）
- `webae/worldmap/engine/WorldMapIsoRayRenderer.java` — **Phase 2** 斜视角逐像素光线追踪（体素 DDA + UV/生物群系/光照；透明叠层）
- `webae/worldmap/engine/WorldMapRenderEngines.java` — `webWorldMapRenderEngine` / `webWorldMapObliqueEngine` / `webWorldMapLowTierObliqueEngine` 解析（uv/legacy/ray）
- `webae/worldmap/engine/WorldMapZoomPyramid.java` — **Phase 3** zoom 金字塔（z0 chunk 写入后 2×2 box 降采样合成 z1/z2；`webWorldMapZoomLevels` / `webWorldMapZoomBudgetPerTick`）
- `webae/worldmap/engine/WorldMapBlockPatch.java` — **Phase 4** 方块局部 AABB patch（block-local 0–1 空间）
- `webae/worldmap/engine/WorldMapBlockPatchRegistry.java` — **Phase 4** JSON + 内置楼梯/半砖/GT patch 注册与 ray-AABB 命中（`registryPrefix` / `classContains`）
- `webae/worldmap/engine/WorldMapGtPatchResolver.java` — **Phase 4** GT MTE 管道/线缆/框架动态 patch（`getConnections()`）
- `webae/worldmap/engine/WorldMapServerAtlas.java` — **Phase 4** 服务端纹理 atlas 烘焙（`webWorldMapServerAtlasEnabled` / `webWorldMapServerAtlasPx`）
- `webae/worldmap/engine/gwm/WorldMapFaceLighting.java` — **Phase C** GWM 面朝向光照计算（增强 sky/block light 平滑）
- `webae/worldmap/engine/gwm/WorldMapFaceRasterizer.java` — **Phase C** GWM 面光栅化（ppb 像素 UV + 光照采样）
- `webae/worldmap/engine/gwm/WorldMapAe2PatchResolver.java` — **Phase C** AE2 方块面片解析（线缆/设备连接 AABB patch）
- `webae/worldmap/WorldMapRenderExecutor.java` — **Phase C** 渲染线程池执行器（`webWorldMapRenderThreads` 配置线程数）
- `webae/worldmap/dynmap/WorldMapDynmapDetector.java` — **Phase B** Dynmap/GWM 存在性检测（`dynmapTileRoot` 目录扫描）
- `webae/worldmap/dynmap/WorldMapDynmapCoordMapper.java` — **Phase B** Minecraft 坐标 ↔ Dynmap 瓦片坐标映射（128 block/tile z0）
- `webae/worldmap/dynmap/WorldMapDynmapTileProvider.java` — **Phase B** Dynmap 外部瓦片 HTTP 代理读取（`GET /api/worldmap/dynmap-tiles/{world}/{z}/{x}/{y}.png`）；限制 world/zoom/坐标/perspective，并以 canonical path containment 防止脱离受控 Dynmap tile root
- `webae/worldmap/WorldMapClientCaptureMode.java` — 客户端 GL 截图策略解析（off / ultra_only / when_online）
- `webae/worldmap/WorldMapTerrainFallback.java` — 渐进占位：低清缓存 tier 或 Dynmap chunk 裁剪
- `webae/worldmap/dynmap/WorldMapDynmapWorldNames.java` — 维度 → Dynmap world 目录名
- `webae/worldmap/dynmap/WorldMapDynmapChunkCropper.java` — 从 128-block Dynmap 瓦片裁剪 16-block chunk PNG
- `assets/textech/worldmap/patches/*.json` — GT 机器/外壳/结构/管道 patch 规则
- `webae/worldmap/WorldMapQualitySupport.java` — **Phase 4** AE chunk 地形清晰度 +1 档解析
- `webae/worldmap/WorldMapTileCache.java` — **Phase B/3/4 + AE + q{tier}** 磁盘缓存 `TeXTech/WebAE/map-tiles/{view}/q{tier}/[{ae}/]{dim}/{cx}/{cz}.png`（旧 `config/textech/web-map-tiles/` 只读回退；`.hd` 标记 + `writeHd`/`isHd`；`invalidateAllTiers`）
- `webae/worldmap/WorldMapTileQueue.java` — **Phase B/3/4 + AE + quality** 分 tick 预算队列（ultra+HD 在线跳过服务端回退；对接 `WorldMapPrefetchQueue`）
- `webae/worldmap/WorldMapPrefetchQueue.java` — invalidate 后分批入队 prefetch，避免单 tick 灌满队列
- `webae/worldmap/WorldMapTilePrefetcher.java` — invalidate/刷新后按 allowedChunks 批量 `enqueueChunkPair`
- `webae/worldmap/WorldMapTileProgressTracker.java` — per-chunk terrain/ae 状态追踪 + `GET /api/worldmap/progress` JSON
- `webae/worldmap/WorldMapHdSupport.java` — **Phase 4** 客户端 GL 可用性判定与 provider 解析（owner/actor 在线 + 目标维度）
- `webae/craft/CraftTreeNodeDto.java` / `CraftTreeCalculator.java` — **Phase 4.1** NEI 配方递归材料树 + `inStock`/`toCraft`/`patternId` + 存储缺口
- `webae/favorites/WebFavoritesStore.java` — **Phase 4.2** `TeXTech/WebAE/web-favorites.json` 按 owner 收藏
- `webae/planner/PlannerFlowExporter.java` — **Phase 4.3** Factory Flow / gtnh-flow 互操作导出
- `webae/api/handler/FavoritesHandler.java` — **Phase 4.2** `GET/PUT /api/favorites`
- `webae/events/EventStreamHub.java` — **Phase9** SSE 订阅 hub（告警广播 + 15s 心跳；`HandlerTick` tick）
- `webae/monitor/MonitorPreviewDto.java` / `MonitorPreviewCollector.java` — 监视器单槽 type-aware 预览：`scalar`、`series[]`、`categories[]`、`rows[]`，可视序列最多 240 点
- `webae/display/` — `DisplayStore` / `DisplayRecord` / `DisplayCaptureService` / `DisplayCapturePage`；`api/handler/DisplayHandler.java`（`/api/display` 发布、layout、`frame.jpg`、`frame-status`、touch；`render.html` 仅调试；viewToken 只读）
- **Phase 3 集成** — `webae/scanner/LinkScannerCollector.java` + `LinkScannerBlockDto`（I1 已加载区块 Link 镜像）；`webae/monitor/MonitorBindingCollector.java` + DTOs（I2 监视器绑定只读）；`webae/api/handler/ScannerHandler.java`/`MonitorHandler.java`/`MonitorPreviewHandler.java`/`PlannerHandler.java`（I3 读/写 `plans.json` + `POST /api/planner/export-flow`）；`webae/assistant/WebAssistantService.java` + `WebAssistantRateLimiter.java` + `AssistantHandler.java`（I4 规则查询 2s 限流）；`webae/cells/NetworkCellSummaryCollector.java` + `CellSummaryHandler.java`（I5 无限元件/字节摘要）；`webae/pocket/PocketOverviewCollector.java` + `PocketHandler.java`（I6 OP 只读概览，无物品内容）；`CraftTreeHandler.java`/`EventStreamHandler.java`/`P2pHandler.java`（Phase6/9/10 REST）
- **Phase 3 告警** — `config/ConfigWebAlertsLoader.java` 加载/保存 `TeXTech/WebAE/web-alerts.json`；`webae/alerts/WebAlertsConfig.java`/`WebAlertsConfigValidator.java`/`WebAlertEngine.java`/`WebAlertStore.java`/`WebAlertHistoryStore.java`/`WebAlertHistoryEntry.java`/`WebAlertDto.java`/`WebhookDispatcher.java`/`AlertDeliveryTransport.java`/`QqIdDiscovery.java`/`QqIdProbeParser.java`/`QqGatewayClient.java`/`QqIdProbeService.java`/`AutomationCooldownTracker.java`（浏览器、owner 聊天/HUD、旧 Webhook、QQ 官方机器人含目标 ID 探测、微信公众号、SMTP 邮件、企业微信机器人/自建应用；外部 I/O 固定 512 队列 + 2 daemon worker + 重试/熔断）+ `webae/network/PacketWebAlertNotify.java` / `renders/WebAlertHudRenderer.java` + `webae/order/OrderSubmitService.java`（Phase 3.3 服务端线程提交合成）+ `AlertsHandler.java`（A1–A5 + `server_tps_below` + `automation_craft` 历史 + `PUT /api/alerts/rules` OP 编辑与投递状态 + QQ ID probe）；`HandlerTick` 对告警引擎独立 perf 分段，`WebAlertEngine` 每告警 tick 最多轮转 2 owner 并复用 storage 索引；前端 `AlertsRulesEditor.tsx` / `QqIdProbeModal.tsx` 编辑多渠道目标与 TPS 安全参数
- **Phase 7 下单模板** — `config/ConfigWebOrderTemplatesLoader.java` + `webae/order/WebOrderTemplate.java`/`WebOrderTemplateItem.java`/`WebOrderTemplatesStore.java`/`WebOrderTemplatesValidator.java` + `OrderTemplatesHandler.java`（`GET/PUT /api/order/templates`）；前端 `utils/orderTemplates.ts` + `components/patterns/OrderTemplatesModal.tsx` + `AeOrdering.tsx`/`OrderBatchPanel.tsx` 保存/加载/重命名/删除/缺口补货
- **Phase 4a 聚合搜索** — `webae/search/WebSearchService.java`（合并 snapshot storage/GT、RecipeCacheStore、PatternBrowseService）；`webae/search/WebSearchRateLimiter.java`（500ms/owner）；`webae/dto/SearchResultDto.java`；`SearchHandler.java`；前端 `hooks/useGlobalSearch.ts` + `CommandPalette.tsx` 扩展（API 优先 + snapshot fallback）
- **Phase 4b 可见性轮询** — 前端 `hooks/usePageVisibility.ts` + `hooks/useVisibilityAwarePolling.ts`；`AppContext` `pauseRefreshWhenHidden`/`refreshPaused`；Settings 开关 `webae_pause_refresh_when_hidden`；覆盖全局 refresh/连接心跳/告警/指标/Chat/AeOrdering/usePlayers
- **UI 全量备份/默认布局** — `webae-frontend/src/utils/uiSettingsBundle.ts`（`WebUiSettingsBundle` v1 collect/apply）；`components/settings/SettingsBackupPanel.tsx`；公开 `GET /api/ui-defaults` + `/admweb defaults`；jar/实例 `ui-defaults.json`
- **WebAE 仪表盘组件设置** — `components/dashboard/EditWidgetModal.tsx`（编辑入口）、`WidgetPinEditor.tsx`（列选择与钉选搜索）、`utils/dashboardColumns.ts`（各数据源列定义与 `undefined`/空数组语义）、`utils/dashboardPins.ts`（钉选当前值解析）；七类共享渲染入口收敛到 `WidgetContent.tsx` + `ScalarWidgetRenderer.tsx`，`PowerWidgetContent.tsx` 复用 `resolveProgressPercent`。改列契约时必须同步所有入口和测试，切换数据源需清除旧列 key；物品搜索需覆盖 displayName/registryName/itemId 并保留 meta=0
- **WebAE → 游戏显示器** — 整页默认发布到 `/api/display` 并复制 `textech-webae-display-binding`，失败可回退 snapshot；`utils/monitorWidgetBundle.ts` 另提供 `textech-monitor-widget-bundle` v1（七类共享 kind、Dashboard 顺序、最多 36、同一 JSON 入口可重导入）。`radarChart` 与复合 widget 只走整页 surface。两条路径都不得包含密钥，也不得新增 API 轮询或 MC Tick 工作
- `webae/debug/WebAeDebugLog.java` — WebAE 分功能 debug 日志（阶段 3.3，仿 `DataLoomDebugLog`；`Feature` 枚举 ICONS/CHAT/DASHBOARD/SYNTHESIS/PATTERNS，每功能写独立 `logs/textech/webae-<feature>.log`，gate by `Config.webDebugXxx` 开关；`info`/`warn`/`isEnabled`；关键错误仍走 `AdvanceDataMonitor.LOG` 无条件）
